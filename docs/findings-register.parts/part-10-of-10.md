| 204 | P1 | The release-state disclosure was nested inside the header node, so every APP-tier route withheld it, including `/advisor`, which is publicly reachable | Closed in PKG-A11Y-1 | Found while fixing finding 147, not reported from a journey. `src/app/[locale]/layout.tsx` built the preview notice and the marketing strip as the two branches of one ternary INSIDE the `header` node it passed to `ChromeGate`, so a switch whose stated purpose is navigation was also deciding a disclosure. The layout's own comment says the notice is "persistent and site wide rather than a per-page banner that some routes forget to mount"; the APP tier defeated exactly that. `globals.css:285` hides every `.topnotice` except `.preview-notice` below 640px, which is direct evidence the preview notice is treated as a disclosure that must survive everywhere. User consequence: `/advisor` is in `SITEMAP_ROUTES` in `src/lib/routePolicy.ts`, so it is publicly indexable once the release gates pass, and it was rendering figures with nothing on the page saying the deployment is sample data. `/docs` renders its own `SampleBanner`; `/dashboard`, `/admin` and `/messages` are session gated; `/advisor` is neither. Fix: `ChromeGate` takes a `notice` slot rendered unconditionally on all three tiers, and the layout hoists the ternary into `previewNotice` and `marketingNotice`. Exactly one is non-null for a given PREVIEW value and the notice slot precedes the header slot, so the rendered DOM order is byte-identical to before. Two guards hold the structure: that `{notice}` is not on a line mentioning `tier`, and that the header node contains no `preview-notice`. Evidence: automated source scan and guard tests. Not verified against a live APP-tier render, because `/advisor`, `/dashboard`, `/admin` and `/messages` are session gated and no session is obtainable in this container. |
| 205 | P1 | `/docs` renders no links at all, and the control that looks like a back button is a `<span>` | Closed in PKG-A11Y-1 | Found while checking finding 147's claim that it "does not generalise to any other route", which was recorded in `docs/a11y-p1-triage.md` and turned out to be true only of navigation. `src/app/[locale]/docs/page.tsx` contained zero `<Link>` elements. Its top bar drew a 32px disc holding a chevron, in the corner where a back control belongs, as a `<span>`: not focusable, not activatable and pointing nowhere. "Download all" and "Share" are `<span className="btn secondary sm">` for the same reason, and remain non-functional by design because the documents are sample data. User consequence: `/docs` is an APP route, so it has no header, no footer and no tab bar, and both of its rails are hidden below 980px and 680px (sat-platform.css:623-624). A keyboard or screen-reader user reaching it had no route off the page but the browser's back button, which SC 2.4.5 does not accept as a way to reach a page. Fix: the chevron is a real `<Link href={`/${params.locale}`}>` with `aria-label={d.backHome}`, a new bilingual key (EN "Back to home", AR "العودة إلى الرئيسية", matching `advisor.backHome`). The hit area is 44px to clear the SAT touch floor while the disc stays 32px, with a negative margin absorbing the difference so the row's layout is unchanged; the inner disc is `aria-hidden`. The guard walks every APP route, follows one level of `@/components` import, and requires either a `redirect()` or a `<Link>` whose href leaves the route. Evidence: automated source scan and guard tests. Not exercised in a browser, because `/docs` is behind `PRIVATE_PREFIXES` and the local production build cannot run in this container. |
| 206 | P0 | `/api/viewings` and `/api/signup` reported success for a request they had not stored, and both clients rendered the confirmation | Closed in PKG-E1-READINESS slice A | Recorded here on closure rather than on discovery, which is the wrong order and is stated as such: the defect was known, was written into `src/app/api/viewings/route.ts` as a comment saying it was "recorded rather than half fixed", and never reached this register, so nothing counted it. Both routes ended validation, asked for a database client, and on finding none answered `NextResponse.json({ ok: true, note: "supabase not configured (request not stored)" })`. A 200 with `ok: true`. Both clients test `res.ok`, which is the status and not the payload, so `src/components/ListingEnquiry.tsx` confirmed a viewing that did not exist and `src/components/SignupFlow.tsx` reached its "Request received" card, which then lists what happens next by role: three specific promises about a request nobody had written down. The `note` that admitted it sat in a field neither client displays. **Fixed** by refusing with the platform's existing `storage_unavailable` code and a 503, which is what nine other routes already do for the identical condition, so no new code was invented. Three further repairs came with it: the signup route parsed its body outside a guard, so a malformed request threw out of the handler and Next answered a bare 500 with no code; the signup route logged the whole PostgREST error object, whose `details` field quotes the failing row back and would write an applicant's email address into the log on a unique violation; and the shared `storage_unavailable` sentence said "File storage is unavailable", which was wrong on the four row surfaces that had adopted the code and would have been wrong on these two. Eight regression tests in `src/lib/functionalTruth.test.ts`, six of which call the real handlers rather than a mock, because a test process has no Supabase environment and so takes the not-stored path by default. The class guard was verified by reintroducing the old line and confirming it fails. |
| 207 | P2 | `GET /api/listings` answers an empty list when there is no database, so a visitor is told there are no listings when the truth is that we cannot tell them | Fixed and awaiting deployment verification | Fixed in this package (PKG-DISCOVERY-1). `src/app/api/listings/route.ts` GET now returns 503 with `code: "storage_unavailable"`, the exact shape `POST` on the same route (line 126, pre-existing) and every other listings route in this file already use. The public `/listings` search page itself was already unaffected by this defect: it is a Server Component reading Supabase directly, not through this API route, and its own `dataOk` honest-state handling (finding 207's sibling work) was already correct; `GET /api/listings` had no confirmed runtime caller in this codebase (dashboard components call `POST`/`[id]` variants), so the route's own honesty for any external caller is what this closes. |
| 208 | P2 | Every route reserved 62px of bottom space for a mobile tab bar that only the marketing tier renders, and the floating Advisor button sat 82px above an empty edge on the same routes | Closed in PKG-E1-READINESS slice B | Named in the reconciliation gate as "the dead 62 px `main.has-tabbar` padding" and confirmed here rather than assumed. `has-tabbar` was written on `<main>` in `src/app/[locale]/layout.tsx`, which is a server component and cannot call `usePathname()`, so it could only set the class on every route. The bar itself travels inside the `footer` node that `ChromeGate` hands to the marketing tier alone, so every APP and PRODUCT route below 1024px ended its document with a reservation beneath which nothing was ever drawn: `/dashboard`, `/admin`, `/advisor`, `/messages`, `/docs`, `/deal`, `/notifications`, `/saved`, `/compare`, `/list`, `/invest`, `/find` and `/post-requirement`. `.advfab` carries the same arithmetic at `bottom:calc(82px + env(safe-area-inset-bottom))`, which is the bar's 62px plus a 20px gap, so on those routes the Advisor button hovered a bar's height above nothing. Both numbers were correct arithmetic about a bar that was not on the page. **Fixed** by moving `<main>` into `ChromeGate`, where the class and the footer slot are now decided by one call to `rendersFooterSlot(path)` four lines apart, and by adding `.advfab.no-tabbar` for the routes that get no bar, chosen by the same predicate. The width half stays in CSS, where it belongs: the bar is hidden by `@media(min-width:1024px)` and the reservation is released by the identical query, and `src/lib/chromeGate.test.ts` holds the two breakpoints equal, because a bar hidden at one width and a reservation released at another is either content under a bar or a gap under nothing. Measured by `scripts/shell-probe.mjs` at nine widths in both locales on both shells: 36 rows, `main` padding 0 everywhere, Advisor button 20px above the edge where there is no bar and 82px where there is. |
| 209 | P2 | The tab bar reservation was on `main`, which is not the last element in the document, so the footer's copyright strip painted underneath the fixed bar | Closed in PKG-E1-READINESS slice B | Found by measurement, not by reading, and it would not have been found by reading: the arithmetic was right and the element was wrong. `src/app/layout.tsx` is `<body>{children}</body>` with no wrapper, so `<main>` and `<footer class="foot">` are siblings, and `padding-bottom` on `main` protects the seam between the two rather than the end of the page. `scripts/shell-probe.mjs` scrolls each shell to the end of its document and measures the bottom of the last painted element against the top of the fixed bar. It found 10px of the footer's copyright strip beneath the bar at 320, 360, 390 and 430, and 24.5px at 768, in English and Arabic alike. **Fixed** by handing the reservation to the element the bar actually covers, through a `--tabbar-reserve` custom property: `globals.css` sets it on `main.has-tabbar ~ .foot` and owns the bar's 62px, `footer.css` adds it to its own two bottom paddings and keeps its own 36px and 26px, and neither file repeats the other's number. `main.has-tabbar` now declares no padding at all and is only the marker that says this document has a bar. Measured after the fix: the copyright strip clears the bar by 52px at phone widths and 37.5px at 768, in both locales. One visible consequence, recorded rather than left to be noticed: on marketing routes below 1024px the 62px that used to sit between the page content and the footer has moved below the copyright strip, so content-to-footer spacing on a phone tightens by that amount and the space now does the job it was declared for. The probe's own first run measured the top of every document instead of the end, because `globals.css:7` is `html{scroll-behavior:smooth}` and a synchronous read after `window.scrollTo` returns the pre-scroll position; the probe now sets `scroll-behavior:auto` before scrolling and asserts that it landed, because an instrument that quietly measures a state it never reached is worse than no instrument at all. Both guards were verified by reintroducing the old arrangement: the CSS defect returns FAIL 10 of 36, and removing the scroll override returns FAIL 36 of 36. |
| 210 | P1 | Every sign-in refusal on `/[locale]/login` rendered the authentication library's own reason, which names the account behind the address and turns the login form into a membership lookup | Closed in PKG-E1-READINESS slice D | Found by reading the three auth call sites in `src/app/[locale]/login/page.tsx` rather than by a report. All three did `setError(error.message)`, so whatever GoTrue returned was rendered. GoTrue does not answer a failed sign-in with one refusal: it answers `invalid_credentials` when the password is wrong, `email_not_confirmed` when the account exists and was never confirmed, `user_banned` when it exists and is suspended, and `user_not_found` when there is no account at all. Four different sentences for four different account states, obtainable by typing an address and reading the screen, with no password and no rate of attempts that would look unusual. On a platform whose members are named owners, brokers and licensed agents that is a disclosure about a person rather than about a request, and the person never agreed to it. Closed by `src/lib/authErrors.ts`, the third bilingual refusal table, whose resolver is an allowlist: `authErrorCode` names a refusal only when the refusal is true no matter whose address was typed, and everything else returns `null` so the call site says its own single generic sentence. The default direction is silence, so a library upgrade that adds a new account-state code cannot leak by omission. The seventeen codes that describe the account are additionally listed in `COLLAPSED_AUTH_CODES` and asserted one by one, in both languages, in `src/lib/authErrors.test.ts`. Evidence: the four enumeration outcomes now produce exactly one sentence, asserted as its own test. |
| 211 | P2 | The same three refusals rendered the library's English into the Arabic build | Closed in PKG-E1-READINESS slice D | The same defect as finding 22 and finding 203, in the one place that had not been swept, and it survived because the English sentence arrived from a dependency rather than from our own source. Closed by the same change: four generic sentences added to the `login` block of both dictionaries, `errSignIn`, `errLinkNotSent`, `errProvider` and `errLinkInvalid`, and passed as the required `fallback` argument at each call site. `authErrorMessage` requires that argument rather than defaulting it, so adding a sign-in surface is a decision about what to say when the platform will not explain. The generic sentence on the password form names the sign-in link as the next thing to try, which is what keeps recovery possible after the account-state reasons are collapsed: the link works for a confirmed account and an unconfirmed one alike, so the reader is never told which of the two they are and never needs to be. Asserted in both dictionaries by script, with the English side required to mention "sign-in link" and the Arabic side "رابط". |
| 212 | P2 | `/auth/callback` was the last monolingual English surface on the authenticated path, and returned every reader to the English login page | Closed in PKG-E1-READINESS slice D | An Arabic reader who asked for a sign-in link on the Arabic build opened it in their mail client and met four hardcoded English sentences and a button whose href was `/en/login` regardless of who was reading. Recovery on this platform is the magic link, so this is the page slice D's brief means when it asks for recovery to stay understandable in both languages. The route carries no locale segment because it is the single address registered with the authentication provider, so the locale is now read from `next`, which the login page already builds as `/{locale}/go`, through `localeOfNext` in the new `src/lib/authRedirect.ts`. No text renders until that read has happened, so an Arabic reader never sees an English sentence corrected a frame later and there is no hydration mismatch. `dir` is set on the section, the return button goes to the reader's own login page, and the four sentences come from the same dictionary block as the page next door. `not_configured` is worded identically to `login.errNotConfigured` on purpose, because it is the same condition and a reader who bounces between the two should not think two different things went wrong. |
| 213 | P1 | `/auth/callback` handed the `next` query parameter straight to `window.location.replace`, so a crafted sign-in link redirected off site wearing this domain in the address bar | Closed in PKG-E1-READINESS slice D | Found while making the callback bilingual, and worth more than the defect that led to it. The page is open to anyone, `next` is supplied by whoever built the link, and it was taken as given, so `/auth/callback?next=https://example.invalid` was an off-site redirect from a legitimate-looking address on this origin. The provider's own redirect allowlist does not close this: it checks the callback address, and the callback address is genuine. Closed by `safeNext` in `src/lib/authRedirect.ts`, which admits only a single-slash absolute path on this origin and refuses the two forms that look like paths and are not, the protocol-relative `//host` which is an absolute URL elsewhere, and any backslash, which enough browsers fold into a slash that `/\evil.example` is the same attack. Nine hostile values are asserted, including the empty string, `null` and `undefined`, and `localeOfNext(safeNext(...))` is asserted so that a refused value cannot pick the language either. `verifyOtp` was narrowed in the same pass from `type as any` to a six-entry allowlist, so an unrecognised `type` is treated as a link we did not send. The rule lives in a module rather than in the page because a route segment file may only export what the framework expects of it, and because a redirect rule worth writing down is worth testing directly. |
| 214 | P1 | The tree had never been linted, and the first ESLint run found 49 React Compiler errors that a framework migration must not absorb | Confirmed open, pinned by a ratchet, assigned to PKG-REACT-1 | Raised in PKG-NEXT16-SECURITY slice A. `package.json` carried `"lint": "next lint"` and no ESLint configuration existed anywhere in the repository, so the script had never run once: `next lint` on an unconfigured project stops and asks an interactive question, which no gate here can answer. Next.js 16 removes the command, so replacing it was in scope and the replacement is what exposed this. ESLint 9.39.5 with `eslint-config-next` 16.2.12 reported 82 problems on first run. Ten were fixed inside slice A because they were trivially safe and local. The remaining 49 errors are three React Compiler rules from the framework author's own core-web-vitals set: `react-hooks/static-components` 28, `react-hooks/set-state-in-effect` 17, `react-hooks/purity` 4. None of them are migration breakage; every one predates this package. They are not swept here because each is a real question about one component's behaviour that has to be answered by reading that component, and because burying several hundred lines of hook refactoring inside a framework semantics diff would make the thing that most needs reviewing unreviewable. They are not downgraded to warnings either: this project has already ruled on the shape of that mistake in the words "do not add decorative per-instance rate limiting and call it protection", and a lint gate that is green because the rules were softened is the same object. Held instead by `scripts/eslint-gate.mjs`, run as `npm run lint-gate` and now one of the six local gates, which pins each count and fails three ways: a count that rises, a count that falls without the pin being lowered in the same diff, and any rule not on the list appearing at all. So the debt cannot grow, cannot be paid off and quietly re-borrowed, and cannot be joined silently by a new kind of finding. Twenty-three warnings are inventoried and not enforced, `@next/next/no-img-element` 14 and `react-hooks/exhaustive-deps` 9, and the file says plainly that an inventory is not a gate. Closing this finding means driving the three pinned counts to zero in PKG-REACT-1, which is a named follow-up package and not a someday. Recorded in `docs/next16-migration.md` section 5.
| 215 | P2 | The desktop listings grid shifts layout intermittently, and five measurement campaigns disagree with each other about how badly | Confirmed open, pre-existing, assigned to the public-discovery and design package | Raised in the PKG-NEXT16-SECURITY release-correction batch, by the independent validation campaign that the batch introduced. `desktop:en:listings` cumulative layout shift reads 0.182, 0.147, 0.182, 0.288 and 0.288 across the Next.js 14 baseline, the Next.js 16 Turbopack comparison, and three Next.js 16 Webpack campaigns; `desktop:ar:listings` reads 0.056, 0.151, 0.183, 0.056 and 0.056 over the same five. The value is bistable rather than noisy: it lands on one of two branches depending on timing, and the mobile cells for the same route are stable at 0.019. It is not migration debt. The Next.js 14 record already carries 0.182, so the shift predates the framework upgrade and was simply never caught, because until this batch the budgets had only ever been written from one campaign and never validated against a second. 0.288 is in the poor band for this metric, and the worst branch is what a desktop reader on a slow connection is most likely to see. Not fixed here because this batch was bounded to release correction and the listings grid is the subject of the next package. The active budget carries a named exception at 0.31 for the two desktop listings cells with the reason written into the budget file itself, so the gate does not fail at random while this is open; that ceiling is not a standard and the exception is meant to be deleted, not renewed. |