| 168 | P1 | The Insights table has no caption and no scope on its header cells | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/listings/page.tsx at line 471, where `<table className="dt" style={{ minWidth: 520 }}>` opens; the `<thead>` on line 472 is a single row of five bare `<th>` elements with no `scope` attribute, and no `<caption>` element appears between the table tag and the head. The visible title sits outside the table as a `<div>` at line 465. SC 1.3.1 info and relationships is engaged: the row and column association is available visually but not programmatically, and the table has no accessible name of its own. Not fixed in this slice because the `.dt` table pattern is shared by the rent index, the dashboard listing tables and the admin account tables, and the header cells are generated from dictionary strings, so adding a caption means adding dictionary keys that this pass was instructed not to touch. **Closed in PKG-A11Y-1 slice O (RC11).** The recorded deferral said "adding a caption means adding dictionary keys that this pass was instructed not to touch". That turned out to be false, and finding out why is most of the fix: every one of the 21 tables already renders a visible title, and that title is already a dictionary value in scope at the table's call site. So the caption reuses the string the reader can already see rather than inventing a second name for the same thing, which is also better practice than a bespoke caption would have been. Not one dictionary key was added for a caption. The technique is `<caption className="sronly">` as the table's first child: `.sronly` is `position:absolute`, which is legal on a caption, removes it from the table box flow and leaves it in the accessibility tree, so the table gains an accessible name and loses nothing visually. It was chosen over a `table.dt>caption{...}` stylesheet rule so that there is one mechanism, greppable and testable at every call site. `scope="col"` is inserted into every `<th>` inside every `<thead>` (90+ header cells), and two tables gained row headers where a first column was acting as one: `/pricing`'s limit-comparison rows and `/saved`'s comparison `Row` helper, both rendered identically to before by pre-narrowing `.matrix th` to `.matrix thead th` and by cancelling the user-agent `th` defaults with `font-normal text-start`. Two guard tests hold the invariant: every `<table` in the tree has a `<caption` as its first child, and every `<th` inside a `<thead` carries `scope=`. The Insights table itself is at `src/app/[locale]/listings/page.tsx`, captioned `dl.indexCut`. Evidence classification: automated source scan plus the browser-emulated reflow measurement above, which is unchanged from baseline. Not tested with an actual screen reader and not independently audited. |
| 169 | P2 | Two links are styled as a tab strip but are not tabs and cannot become tabs | Open | Verified in src/app/[locale]/listings/[id]/page.tsx at lines 341 to 344. The container is `<div className="tabs">` and the two children carry the `t` and `t on` classes that src/styles/sat-platform.css lines 225 to 227 paint as a bottom-bordered tab strip. Confirming what they actually are, as instructed: the first is an in-page anchor to `href="#ov"` and the second is a Next `Link` to a different route, `L("/invest")`. So this is not an unlabelled tab widget; it is two navigation links wearing tab styling, and adding `role="tablist"`, `role="tab"` and `aria-selected` would be the wrong remedy, because there is no tab panel to select and activating the second one leaves the page. SC 1.3.1 info and relationships and SC 4.1.2 name role value are engaged by the mismatch between the visual affordance and the real semantics. Not fixed in this slice because the correct remedy is either to restyle the pair as links or to build real tab panels, and both are structural rewrites larger than a bug fix. |
| 170 | P1 | The listing video is served with no captions track | Open, partially mitigated in PKG-A11Y-1 | RC12. SC 1.2.2 is not satisfied and this row stays open, because captions are a content and ingest commitment rather than a markup change. Rendering an empty `<track>` would assert a captions track that does not exist, which is worse than the defect it pretends to fix. What shipped instead is the truth, stated where it is useful, plus the accessible name the player never had. The `<video>` in src/app/[locale]/listings/[id]/page.tsx now carries `title` and `aria-label` from `ld.videoTour`, the same string the YouTube and Vimeo iframe branch beside it has always carried, so a screen reader announces the player as the video tour rather than as "video". Below it a new bilingual line, `ld.videoNoCaptions` ("No captions are available for this video." / "لا تتوفر ترجمة نصية لهذا الفيديو."), tells a reader who depends on captions before they press play rather than after. The note is deliberately bare. It does not claim the written listing description carries the same information, because nobody has checked that a given lister's video says nothing the text does not, and owner ruling 3 forbids inferring a claim from a route type or a generic wording. Remaining work, named so it can be scheduled rather than rediscovered: a caption field on the listing media record, an upload path that accepts a WebVTT file per language, validation that the file is a real cue list rather than an empty one, and an authored track per video in English and Arabic. Until all four exist there is nothing for a `<track>` element to point at. Evidence: automated and source-read. |
| 171 | P1 | The listing description paragraph declares neither lang nor dir | Closed in PKG-A11Y-1 | Verified at `src/app/[locale]/listings/[id]/page.tsx` line 568 before the fix: `{(ar ? l.description_ar : l.description_en) && <p className="muted" style={{ ... }}>{ar ? l.description_ar : l.description_en}</p>}`. The paragraph carried no `lang` and no `dir`, so whichever of the two stored columns was rendered inherited the document direction set by `src/components/HtmlLangDir.tsx`. This is the one paragraph on the page that SAT did not compose. Everything else there comes from the dictionary or from a controlled vocabulary and is in the reader's language by construction; this is prose a stranger typed into a textarea, and the column it arrived in is that stranger's declaration rather than a measurement. It is frequently wrong in both directions: a lister who writes the whole listing in English pastes the same paragraph into both fields, and a lister writing in Arabic drops an English building name or a unit specification into the middle of one. English prose inheriting `lang="ar"` is not read aloud as accented English, it is read with Arabic phonetics, which is noise; and the bidi algorithm resolves the same run against the page direction and can reorder it on screen. SC 3.1.2 language of parts and SC 1.3.2 meaningful sequence are engaged; severity P1, journey 3, search, listing detail and Evidence Passport. The earlier note recorded this as blocked on a data-model question, and that was the right caution and the wrong conclusion. The passport's instrument is a stored flag, `reasonLang` read by `readCorrectionReason` in `src/lib/evidence.ts`, and it is correct there because a correction reason is filed through one field with one recorded language. It is unavailable here, and adding a language column would only move the problem, because the new flag would be the field name again. What is available is the text. Script is not inferred, it is read: the code points are either Arabic or they are not, which is evidence of the same kind as anything else this platform publishes and the only kind available for prose a stranger typed. Fixed by `src/lib/textScript.ts`, which counts letters, excludes Arabic-Indic and extended Arabic-Indic digits because SAT writes Western numerals in both languages, requires eight letters before a majority means anything and a seventy percent share before a run is called, and returns null for everything else including genuinely mixed text. A caller that gets null says so: `dir="auto"` states that the direction is to be resolved from the content, which is true, where `dir="rtl"` on a mixed paragraph is a claim about text nobody checked. `dir` is never omitted now. The one assumption in the module, that Latin script means English on a platform that publishes in exactly two languages, is stated in its doc comment rather than hidden, because the alternative of leaving the paragraph untagged makes the same claim silently through inheritance. Thirteen tests in `src/lib/textScript.test.ts` cover the floor, the majority, both digit ranges, the Arabic presentation-forms blocks, accented Latin, the mixed case and every refusal; a guard test in `src/lib/formGroups.test.ts` asserts the render site. Evidence classification: automated source scan and unit tests. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 172 | P2 | Several Evidence Passport label and value pairs are lists rather than description lists | Open | Verified in src/components/EvidencePassport.tsx. The main provenance rows are already a description list, `<dl className="evi-dl">` at line 219 with real `<dt>` and `<dd>` children at lines 222 and 223, so the finding as recorded overstates the scope. What remains outside that structure is the permissions block at lines 306 to 311, where each of the three permission pairs renders as `<li><span>{k}</span><span className="evi-val">{v}</span></li>`, and the verification block at lines 245 to 256, which uses the same two generic spans inside a list item. The section headings above them at lines 265 and 304 are `<div className="evi-h">` rather than headings. SC 1.3.1 info and relationships is engaged, because the pairing between a label and its value is carried by flexbox placement rather than by markup. Not fixed in this slice because converting three sibling blocks to consistent description lists and promoting the section titles to real headings is a structural rewrite of the panel larger than a bug fix, and the panel has a render test at src/components/EvidencePassport.render.test.tsx that would need to move with it. |
| 173 | P2 | The result card heart is a decorative glyph that imitates a save control | Fixed and awaiting deployment verification | Verified against current source (2026-08-09). PKG-CARD1 (PR #6, `bbb2c30`) replaced the search results grid's hand-rolled `Photo`+bare-`fav` card with the canonical `ListingCard` component; `src/app/[locale]/listings/page.tsx:668` now renders `<ListingCard ... />`, whose own comment at that call site names finding 173's class explicitly ("the decorative fav star... no handler") as what it replaced. `ListingCard`'s `SaveHeart` is real and functional, not the old inert `<span>`. `satkit.tsx`'s `Photo`/`fav` prop still exists for whatever else still calls it, but the search results grid, which is what this finding was written against, is closed. |
| 174 | P1 | The whole type scale is declared in px and does not answer a browser text-size preference | Closed in PKG-A11Y-1 | Verified in src/styles/globals.css at lines 414 to 416, where every step of the scale is a px literal: `--fs-cap:9px` through `--fs-4xl:44px`, with `--fs-input:16px` among them. The right to left overrides at line 122 restate the same scale in px. Beyond the tokens, the journey surfaces set sizes as bare React numbers, which React serialises as px: src/app/[locale]/listings/[id]/page.tsx line 253 sets `fontSize: 30` on the page heading and line 568 sets `fontSize: 14.5` on the description, and src/app/[locale]/listings/page.tsx line 353 sets `fontSize: 32`. A px font-size does not scale with a browser default text size setting the way rem does. SC 1.4.4 resize text is engaged. Not fixed in this slice because converting the scale to rem is a design token change that reaches every surface in the product, including many this pass did not read, and it belongs to the parked visual package; a partial conversion would leave two scales disagreeing. Closed in PKG-A11Y-1 RC4 by converting wholesale, which is the only safe way to do it. The scale tokens, the right to left block, 772 bare numeric React fontSize values, 176 Tailwind text-[Npx] arbitrary values and 128 CSS font-size declarations are now rem, 1073 sites in 89 files, and a scan of src reports zero px font sizes remaining. The conversion is exact at the 16px default root size, so a reader who has changed nothing sees an identical page: 0.8125rem is 13px to the pixel. The one deliberate exception is --fs-input, which keeps a 16px floor as max(16px, 1rem) because it exists to stop iOS zooming the viewport when a text field takes focus below a computed 16px; the max() keeps that floor while still answering a larger preference. Evidence class: automated scan for the completeness of the conversion, plus browser-emulated measurement, scripts/responsive-probe.mjs over 13 shipped fragments in EN and AR at 320, 360, 390, 430, 768 and 1280, 156 measurements, no row past its box and no item wider than its content box. Guarded by src/lib/typeScale.test.ts, which fails on any new px font size anywhere in src. Not verified on a physical device and not verified with an actual browser text-size preference raised, which is the remaining independent-verification item for this finding. |
| 175 | P2 | Floor plan tiles open a new tab with no warning in the link name | Open | Verified in src/app/[locale]/listings/[id]/page.tsx at line 503: the plan tile is `<a key={i} href={fp.url} target="_blank" rel="noopener noreferrer" className="card" ...>` and its accessible name comes from the image alt at line 507 and the caption div at line 509, both of which are just the plan label. Nothing in the name says a new tab or window will open, and there is no icon or visually hidden note. The same pattern repeats at line 552 for the external video link. SC 3.2.5 change on request is engaged, and SC 2.4.4 link purpose in context bears on it. Not fixed in this slice because the remedy needs a dictionary string for the warning, in both English and Arabic, and this pass was instructed not to edit src/i18n/dictionaries; writing the sentence inline here would put a fourteenth English literal into a bilingual surface. |
| 176 | P2 | The breadcrumb arrow and slashes are inside the link name and are read aloud | Open | Verified in src/app/[locale]/listings/[id]/page.tsx at line 230. The entire breadcrumb is one link whose children are `{ar ? "→" : "←"} {dict.ld.crumbListingsUpper} / {String(dn).toUpperCase()} / {type.toUpperCase()}`. The arrow is a literal U+2192 or U+2190 character in the text content, not a background image and not marked `aria-hidden`, so it is part of the computed accessible name and is announced. The two forward slashes are in the name for the same reason. SC 4.1.2 name role value and SC 2.4.4 link purpose in context are engaged, since the name of the control includes punctuation that is purely visual. Not fixed in this slice because the honest remedy is to make this a real breadcrumb, an ordered list of separate links with CSS-generated separators, rather than one link that concatenates three levels; that is a structural rewrite larger than a bug fix, and the JSON-LD BreadcrumbList at lines 335 to 340 already describes the four-level structure the markup does not have. |
| 177 | P2 | Token drift, a map wash written as a literal rgba where a token declares the same value | Fixed and awaiting deployment verification | Verified against current source (2026-08-09). `src/theme/palette.ts`'s `MAP` object now carries `hoverWash: "rgba(58,110,165,0.14)"` as a named role (PKG-DISCOVERY-1 slice C), and `src/components/ListingsMap.tsx:168` paints the hover-highlight layer with `MAP.hoverWash` rather than the literal. The surrounding `MAP.pin`/`MAP.pinStroke` usage this row noted as already-correct is unchanged. |
| 178 | P2 | Token drift, the sample tag on the requirements board is three raw hex literals | Open | Verified as a second real instance in a different file. src/app/[locale]/requirements/page.tsx line 71 renders the sample tag with `style={{ color: "#92400E", background: "#FFFBEB", borderColor: "#FCD34D", fontSize: 10.5 }}`. None of the three values appears anywhere in src/styles/sat-platform.css, and the declared attention role that this tag is plainly reaching for is `--status-attention:var(--amber)` with `--status-attention-wash:#FBF3E3` at line 38 of that file, which the requirement detail page uses correctly at src/app/[locale]/requirements/[id]/page.tsx lines 179 and 180. So the same semantic state is painted two different ways one click apart. Contrast is not asserted here, because #92400E on #FFFBEB is outside the declared token set and any figure would be arithmetic on values that no token owns. Not fixed in this slice because it is a design token change and the correct target role has to be chosen deliberately, attention or stale, and that choice reaches the tag component and every other surface that marks sample data. |
| 179 | P1 | Amber verdict text on its own wash computes to roughly 3.30 to 1 | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/requirements/[id]/page.tsx at lines 178 to 180, where a non-exact match takes `{ fg: "var(--status-attention)", bg: "var(--status-attention-wash)" }`, applied to the verdict tag at line 189 at `fontSize: 10.5`. The same pair is set on the lister side at src/app/[locale]/dashboard/requirements/page.tsx lines 221 to 223. The figure is arithmetic on the declared token values and is not a measurement: src/styles/sat-platform.css line 38 resolves `--status-attention` to `--amber`, which line 18 declares as #B7791F, and declares `--status-attention-wash:#FBF3E3` inline. WCAG relative luminance for #B7791F is about 0.2384 and for #FBF3E3 about 0.9016, giving (0.9016 + 0.05) / (0.2384 + 0.05), or about 3.30 to 1. SC 1.4.3 contrast is engaged, and at 10.5px this is not large text by any definition. Not fixed in this slice because the remedy is the same `--amber` token change recorded at 166, and it reaches surfaces this pass did not read; it belongs to the parked visual package. **Corrected in RC15.** Closed by `beef75a` through the same token split as 166; this row was never updated. The verdict tag now takes `{ fg: "var(--status-attention-text)", bg: "var(--status-attention-wash)" }` at `src/app/[locale]/requirements/[id]/page.tsx:243`, and `#8A5A12` on `#FBF3E3` computes to 5.36 to 1, up from 3.30. The stale counterpart `--status-stale-text:#A26100` is 4.95 to 1 on paper. The comment block at `src/styles/sat-platform.css:55` records the reasoning beside the tokens themselves. Evidence class: automated. The ratios are arithmetic on the declared token values in the source at `beef75a`, recomputed at `7aaab03`, not a measurement of rendered paint, and `src/lib/palette.test.ts` recomputes them on every run so a future edit that reintroduces the step fails the suite. Not tested on a physical device, not tested with an actual screen reader and not independently audited. |
| 180 | P1 | The listing attachment radio group cannot be cleared and has no accessible name | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/requirements/[id]/page.tsx at lines 215 to 222. Each match renders its own `<input type="radio" name="attach-listing" checked={on} onChange={() => setAttached(on ? null : m.listing_id)} />`. The clearing branch is written but is unreachable in practice, because a browser fires no change event when an already-checked radio is activated, so once a listing is attached the responder has no way back to attaching nothing. Separately, the inputs share a `name` but sit in per-match `<div>` containers with no `<fieldset>`, no `<legend>`, no `role="radiogroup"` and no `aria-labelledby`, so the group as a whole is unnamed; each individual radio does get a name from the wrapping label at line 215. SC 4.1.2 name role value and SC 3.3.2 labels or instructions are engaged. Not fixed in this slice because both halves want the same structural change, wrapping the whole match list in a named group and adding an explicit none option, and that is a rewrite of the panel rather than a bug fix. Closed in PKG-A11Y-1 (RC8 slice G). The match list at `src/app/[locale]/requirements/[id]/page.tsx:175` is now a `<fieldset>` whose `<legend>` at `:176` carries `{t.matchesH}`, the caption that was previously a bold `<div>`, so the group is named with no new dictionary key in either language. Each radio's own name now carries its listing title through `<span className="sronly">{mt}</span>`, so the options are told apart by what they are rather than by which card they are drawn inside. The way out of the group is an explicit attach-nothing option at `:249`, `checked={attached === null}` with `onChange={() => setAttached(null)}`, and it is the option that starts selected. Addition to the finding as filed: it records the clearing branch as written but unreachable, which is correct, and reads that as one defect with the missing group name. It is two. Attaching nothing is a choice a responder can legitimately want to make after making a different one, so a radio group with no such option is incomplete regardless of whether the toggle worked; `setAttached(on ? null : m.listing_id)` was not a broken implementation of a way out, it was a toggle written into a control type that has no toggle semantics, and the repair is a member of the group, not a fixed handler. The old expression is now forbidden by name in the guard. `minInlineSize: 0` is set inline on this fieldset in addition to the global rule because the group sits inside a narrow response panel. Guarded by `src/lib/formGroups.test.ts` with four assertions: the legend, the none option, the absence of the old toggle expression, and the per-radio title span. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 181 | P1 | The size inputs' visible label and their accessible names share no words | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/post-requirement/RequirementForm.tsx. Line 319 renders the visible label as `<label htmlFor="pr-size-min">{pr.sizeRange}</label>`, and the two inputs override it with `aria-label={pr.sizeMinLabel}` at line 324 and `aria-label={pr.sizeMaxLabel}` at line 330. Reading the values in src/i18n/dictionaries/en.json, line 878 gives `sizeRange` as "Size range (m²)" and lines 897 and 898 give "Smallest size (m²)" and "Largest size (m²)". The visible string "Size range" appears in neither accessible name, and because the `aria-label` wins over the associated `<label>`, the first input's programmatic name is "Smallest size (m²)" while the text a speech-input user reads beside it says "Size range". SC 2.5.3 label in name is engaged, and SC 4.1.2 name role value bears on it. Not fixed in this slice because the remedy needs new or reworded dictionary values that carry the visible words inside the accessible name, and this pass was instructed not to edit src/i18n/dictionaries. Closed in PKG-A11Y-1 (RC8 slice G). The size range at `src/app/[locale]/post-requirement/RequirementForm.tsx:337` is now a `<fieldset className="field">` whose `<legend>` at `:338` is `{pr.sizeRange}`, matching the three sibling groups this form already wrote that way at `:276`, `:283` and `:348`, and both accessible names now begin with the visible caption: `en.json` `postReq.sizeMinLabel` is "Size range (m²), smallest" and `sizeMaxLabel` is "Size range (m²), largest", with the Arabic pair using the Arabic comma. A speech user saying the words printed on the screen now addresses a control that answers to them, which is what SC 2.5.3 asks. The deferral reason, that this pass was instructed not to edit `src/i18n/dictionaries`, expired with that pass; the two reworded values and the strings for finding 180 are this slice's only dictionary changes and both files were rewritten textually and re-validated, never round-tripped through a parser. Scope correction: the finding does not say whether this is a pattern. An `src`-wide sweep for a visible `<label>` overridden by a differing `aria-label` on the control it names found this one site pair and no other, so it is a defect and not a class, and no sweep was performed. The second input, which previously had no visible label associated with it at all, is now inside the named group. `pr-size-min` remains the validation focus target at `:133` and is unchanged. Guarded by `src/lib/formGroups.test.ts`, which asserts the legend and, for both languages, that each name contains the caption verbatim. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 182 | P1 | Asset chips use aria-pressed for a single-valued choice | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/post-requirement/RequirementForm.tsx at line 272: `<button key={a} type="button" aria-pressed={asset === a} ... onClick={() => setAsset(a)}>`. The handler assigns rather than toggles, and the state it drives is the single string `asset` declared at line 71, so exactly one chip is ever pressed and pressing another silently unpresses the first. That is a radio group, and `aria-pressed` describes it as an array of independent toggles instead. The contrast is visible in the same file: the must-haves at line 345 use the identical chip pattern and there `toggle` really is multi-select, so `aria-pressed` is right there and wrong here. The enclosing `<fieldset>` and `<legend>` at lines 268 and 269 are correct and would carry over. SC 4.1.2 name role value is engaged. Not fixed in the slice that found it because converting to `role="radio"` inside a `role="radiogroup"` also requires roving tabindex and arrow-key handling to be written, which is a structural rewrite larger than a bug fix. **That deferral reason was wrong, and it is left standing above rather than quietly edited, because the mistake in it is the reason this defect class reached four sites.** The control this needed is not `role="radio"`. It is `<input type="radio">`, which is the platform's own implementation of the same role and which supplies roving tabindex, arrow keys, wrapping, direction-correct horizontal arrows, the impossibility of clearing a chosen option, and form participation, with no JavaScript written at all. The proof that this was available rather than merely arguable is that it was already in the same file three lines further down: the transaction-type control at `src/app/[locale]/post-requirement/RequirementForm.tsx:296` has been a native radio inside a `.seg` label with an `.sronly` input since the deal-type and timeline selectors were converted from `<span onClick>`, and the technique is documented in the comment on `.sronly` at `src/styles/globals.css:498`. The register looked at the ARIA pattern and priced the ARIA pattern's cost; it did not look one screen down. Fixed in slice I at `RequirementForm.tsx:286-293`: the eight `<button aria-pressed>` chips are now eight `<label class="chip">` elements each wrapping `<input type="radio" name="asset">`, the existing `<fieldset>` and `<legend>` carried over unchanged as predicted, and the inner `role="group"` was removed because the fieldset was already the group and the shared `name` is what binds the options. Nothing visual moved: the chips keep `.chip` and `.chip.on`, and the input is `.sronly`. One CSS rule was added with the conversion, `.chip:has(input:focus-visible)` at `src/styles/sat-platform.css:161`, modelled on `.seg label:has(input:focus-visible)` at `:223`, because an `.sronly` input is a one pixel clipped box and the browser would otherwise draw the focus ring where no one can see it; without that rule the conversion would have traded SC 4.1.2 for SC 2.4.7. The same defect class was then found at three further sites and is recorded as findings 197 and 198. Guarded by six tests in `src/lib/formGroups.test.ts`, which scan every shipping file under `src` rather than the four critical journeys alone, since the spread is the point: a rebuilt one-of-many chooser anywhere in the repository now fails the suite unless it is named in `HAND_BUILT_RADIO_EXEMPT` with a reason. Evidence classification: automated source scan, plus browser-emulated measurement in Chromium through `scripts/radio-probe.mjs`, which renders the shipped markup with the shipped stylesheet under a coarse pointer at 390 wide in English and Arabic and presses the keys: one tab stop per group for 24 options, ArrowDown walks and wraps, ArrowUp steps back, ArrowRight advances in `ltr` and retreats in `rtl`, two presses of Space on the chosen option leave it chosen, the label carries a 2px `rgb(58, 110, 165)` focus ring, every target clears 44 by 44, and the group name comes from the legend once. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 183 | P2 | The shared arrow icon is not mirrored under a right to left direction | Open | Verified in src/components/satkit.tsx at line 68, where `Icon.arrow` is a fixed path, `<path d="M5 12h14M13 6l6 6-6 6" />`, pointing rightward with no transform. It is used on the requirement journey at src/app/[locale]/post-requirement/RequirementForm.tsx line 428 on the submit button and line 216 on the view requirement link. Searching both stylesheets for a mirroring rule finds only two, `[dir="rtl"] .asset-arrow svg{ transform:scaleX(-1); }` at src/styles/globals.css line 118 and `[dir="rtl"] .dside .me .chev{transform:scaleX(-1);}` at src/styles/sat-platform.css line 337; neither selector reaches this icon. So an Arabic reader sees a forward-progress arrow pointing against the reading direction. SC 1.3.2 meaningful sequence and SC 1.3.3 sensory characteristics are engaged. Not fixed in this slice because the right remedy is a single rule that mirrors every directional glyph in the shared icon set, which reaches surfaces this pass did not read and belongs to the parked visual package. |
| 184 | P1 | A 320px minimum grid track overflows a 400 percent zoom viewport | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/requirements/page.tsx at line 59: `gridTemplateColumns: "repeat(auto-fill,minmax(320px,1fr))"`. The `auto-fill` collapses to one column but the `minmax` floor does not shrink below 320px. The container at line 37 is `padding: "36px 24px 48px"`, so at the 1280 by 1024 reference resized to 400 percent, which is a 320px CSS viewport, the available width is 272px and the track still demands 320px, producing horizontal scrolling for the card content. The requirement detail page uses the safer `minmax(120px,1fr)` at line 125 of src/app/[locale]/requirements/[id]/page.tsx, so the pattern is inconsistent within the journey. SC 1.4.10 reflow is engaged. Not fixed in this slice because lowering the floor changes the card layout at every breakpoint, and picking the replacement value is a layout decision belonging to the parked visual package rather than a bug fix. Closed in PKG-A11Y-1 (RC7 slice F). `requirements/page.tsx:59` now reads `repeat(auto-fill,minmax(min(100%, 320px), 1fr))`, and the same substitution was applied to the other 30 fixed `minmax(<N>px, 1fr)` floors across 22 files, because the defect was a pattern and not one line. `minmax(min(100%, Npx), 1fr)` resolves identically to `minmax(Npx, 1fr)` at every width where the container is at least N wide and collapses only in the case that previously failed, so no working layout moves. Two families were deliberately left alone with the reason verified in source: `bilingual/page.tsx:101`'s `minmax(40px,auto)` label gutter, which is below every content box measured, and `.lst-split`'s 300px map track at `sat-platform.css:682`, which is removed entirely at max-width:1080px by :687 and :688 and therefore exists only at widths where 300px fits. Correction to this finding as filed: it states the defect produces horizontal scrolling. It does not, and that is worse. `html,body{max-width:100%;overflow-x:clip}` at `sat-platform.css:617` and `globals.css:220` means clipped overflow is not scrollable overflow: measured, the defect track put a 320px card inside a 272px box at the 400 percent reference, an overhang of 48px, and 8px of overhang at 360, while `document.documentElement.scrollWidth - clientWidth` reported zero in both cases. The card's right edge was cut off and unreachable rather than scrollable, which is why no automated check on this site had seen it and why `scripts/reflow-probe.mjs` measures per-item overhang instead. After the fix the card is 272px at 320 and 312 at 360 with zero overhang, and is unchanged from 390 upward (342, 382, two of 352 at 768, three of 349.3 at 1280) in both languages. Guarded by `src/lib/reflow.test.ts`, registered in `npm test`, which forbids an unescaped fixed floor anywhere under `src` and requires any survivor to be named with its reason. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 185 | P2 | Form inputs are set below 16px, which triggers focus zoom on iOS Safari | Open | Verified in three places. src/app/[locale]/post-requirement/RequirementForm.tsx line 433 declares the shared input style with `fontSize: 14`; src/app/[locale]/requirements/[id]/page.tsx line 313 declares its own with `fontSize: 13.5`; and the shared class they both sit on, `.input` at src/styles/sat-platform.css line 300, is `font-size:13.5px`. Because the two inline objects are spread onto the elements, they beat the stylesheet regardless. The codebase already knows the rule: src/styles/globals.css line 412 carries the comment that `--fs-input` stays 16px on purpose to prevent iOS focus zoom, and line 415 declares `--fs-input:16px`, but no rule on either requirement surface consumes it; the only consumers found in the tree are src/components/FilterBar.tsx and src/components/MarketingHome.tsx. The one mobile override that exists, src/styles/globals.css line 371, is scoped to `.advpanel input`. This is not a WCAG failure on its own, though SC 1.4.4 resize text and SC 1.4.10 reflow are engaged once the browser zooms the viewport without the user asking. Not fixed in this slice because the remedy is to route both inline styles through the declared token, and the two inline objects are shared by every control on their pages, so the change reaches layouts this pass did not measure. |
| 186 | P2 | Two h1 elements render together on the lister requirement surface | Open | Verified across a layout and its page. src/app/[locale]/dashboard/layout.tsx line 110 renders `<h1>{acctName}</h1>` inside the persistent top bar, and src/app/[locale]/dashboard/requirements/page.tsx line 176 renders `<h1 style={{ fontSize: 22, ... }}>{t.title}</h1>` as the page heading, so both are present in the same document whenever a lister opens the requirements queue that the Pitch links come from. Neither is inside a `<section>` with its own accessible name, so this is not the sectioning-element case. The three public requirement surfaces each carry exactly one: src/app/[locale]/requirements/page.tsx line 42, src/app/[locale]/requirements/[id]/page.tsx line 122, and src/app/[locale]/post-requirement/RequirementForm.tsx lines 198 and 238, which are mutually exclusive because line 198 sits inside the early return at line 190. SC 1.3.1 info and relationships and SC 2.4.6 headings and labels are engaged. Not fixed in this slice because demoting the top bar heading changes the heading outline of every page under the dashboard layout, which is a structural change across surfaces this pass did not read. |
| 187 | P1 | Client-fetched requirement board and detail content arrives outside any live region | Closed in PKG-A11Y-1 | Verified, and the note in the description is correct that the matches panel was fixed: src/app/[locale]/requirements/[id]/page.tsx line 165 now wraps the matches in `<div aria-live="polite" aria-busy={matchesLoading}>`. That was not the only instance. Two remain. src/app/[locale]/requirements/page.tsx fetches the whole board client-side at line 33 and swaps a loading line at lines 47 and 48 for a grid of cards at line 59, with no live region and no focus move, so a reader is told nothing when the board arrives. src/app/[locale]/requirements/[id]/page.tsx line 110 does the same for the entire requirement, returning a bare loading div and then replacing the whole document body at line 113. SC 4.1.3 status messages is engaged. Not fixed in this slice because announcing a whole-page swap through a live region is the wrong instrument; the honest remedy is a route-level loading boundary and a focus move to the new heading, which is a structural change to how these two routes are rendered. Closed in slice L, and half of that deferral was wrong. The route-level loading boundary does nothing here: the route itself resolves immediately and it is the browser's own fetch, started on mount, that arrives late, so there is no server wait for a boundary to cover. The focus move is worse than useless and was deliberately not implemented, which is the second time in this package the register's own proposed remedy has been corrected rather than applied. A screen reader user who lands on either URL does not wait in silence for the request: they begin reading the heading and the subtitle with the virtual cursor, which walks the document without moving DOM focus. `document.activeElement` is `document.body` for that reader and for a reader who has touched nothing, so no guard written around focus can distinguish them, and calling `focus()` when the data settles would drag an attentive reader back to the top of a page they were already part way through. Focus is moved when the user asked for the change. Nobody asked for this one; the page finished loading. The instrument that fits is a status message, which is exactly what SC 4.1.3 is for, and it moves nobody. Both routes now carry a `.sronly` `role="status" aria-live="polite"` region and set `aria-busy` on the container while the request is in flight. The structural care is in where the region sits rather than in its attributes: a live region announces changes inside an element the browser was already watching, so a region created in the same render as its text says nothing. `requirements/page.tsx` holds the region outside the loading branch entirely. `requirements/[id]/page.tsx` had three early returns with three unrelated root elements, which is the shape that breaks this, and was restructured so every branch opens with the same root and the same region as its first child, which is what makes React preserve it across the swap. The board's messages are the count that arrived, the empty-market sentence, or the failure sentence; the detail's are that the requirement loaded, that it does not exist, or that a response was registered. Two further repairs came with it. The board's `.catch` set `loading` to false and nothing else, so a request that failed fell into the empty branch and told the visitor that no occupier in the country is looking for space, which is the ELITE-4 J4-5 defect in a second place; it now states the failure, distinguishes it from an empty market in the sentence itself, and offers a retry. And a `!r.ok` response was being parsed as JSON and treated as success, so a 500 produced an empty board rather than a failure. Five guard tests in `src/lib/formGroups.test.ts` cover both regions, the region count on the detail page, the catch and the retry; one of them asserts the absence of `.focus()` in the board file, because a deliberate absence with no test against it reads as an oversight to the next reader and gets helpfully corrected. Evidence classification: automated source scan and guard tests, plus the browser-emulated reflow probe confirming both surfaces are unchanged at every width. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
