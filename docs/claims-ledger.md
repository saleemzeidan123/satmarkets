# Claim and evidence ledger (WS02)

Every material public promise, with owner, evidence and state. No new material
claim ships without an entry here. States: Supported | Unsupported, replacement
pending approval | Removed | Preview-qualified.

| # | Surface | Current claim (EN / AR) | State | Evidence and action |
| --- | --- | --- | --- | --- |
| C1 | Home | "No one else in the Kingdom brings all four together" | Removed | Comparative claim with no market survey. Carried as pending replacement since WS02. Closed by C35: the line is gone from `MarketingHome.tsx` in both locales and the strip now describes what the page holds rather than where it ranks. |
| C2 | Home | "Sourced, never estimated" | Removed | An absolute that conflicts with representative figures elsewhere on the platform. Carried as pending replacement since WS02. Closed by C35: every band string on the home page now names its period and its source instead of asserting a universal. |
| C3 | Home | "Live market board" | Unsupported, replacement pending approval | Inventory is seed data. Replacement: "Market preview with sample data" once Saleem approves preview-state wording (WS05). |
| C4 | Home/Listing | "Owner-verified" KPI and badge | Corrected | Three queries counted a checked owner, a broker's authorisation to market and the row being our own stock, and called all three owner-verified. `src/lib/gate.ts` says only the first carries the claim. The home KPI, the `/listings` verified filter and its facet counts now all read `ownership_verified` alone, which is what the badge on those same rows already read, so the filter can no longer return a row the badge contradicts. Fixed in `726b72b`; a `src/`-wide scan in `src/lib/claims.test.ts` stops it spreading back by copy and paste. |
| C5 | Pricing | "Browse & enquire, free forever" | Unsupported, replacement pending approval | Perpetual commercial promise. Replacement: preview-period wording; whole page pending pricing-visibility decision. |
| C6 | Home map | "Buildings across the Kingdom are clickable" | Removed | Coverage is 75 sample buildings in Riyadh. Carried as pending replacement since WS02. Closed by C39: the string lived in `home.mapBody`, which nothing rendered, and the whole unreferenced group was deleted rather than given the weaker wording, because a rewritten string with no call site is a claim waiting for one. |
| C7 | Deal | "Full deal" / SAT "track(s) the deal" | Unsupported, replacement pending approval | Replacement: deal workspace language (WS28). |
| C8 | About | "We check the person, the property, and the licence" | Unsupported as universal | Replacement: badge-specific scope wording. |
| C9 | Rent Index | Attribution to REGA Rental Index (Ejar), averages of registered contracts, Q2 2026 | Supported | rent_index_published (period 2026-Q2, sufficient=true) + source_registry.rega_ejar (averages, attribution licence). PKG-0A aligned both locales. |
| C10 | Footer | FAL 1200025510, operated by SAT Real Estate | Supported | Licence number per Law 1. |
| C11 | Footer | SATMARKETS.SA | Removed | Domain not acquired. Removed in PKG-0A; law test guards regression. |
| C12 | Global | Preview notice: sample data for testing, not live inventory | Supported | dict chrome.preview, both locales. Keep until real inventory. |
| C13 | Invest | "underwritten on verified comparable transactions" / "مُحلَّل على صفقات مقارنة موثّقة", and "Comps drawn only from verified SAT-advised transactions, no asking prices, no scraped figures." | Removed | No SAT-advised transaction record exists behind any row. Replaced with the wording /hbu already carried: "Simulated demonstration. Every comparable, price and cap rate in this table is illustrative and does not describe a real transaction." Owner ruling 3. Guarded by `src/lib/claims.test.ts`. |
| C14 | Invest | Potential NOI of SAR 4,583,333 and a going-in cap rate of 6.8%, the latter described in source as "the verified comp cap rate" | Removed | Both were compiled constants presented as findings, and the 6.8% also drove the "at/above district" KPI note against a district benchmark no query produced. Both are now user inputs with starting values, in the same class as acquisition price and leverage, so the model does arithmetic on the reader's assumptions and asserts nothing of its own. Owner ruling 3. |
| C15 | Invest, HBU | Named comparables: Olaya Tower, Al Akaria Plaza, Tahlia Gate, Granada Oasis, and their Arabic counterparts | Removed | Real Riyadh buildings carrying a price and a cap rate with no documented public source and no permission for this use. Anonymised to Comparable A to D with grade and configuration only, identically in both surfaces and both locales. Owner ruling 4. |
| C16 | Invest | Verified tick rendered in the Source column of every comparable row | Removed | Verified green is reserved for evidence-backed verification. The column now reads "Simulated" / "محاكاة", and the `Verified` component is no longer imported by either modelling page. Owner ruling 3. |
| C17 | Invest, HBU | "Last 6 months" filter chip above the comparables table | Removed | Described a recency query over transaction records. No such query and no such records. Key deleted from both dictionaries rather than reworded, so a future page cannot revive it. Owner ruling 3. |
| C18 | Invest | CSV export filename `sat-underwriting-olaya-tower.csv` | Removed | The export leaves the platform and is read without the page around it, so it carried a real building name into a spreadsheet with a full underwriting on it. Now `sat-underwriting-sample.csv`, and the export includes the reader's own NOI and pricing cap as declared inputs. Owner rulings 3 and 4. |
| C19 | HBU | Page headline "Grade A Office Floor, Olaya Tower", its subtitle and the "Q2 2026" eyebrow | Removed | Found by live verification, not by test: `41f4f8f` guarded `dict[section].comps` only, so the page's own headline escaped it. Now "Grade A office floor · sample asset" with a simulated-model eyebrow. The lesson is recorded in the guard: a test scoped to the field where a defect was first seen will not find the same defect one field over, so the scan now covers the whole dictionary and all page source. Owner ruling 4. |
| C20 | Messages, notifications, deal room, term sheet, docs, dashboard, list | Named counterparties (Olaya Towers, Tahlia Holdings, KAFD Devco and their Arabic forms) carrying fabricated quotes, viewing appointments, calendar entries and a signature line | Removed | Eighteen dictionary strings plus one name hardcoded in `deal/termsheet/page.tsx`. The term sheet mattered most because it is printed and carried out of the product, so it named a real Riyadh landlord as a verified party to a lease that does not exist. All replaced with "Sample Owner Co." style placeholders in both locales. Districts and developments were deliberately left alone: naming Al Olaya or KAFD is a fact about geography, not a claim about a counterparty. Owner ruling 4. |
| C21 | Notifications | "The Q2 2026 band for Olaya Grade A offices is published" | Removed | An AI-surfaced market statistic naming a real building, which Law 3 forbids independently of ruling 4. Rewritten as a published-band statement with no named subject rather than deleted, because the notification is rendered under a Rent Index title that the page still needs. |
| C22 | About | "The verified data authority for Saudi commercial real estate." / "المرجع الموثوق لبيانات العقارات التجارية في السعودية." | Removed | A self-declared category authority with no survey, no citation base and no third party asserting it. Replaced with a statement about how the platform is built rather than what rank it holds: "built so every figure can name its source". Owner ruling 3. |
| C23 | About | "no listing enters the market without an advertising permit on file" / "ولا يدخل السوق عرض بلا تصريح إعلان مسجّل" | Removed | The single worst string on the platform against the record. 88 published listings, 0 of them holding an `ad_permit_number`. The claim is not weakened, it is rescoped: the permit rule is stated as the publishing standard that applies at launch, followed by the fact that preview inventory holds no permit. Owner ruling 3. |
| C24 | About | "Only after these checks does a listing publish, with a Verified badge any visitor can trust." / "مع شارة موثّقة يمكن لأي زائر الاطمئنان لها." | Preview-qualified | `verification_events` holds 3 rows, all `is_demo`, and each one's own `basis` text states that no Wathq lookup and no REGA lookup was performed. `listing_verification_events` holds 94 rows on gate `rega_permit`, all `is_demo`. `account_verifications` is empty. Nothing has been checked against a government register. Now scoped to launch, and the badge is described as naming the gate it rests on rather than standing in for all three. |
| C25 | About | "How we verify every listing" and "we check the person, the property, and the licence" (supersedes C8) | Preview-qualified | C8 recorded this as "Unsupported as universal" and left the replacement open. Closed here: the heading becomes "Our verification standard", and the subtitle states the three gates, that the automated lookups behind them arrive before launch, and that nothing in this preview has been checked against Nafath, REGA or Ejar. The three gate steps themselves already read "arrives before launch" and are untouched. |
| C26 | About | `intro`, `metaDesc` and `foot` asserting "verified listings" and "verified commercial real estate intelligence" of the current corpus | Preview-qualified | Same evidence as C24. "Verified" is dropped where it described the corpus and kept where it describes a source, and the intro now says the platform is in preview on sample data. The rent index reference in the intro and in the data-moat card gained the REGA Rental Index (Ejar) attribution that owner ruling 2 requires and that both strings were missing. |
| C27 | Global dictionary | Seven unreferenced sections: `filters`, `areaIntel`, `why`, `statBand`, `rentTeaser`, `featured`, `hero` | Removed | Confirmed dead by reference count across all of `src/` outside the dictionaries. Between them they carried "Every figure is computed from verified data. No model-generated numbers", "Decision-grade rent bands. Verified, never modelled", "The moat that makes this an authority, not a board", "Owner-direct listings, verified by SAT before they publish", "Every lister is verified, our own included" and "The Kingdom's home for verified commercial space". Deleted rather than reworded: the cheapest route for a false claim onto a page is to be sitting in the dictionary already, correctly translated, one autocomplete away. |
| C28 | Advisor, advisor widget, search | "the verified index", "verified platform indicators", "verified stock", and source labels naming the index without attribution | Removed | The advisor is the surface a reader is most likely to believe, because it answers in sentences. `rent_index_published` holds 7 rows, `data_class = 'synthetic'`, `is_demo = true`, `source = 'REGA Rental Index (Ejar)'`. There is no verified index to be grounded in. Every grounding string now names what it actually reads (the Rent Index derived from the REGA Rental Index (Ejar), and listing records) and states that the assistant explains the data rather than inventing it, which is the Law 3 promise stated positively. Owner rulings 2 and 3. |
| C29 | Listing, building, map, home, compare, market, findPage, ui | "verified rent band" / "نطاق الإيجار الموثق" across every surface that renders a band | Removed | Two faults in one phrase. The band is REGA's and we republish it, so attaching our own verification vocabulary to someone else's data is a false claim about who checked it; and the rows behind it are `data_class = 'synthetic'`. Now "published rent band" / "نطاق الإيجار المنشور" everywhere, which owner ruling 2 blesses, and every string that names the index as a source carries the REGA Rental Index (Ejar) attribution the same ruling requires. A per-locale test pins both halves. Owner rulings 2 and 3. |
| C30 | Requirements | "Verified occupiers looking for space" / "مستأجرون موثّقون يبحثون عن مساحات", and the matching meta description | Removed | The strongest finding of the sweep. `requirements_public` has no verification column at all, not an empty one, none, so the claim had no field to rest on even in principle and could not have become true without a schema change. Now "Occupiers looking for space". Guarded by a test that asserts the word cannot return to `req.h1` or `req.metaDesc` in either locale. Owner ruling 3. |
| C31 | Listing detail, list your space | "every listing is verified by SAT before it publishes", "owners are verified before listing", an average verification turnaround, and "the faster verification" | Preview-qualified | Same treatment as C23: the standard is stated as what applies at launch, followed by what the preview actually holds. `listing.trustBody` and `list.intro` now scope to launch and say preview inventory has not been through the checks; `list.avgTimeValue` reads "Checked at launch" and its note says we do not quote a turnaround we have not measured. `list.detailsSub` was found only on the second pass, sitting directly above that note and promising faster verification, so the page contradicted itself inside one screen. Owner ruling 3. |
| C32 | Taglines, meta descriptions, footer, app metadata, signup, pricing, brokers | "verified commercial real estate intelligence", "verified owners and licensed brokers", "the verified platform" in strings that leave the site | Removed | Meta descriptions, the footer legal line and `common.poweredBy` are read in search results, link previews and printed output, where the global preview notice is not on the page to qualify them. Each now describes how the platform is built or what the record holds: spaces listed by owners and licensed brokers, a source-attributed rent index, and AI that never invents a figure. Owner ruling 3. |
| C33 | Deal room, enquiry, listing detail | "we verify the parties", "the verified lister", "SAT tracks every milestone", "we show verified facts" | Removed | `account_verifications` is empty and all 10 accounts are `is_demo`, so no party to any deal has been checked. The disclaimer now states the true division of labour: the Ejar contract, deposit and registration happen between the parties, SAT neither signs nor holds funds, and our role is the workspace. `ld.locNote` drops "verified facts" for "the facts on the record", which is what a listing page renders. Owner ruling 3. |
| C34 | Global dictionary, locations | `home.chips` deleted; `locations.verifiedSpace` / `verifiedSpaces` renamed to `listedSpace` / `listedSpaces`; six recipient-restriction strings deliberately kept | Removed / Corrected | `home.chips` was unreferenced across all of `src/` outside the dictionaries and its first element read "Verified, owner-direct", the same latent-claim defect as C27 one level in. The locations keys were renamed as well as reworded, because a key called `verifiedSpace` holding the word "listed" is a trap for the next reader; the single call site in `locations/page.tsx` moved with it. Four `postReq.*` and two `reqDetail.*` strings keep "verified owners and licensed brokers" and are listed by path in the test: they restrict who receives a requirement, one of them is the consent label the person actually agrees to, and deleting the word there would widen a data-sharing promise rather than correct a claim. Keeping them is a decision on the record, not a hole in the pattern. Owner ruling 3. |

| C35 | Home | Hero, exchange strip, feature cards, band panel and CTA: "verified commercial space", "Verified listings", "Verified spaces, priced in context", "decision-grade data", and the dead `bandStat` captions | Removed / Corrected | Closes C1 and C2, both carried as "Unsupported, replacement pending approval" since WS02. C1's comparative claim ("No one else in the Kingdom brings all four together") and C2's absolute ("Sourced, never estimated") are gone from `MarketingHome.tsx` in both locales, replaced by what a reader can check on the page: discovery, requirements, published pricing and an advisor that names its source. The corpus wording follows the record, not the route: 88 published listings, so the KPI reads "Published listings"; each row carries its own `ownership_verified` flag, so the cards say each listing shows its verification state; the band is REGA's, so the Rent Index card carries the REGA Rental Index (Ejar) attribution owner ruling 2 requires. `bandStat` ("Riyadh Grade A, YoY (published)") was deleted rather than reworded: grep proved nothing renders it, the year-on-year block it belonged to was removed when it turned out a trend needs two periods and `rent_index_published` holds one, and a dead string that reads as a published trend is the C27 latent-claim defect. `feats[2]` now states plainly that mobility and visitation data are not enabled, which is the ADV-5 gate stated on the page. Owner rulings 2 and 3. |
| C36 | Global footer, social card, structured data | Footer CTA and paragraph ("on verified ground", "Verified listings, decision-grade data"), the Open Graph cards, the card alt text, and the JSON-LD organisation description | Removed / Corrected | These four are the claims that travel. The footer renders on every page in both languages; the card is seen by people who never load the page; the alt text is read aloud by a screen reader and scraped into a link preview; the JSON-LD description is read by a crawler with no page around it. None of the four is covered by the global preview notice, because none of them is on the page. All now describe how the platform is built and what the record holds. Both PNGs were regenerated in-container (`node scripts/og-cards.mjs --fonts /tmp/ogbuild/node_modules/@fontsource --chromium /opt/pw-browsers/chromium`) and read back as images to confirm Arabic shaping, RTL and Western numerals. Owner ruling 3. |
| C37 | Home featured card | "Verified owner" badge computed as `ownership_verified \|\| authorization_verified \|\| is_sat_listed` | Corrected | The second home of the C4 widening. `726b72b` fixed three Supabase `.or()` queries and added a `src/`-wide scan for them; this one was plain JavaScript on the home page, invisible to a scan that looks for `.or()`. Now `ownership_verified` alone, which is what `src/lib/gate.ts` says carries the claim and what the badge on the same row already read. Pinned by three direct assertions on the flag's shape. Owner ruling 3. |
| C38 | Filter bar, signup, dashboard requirements, listings loading, post-requirement metadata, advisor API, shortlist API | "Verified areas", "verified spaces", "Verified occupiers", "Loading verified spaces", "Verified owners and licensed brokers", and the advisor's own system prompt and live context calling the corpus verified | Removed | Found by widening the guard rather than the sweep. The dictionary sweep is only as wide as the dictionaries and `prose-scan.mjs` defers its BASE tier to the page-redesign packages, but that deferral is an i18n decision and a claim does not care which tier its string lives in, so `claims.test.ts` now runs a claims-only strict scan over all component, app and script source. It found nine files on its first run. The two that mattered most are in `api/advisor/route.ts`: the system prompt told the model itself that SAT Markets holds "verified listings", and the live context string interpolated `ls.count` as "verified listings" when that query is `.eq("status","published")` and never counted a verified row. One offender survived the second pass because `Found ${n} verified ${n === 1 ? "space" : "spaces"}` splits the phrase across an interpolation where a source regex cannot see it; the Arabic mirror on the same line caught it. `post-requirement` keeps the recipient restriction in `postReq.intro` and the consent label, per C34, and drops it only from the meta description, which is read in a search result away from that promise. Owner ruling 3. |
| C39 | Home dictionary | Nineteen unreferenced `home.*` keys: `lens`, `mapTitle`, `mapBody`, `mapCta`, `forTitle`, `forSub`, `occT/occB/occC`, `ownT/ownB/ownC`, `invT/invB/invC`, `cityBars`, `rentEyebrow`, `teaserPlaces`, `occupancyLabel` | Removed | Closes C6. The Kingdom-wide map claim lived in `home.mapBody`: "Explore the Kingdom's commercial buildings as clickable objects ... Live now in Riyadh, expanding nationwide", against 75 sample buildings in one city. `home.occupancyLabel` held "Riyadh Grade A occupancy", a market statistic no query produces and Law 3 forbids. `home.cityBars` named Jeddah and Dammam. None of the nineteen is referenced anywhere in `src/` outside the dictionaries. `home.ownB` is the proof that this matters: it was corrected for REGA attribution in an earlier commit while it was unreferenced, so a correction was spent on a string no reader could ever see, and its row is now removed from the attribution table. Deleted rather than reworded, for the C27 reason. Owner ruling 3. |

Review cadence: every release touching a listed surface. Last review: 2026-07-28 (owner ruling 3 and 4 corrections; `/invest`, `/hbu`, the verification predicate, the named-entity sweep, `/about`, the unreferenced sections, the discovery, listing, lister, requirement, research and advisory surfaces, and finally the home page, the site-wide footer, the social card and structured data, the advisor API, and the component and script tier).

Entries C13 to C18 are marked Removed rather than replaced because each was a claim about
evidence that does not exist. A claim of that kind has no honest weaker wording: the
correction is to stop making it, state what the surface actually is, and let the claim
return through the evidence path when a record supports it.
