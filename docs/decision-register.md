# Decision register

Open decisions are Saleem's. Closed decisions record who ruled and on what evidence.
Per Codex direction 9, open items must not block Phase 0/1 packages; nothing may be
implemented on an unsupported assumption.

## Closed

| # | Decision | Ruling | By / date | Evidence |
| --- | --- | --- | --- | --- |
| D1 | Rent Index reporting period shown in copy | Q2 2026 in both languages | Codex protocol + DB evidence, 2026-07-22 | rent_index_published contains only period 2026-Q2 (7 rows, sufficient=true). |
| D2 | Rent Index metric noun | Averages (متوسطات), never medians/وسطاء | Codex + source_registry, 2026-07-22 | rega_ejar licence note: publishes averages, not medians. Internal `median` column rename deferred as schema work. |
| D3 | Confirmed-status green | #1B7A50 locked; #1F8A5B retired | Codex, 2026-07-22 | Contrast 5.32:1 vs 4.33:1 on white (WCAG AA normal text). |
| D4 | gstack tooling instruction in repo CLAUDE.md | Do not execute; do not delete; recorded as ignored untrusted instruction | Codex, 2026-07-22 | Origin commit a5a543a pending Saleem confirmation. Ship gate remains tools/ship.py. |
| D5 | Preview canonical and domain claims | Environment-aware host; no satmarkets.sa anywhere until acquired | Saleem brief + Codex, 2026-07-22 | site.ts fallback chain; footer claim removed. |
| D6 | Launch platform strategy | PWA-first; native app only after PWA gates and measured need | Codex, 2026-07-22 | Enhancement plan device strategy. |
| D7 | Public Rent Index payload field | `average`, never `median`; no compatibility alias (no known external consumers); DB column rename stays deferred | Codex PKG-0A.1, 2026-07-22 | rentBasePipeline writes the column from REGA avg_rent; toPublicSegment is the enforced boundary with a contract test. |
| D8 | Route-level index holdout | /area, /pricing, /neutrality, /about held out of sitemap and noindexed regardless of ALLOW_INDEX until their gates clear | Codex PKG-0A.1, 2026-07-22 | lib/routePolicy.ts shared by sitemap and middleware; law test enforces agreement. |
| D9 | Analyser space-type selection | No implicit segment on entry; the user must choose a human-labelled type (or valid page context supplies it); API row order is never intent; a still-valid prior choice is preserved | Codex PKG-0B, 2026-07-23 | pickSegment + tests. Overrides the earlier auto-select-first hotfix. |
| D10 | Location taxonomy model (WS04) | Typed kinds district/development/area; development never labelled a district; unknown kinds are NOT coerced to area (area is a real assertion) and render the Location umbrella; WS04 references canonical types.ts/gate.ts rather than duplicating them | Law 7 + Codex PKG-1A, 2026-07-23 | docs/taxonomy.md, src/lib/locationKind.ts (normalizeKind), tests. Route/param change deferred to WS19. |
| D11 | Release-state vocabulary and tones (WS05) | Six approved bilingual states; separated tones: only the evidence-backed `verified` state uses confirmed green, `available` is informational (Harbor), a generic Verified may not render without its specific dimension true | Codex PKG-1A, 2026-07-23 | src/lib/releaseState.ts + tests; per-surface wiring is WS13. |
| D12 | Analyser unit basis | The rent unit is never inferred; an unrecognised unit blocks analysis with an explicit unsupported state | Codex PKG-1A, 2026-07-23 | unitKind/isKnownUnit + tests; validBand also requires strictly positive low/average/high. |
| D13 | Arabic font parity | Mono never renders Arabic script; Arabic-bearing mono label classes flip to IBM Plex Sans Arabic under RTL, only true figure classes (.fig/.tnum) stay mono | Codex PKG-1B, 2026-07-23 | globals.css RTL flip; e2e/arabic-font.spec.ts computed-style gate; live sweep 0 offenders on 8 public routes. |
| D14 | Semantic colour separation | Generic map-pin is Harbor; a separate map-pin-verified carries green; dv-quote below=Harbor-ramp, within=Harbor, above=attention; verification/comparative/outcome never share a colour; green stays exclusive to evidence-backed verification | Codex PKG-1B, 2026-07-23 | sat-platform.css tokens + advisor/map inline colours updated. |
| D15 | Breakpoint architecture | One authoritative source in src/theme/breakpoints.ts generates Tailwind screens (values preserve defaults + xs); CSS --bp-* are documented mirrors, not media-query drivers | Codex PKG-1B, 2026-07-23 | breakpoints.ts + tailwind.config.ts. |
| D16 | RTL serif fallback | No Georgia in the Arabic serif stack; IBM Plex Sans Arabic then Arabic-capable system fonts | Codex PKG-1B, 2026-07-23 | globals.css --serif under RTL. |
| D17 | Shared data-state pattern (WS13), INITIAL foundation | One accessible, RTL-safe, tone-driven DataState component (loading/empty/error/stale/sample/planned/permission); role=status/alert, aria-busy, tone drives icon AND colour never colour alone; server-compatible. Scope note: this is an INITIAL shared-state foundation with a single adoption (listings empty state), NOT a complete shared component/shell system. The broader responsive shell and expanded DataState adoption are deferred to the later page-redesign packages, against real screens. | Codex PKG-1B area 1 / PKG-1B.1, 2026-07-23 | src/components/DataState.tsx; adopted in listings empty state; tsc/test green. |
| D18 | Inline-hex to token migration by role | Classify every hex by role, replace only with the equal-valued token; map paint, SVG presentation attributes, brand logos, category/legend hues and non-verification greens stay literal; green stays exclusive to verification | Codex PKG-1B area 2, 2026-07-23 | 142 migrated (400 to 258), 0 value mismatches (paired check), guard: no var() in SVG/lib strings; docs/token-migration-{spec,results}.md. |
| D19 | Mobile search control overflow | The listings search input is a flex child with min-width:0 so it shrinks instead of forcing horizontal overflow past the viewport edge | Codex PKG-1B area 3, 2026-07-23 | sat-platform.css .search input; measured 77px overflow at 320px eliminated to 0. |
| D20 | Numeric ranges under RTL | A low-high figure range is one LTR-isolated numeric atom (bdi dir=ltr), reading low-to-high left-to-right in both locales; the advisor bar and its scale are forced LTR and the average moved to its own line to end the EN mid-label collision | Codex PKG-1B area 4, 2026-07-23 | rent-index, advisor, listings insights; live-measured RTL flip (high-on-left) before fix. |
| D21 | Neutral PWA entry and private cache | Single manifest, start_url "/" (middleware redirects to locale), theme_color Harbor #3A6EA5 (retires gold-family #1C1A15) aligned across manifest and meta; no service worker exists so nothing is precached; PRIVATE_PREFIXES routes carry Cache-Control no-store as defence in depth | Codex PKG-1B area 5, 2026-07-23 | public/manifest.webmanifest, layout.tsx, middleware.ts; docs/pwa-and-private-cache.md. |
| D22 | Accessibility FOUNDATIONS (not complete a11y) | Implemented foundations only: visible keyboard focus (:focus-visible), reduced-motion handling, and >=44px touch targets on coarse pointers. Scope note: this is NOT a claim of complete accessibility. Runtime keyboard-navigation, screen-reader and full WCAG evidence remain a SEPARATE verification package. | Codex PKG-1B area 6 / PKG-1B.1, 2026-07-23 | sat-platform.css focus + @media(pointer:coarse) + prefers-reduced-motion. |
| D23 | Deep token migration | Added --status-stale; unified all vs-index comparative colours to --dv-quote-* (fixing the non-verification confirmed-green on market/compare/saved, a D14 violation); centralized MapLibre paint into src/theme/palette.ts (BRAND/ASSET_COLORS/HEAT_RAMP/MAP) so map colours are one named token-mirrored source. Scattered inline hex 400 to 189. | Codex PKG-1B area 2 (deep), 2026-07-23 | palette.ts; MapExplorer/ListingsMap 0 inline hex; live: market "below" now Harbor-ramp, map legend swatches correct, no MapLibre errors. |
| D24 | Green reservation (settled) | Confirmed green is reserved for evidence-backed verification. Semantic map: verified=green; new/informational=Harbor; generic completion/success=Harbor Deep; below-band/price-reduction=--dv-quote-below; warning/aging/stale=amber; destructive/failed=red; WhatsApp=brand green (isolated third-party exception). Availability freshness keeps green only when backed by availability_confirmed_at and shown with a non-colour label/date. best-value is a comparative outcome (--dv-quote-below), not verification. New badges, report-received checks, generic success, viewing-completion and ordinary action success moved off green. AMENDED 2026-07-27: the reservation is BIDIRECTIONAL. It is equally a defect for an evidence-backed verification surface to render WITHOUT confirmed green, which is what the first pass missed on ListingCard (the passesGate() tick was an off-palette teal #0E9488). The teal is retired as a status, verification, party or role colour; it survives only as the retail entry in ASSET_COLORS, a centralized categorical hue permitted by D18/D23. | Codex PKG-1B.1, 2026-07-23; inverse defect corrected 2026-07-27 | Reclassified 20+ occurrences; src/lib/greenReservation.test.ts enforces green only in enumerated verification/availability/brand files, and a second test bans #0E9488 everywhere outside src/theme/palette.ts. |
| D25 | Advisor value answer: structured evidence boundary | The "value" (price-vs-band) answer builds ONE structured evidence result (evidence id, source, period, localized location, asset, supported vs requested segment, unit, low/avg/high, user figure, support status, limitation) and renders BOTH locales deterministically from it. A general-office band can never be relabelled a Grade A band; EN and AR provably share scope, numbers and source; Arabic location is localized; Western numerals kept. Replaces the previous two-separate-LLM-calls design that let AR and EN diverge. | Codex PKG-1B.1 Advisor P0, 2026-07-23 | src/lib/market/valueEvidence.ts + 11 deterministic parity tests; api/advisor value mode rewired. |

| D27 | Competitive Advantage Strategy adoption, reconciled not blanket | The strategy is adopted through `docs/strategy-reconciliation.md`, which classifies every recommendation as shipped, partial, missing, gated or already answered more strictly in this repository, and only the surviving items enter the roadmap as ADV-0 to ADV-6. Three reconciliation rulings bind: (a) the maps and POI question is already closed harder here, no isochrone cache may be reintroduced and no Navigation-scoped token is held; (b) D24 green is bidirectional, so an evidence-backed verification surface rendering without confirmed green is a defect too; (c) PD5 procurement recognition becomes owner action only under ruling 7 and produces no engineering task. PD1 to PD4 fold into ADV-0, ADV-1 and ADV-4 rather than running as a parallel plan. | Saleem directive + Claude reconciliation at HEAD 058a568, 2026-07-28 | docs/strategy-reconciliation.md; source_registry rows foursquare_mapbox (id_only/none), fsq_os_places (Apache 2.0), broker_overlay (internal), gastat_sama (public, use policy 1.2.2), rega_ejar (averages not medians); D24 amendment 2026-07-27; owner rulings 1, 3, 4 and 7. |

| O3 | Verification label policy (was open, resolved by implementation under owner ruling 3) | Verification is never one badge. Five factual dimensions are named and typed (ownership, authorization, identity, advertising permit, listing operator), each resolving independently to verified, unverified or not applicable, and each carrying the reason it is not verified. A dimension reaches `verified` only when four conditions hold together: the record is not a fixture, the method names a check against something outside this database (`seed` is the fixture loader and is excluded), there is a date of check, and an actor countersigned it. No surface may render a badge that does not name its dimension, and no badge may be derived from `passesGate()`, whose ownership and authorisation legs answer PASS on an unset column. `gate.ts` stays the truth source and `ownerVerified` its narrowest claim; the resolver is narrower still and never broader, and that direction is asserted over the real column combinations. Confirmed green follows the resolved dimension in both directions per D24. | Claude implementation under Saleem owner ruling 3, 2026-07-28 | src/lib/evidence.ts (dimension and state vocabulary, Evidence Passport), src/lib/listingVerification.ts (resolver, four fail-closed conditions), src/lib/listingVerification.test.ts, src/lib/claims.test.ts value-level dictionary guards; commits 1047ee6, bbed926, 0625309, 0c4e615; live EN and AR on 0c4e615 show 0 retired owner claims on all twelve saved public pages against 4 and 4 at 0625309. Register ranks 3 and 24 closed with live evidence. |

| D26 | Public data strategy (post-Paseetah) | SAT Markets sources its market data from the same public Saudi government products Paseetah uses, through official channels only, and competes on the two things they lack: published methodology and deed-level verification. Named workstreams PD1 to PD5 in docs/roadmap.md. Scraping srem.moj.gov.sa or the Najiz UIs is forbidden. No consumer price war. | Saleem, 2026-07-28 | docs/competitive-paseetah.md: their own attribution names MOJ, the Real Estate Registry and Ejar; no FAL licence, MOU or partnership found; Ejar publishes commercial price per sqm for six cities back to 2019 at sakani.sa/reports-and-data; Etimad 240141005052 shows their one real asset is procurement recognition, not data. |

| D28 | Workflow honesty rules for supply and demand (ADV-2) | Four rules bind every workflow surface and are enforced by test rather than by review. (a) ABSENCE IS UNKNOWN. A fact the record does not state is `unknown` in every model that reads it, never zero, never a default and never a pass; `listingQuality`, `matching` and `decisionPack` each type it and each test it. (b) NO VERDICT WITHOUT ITS REASONS. A match, a completeness band and a decision-pack readiness are all derived from named dimensions with a state each, and no surface may render the outcome without the dimensions it came from, because an outcome with no reasons is a recommendation and a recommendation is a claim SAT cannot evidence. (c) TOLERANCE IS PUBLISHED. The only two places a near miss is permitted are size and budget, both at 10 percent (`SIZE_TOLERANCE_PCT`, `BUDGET_TOLERANCE_PCT`), and a near miss returns `possible` with the miss stated rather than being rounded into an exact match. (d) THE PLATFORM NEVER SCORES WHAT IT CANNOT READ. The media standard is a brief plus a count of the three facts the record holds; it does not assert what any file depicts, because scoring image content would assert a fact nobody established, which is the same defect as inventing a rent. Corollary under D24: completeness, readiness and media status never render in confirmed green, because none of them is verification. | Claude implementation under the Saleem owner directive, 2026-07-28 | src/lib/listingQuality.ts, src/lib/factScope.ts, src/lib/listingStudio.ts, src/lib/listingEdit.ts, src/lib/matching.ts, src/lib/decisionPack.ts, src/lib/mediaStandard.ts with 155 tests across their seven suites; src/components/{ListingStudio,DecisionPackPanel,MediaBrief}.tsx; commits ca8e13e through dc780e3; docs/adv-2-closure.md. |

| D29 | Mobility and visitation publication rule (ADV-5B) | A movement, visitation, dwell, catchment or spend figure may be published only after four independent gates pass in a fixed order, and the order is part of the rule. (a) RIGHTS: the source has a `source_registry` row permitting display to this audience; an unknown source has no rights and an unread register denies exactly as an empty one does. (b) SUFFICIENCY: a recorded agreement answers every Part E clause, including a written prohibition on user-level output. (c) DATA: an aggregate exists for this area and came from the source that was permitted, because a permission is not transferable between sources. (d) COVERAGE: the aggregate meets the published thresholds for geography, aggregation count, period age, observed share, sample basis and method note. Three consequences bind. First, the smallest publishable geography is the district: `MobilityAvailable` narrows its geography to city or district, so a building-level or parcel-level movement figure cannot be constructed rather than merely being refused. Second, an available figure carries its aggregation count, period, coverage share, method and the vendor's verbatim attribution, all required rather than optional, so a figure that cannot state its own basis does not typecheck. Third, no travel-time area is stored and no record of an individual visitor is held, per D27(a). Rendering surfaces reach a figure only through `districtMobilityPanel`, which returns a translation key and never the verdict's licence reasoning, register code or clause list. | Claude implementation under the Saleem owner directive and owner ruling 7, 2026-07-30 | src/lib/location/{mobility,coverage,sufficiency,panel}.ts with 46 tests across mobility, coverage and panel; src/lib/location/claims.test.ts as the structural gate over every surface in src/app and src/components; findings 70 to 73; docs/mobility-privacy-methodology.md. |
| D30 | Strategy reconciliation, second pass, and the reachability standard | `docs/strategy-reconciliation.md` is reconciled a second time at HEAD `a2d2817` and keeps both passes on the record, the first at `058a568` beside the current one, because a classification whose earlier readings are deleted cannot be audited. Three things bind from it. (a) A sixth verdict, BUILT BUT UNREACHED, is added and is distinct from Partial: the module exists, is typed and is tested, and nothing on a rendering surface or request path constructs or reads it, so the remaining work is a producer and a rendering rather than more module. (b) REACHABILITY IS PART OF DELIVERY. A package does not close on a green unit suite alone; the closing evidence must show that a rendering surface or a request path reaches what was built, or must state plainly that nothing does and why. This is ADV-5B's lesson, that the gate is worth more than the module it guards, made general. (c) UNREACHED BY GATE IS NOT UNREACHED BY OMISSION, and the two are never reported as one defect. Code that is dormant because a documented boundary refuses to activate it, as the AI agent layer is while `AI_AGREEMENT_IN_FORCE` is false and no provider agreement is recorded, is a refusal working as designed under owner ruling 7. Code that is dormant because nobody wired it, as the Evidence Passport is, is an omission. Findings 76 and 77 record the omissions and ADV-1C is the package that closes them. | Claude implementation under the Saleem owner directive, 2026-07-30 | docs/strategy-reconciliation.md sections 0 and 5; findings 76 and 77; roadmap ADV-1C |
| D31 | Whether the published inventory count is mock, seed or genuine, and how it may be shown | The 88 published listings, of which the 56 on `/listings?city=riyadh` are the Riyadh subset, are PREVIEW SAMPLE INVENTORY and are not genuine publishable inventory. Determined from record-level evidence, not from route type or wording, per owner ruling 3. Three findings decide it. (a) Not one of the 88 has passed a verification check: every row carries `ownership_verified = true`, none carries a `verified_by` actor, and none carries a `verification_method` naming a real check, which is why the four-part verified count on the home page reads zero. (b) CORRECTED 2026-07-31, finding 79. This clause originally read that zero rows carry the `SIMW1-` reference prefix written by `scripts/seed-world.mjs` and that zero carry `is_demo = true`, and concluded that the set is neither seeder's output but hand-built preview inventory. The first half stands and the second half was false. The record-level audit already on file in `docs/ruling-3-4-closure.md` and in finding 3 says that `listings` holds 93 rows, EVERY ONE of them `is_demo = true`, and that all 88 published rows are among them. The corrected reading strengthens this decision rather than weakening it: the set is not merely unverified sample inventory, it is flagged as simulated at record level on every row. What the false clause caused is recorded as finding 79, because a fix was built on it that suppressed simulated rows unconditionally and so emptied the exchange. (c) The live response carries `x-robots-tag: noindex, nofollow`, so under the current release policy the number cannot become an indexed marketplace-scale claim. THE DISCLOSURE CODEX ASKS FOR IS ALREADY SHIPPED: `src/app/[locale]/layout.tsx` renders on every route, in both languages, that listings and figures shown are sample data for testing and not live market inventory. The count is therefore neither suppressed nor relabelled, because it is already labelled and a second label on the same page would say nothing the first does not. WHAT IS CORRECTED IS THAT THE LABEL HOLDS BY ACCIDENT OF THE DATA RATHER THAN BY CONSTRUCTION, which is finding 78. The number must never be published outside a surface carrying the preview banner, and must never be published at all once the release policy drops noindex, until the inventory behind it is real. | Claude implementation under the Saleem owner directive, answering Codex boundary 2, 2026-07-31 | Live `/en/listings` and `/en/listings?city=riyadh` on production, 88 and 56 unique listing ids; `src/app/[locale]/page.tsx` verified-count chain; `scripts/seed-world.mjs` and `scripts/seed-demo.mjs`; `x-robots-tag` on the live response; findings 78 and 79; `docs/ruling-3-4-closure.md` line 37 for the corrected clause (b) |
| D32 | Whether a correction reason filed in one language is translated for a reader of the other | Never translated. The filed words are shown as filed, and the language they were filed in is recorded and marked so the reader's software handles them correctly. Where a correction is genuinely filed in both languages, both are stored and each reader gets their own. | Claude under ADV-1C boundary 10, 2026-07-31 | Boundary 10 forbids filling missing evidence with generated wording. The stated reason a figure changed is evidence, so a translation of it is a sentence nobody filed sitting in the record as though somebody had. `FiledReason` and `readCorrectionReason` in `src/lib/evidence.ts`; a filing language that was never recorded is left unmarked rather than guessed, because a wrong `lang` attribute makes a screen reader read the sentence as noise. Finding 83. |
| D33 | What the Evidence Passport's compact indicator says before it is opened | The tier and, when a state other than `held` applies, that state. No date. | Claude under ADV-1C boundary 5, 2026-07-31 | Boundary 5 asks for a compact indicator and warns against overloading the main interface. Two measurements decided it rather than taste: the panel mounts in a listing tile that narrows to 103px once the page's auto-fit grid splits, and the dated chip is 180px and cannot wrap, so it overflowed its card at four of the six probed widths in both languages. Nothing is lost, because the panel below carries the freshness date on its own row and a checked date per verification dimension, which is a finer record than the single conflated date the chip showed. Finding 85. |

## Pre-launch, deferred by owner (2026-07-23): prototype stage, do not action now

The product is a private, unshared prototype on mock data; the whole preview stays
noindex. These launch-level items are parked until closer to launch and must not
consume prototype-stage effort:

- Acquire and configure the production domain; remove domain references from draft
  legal documents; final legal wording and contact emails (registers ranks 9, 34 stay open).
- Production SEO and canonical-domain perfection (rank 7 env-awareness is enough for now).
- A full backward-compatible migration framework (prototype state is disposable).
- Final Rent Index statistical methodology, including how the low/high range is derived.
  The current low/high are development test ranges; UI now labels them "sample
  indicative range" and attributes only the REGA average + period as sourced.
- Physical database-column rename of `median` (rank 31 stays deferred; the public
  API boundary already exposes `average`).

## Open (owner: Saleem)

| # | Decision | Blocking | Context |
| --- | --- | --- | --- |
| O1 | Pricing visibility: labelled concept vs hidden until real | WS29, claim C5 | Codex requires every CTA truthful or labelled. |
| O2 | SAT Markets / SAT Real Estate relationship statement | WS30, /neutrality | Coordinate with counsel. |
| O4 | Production default locale (en, ar, or selector) | WS31 | x-default target follows this. |
| O5 | Legal wording and counsel engagement | WS30 | Terms, Privacy, Contact placeholders. |
| O6 | Licensed market datasets for public display and AI retrieval | WS20/WS31 | REGA/Ejar is cleanly licensed; broker overlays are internal-only. |
| O7 | Requirements indexability and requester-data exposure | WS31 | Consent and redaction rules. |
| O8 | Write canonical-Laws amendments back into KB sat-markets/CLAUDE.md | docs/LAWS.md amendments section | Green token, Source Serif 4, environment truth. |
| O9 | Origin of repo CLAUDE.md gstack instruction (keep or remove) | D4 follow-up | Entered in a5a543a. |
| O10 | External written confirmation of REGA Rental Index (Ejar) permitted use, across the ten clauses stated in `docs/regulatory-register.md` and held as data in `src/lib/sources/o10.ts`: source access; public display; attribution wording; SAT transformations and derived figures; aggregation and minimum samples; export; API and machine-readable output; AI retrieval and response use; retention and correction; Arabic and English publication | PD1, PD2, PD3, ADV-1E, finding 90 | ADV-1E narrowed this row to the one source it blocks. It previously read as "licence terms for each public dataset", which is how a question gets answered for open.data.gov.sa and believed answered for Ejar. The other datasets (MOJ open data, GASTAT REPI, REGA indicators, open.data.gov.sa) each carry their own `source_registry` row and their own rights status, and none of them is O10. Resolution is unanimous across the ten clauses: nine answered is unresolved. Until then `decidePublicQuote` withholds the figure on every surface, including API, metadata, structured data and the assistant. |
| O11 | Whether the public CRE bulletin (PD3) is the surface that lifts the site-wide noindex, and on what gate | PD3 / ADV-4 indexing half, routePolicy | Today ALLOW_INDEX is off and everything is noindex. A public bulletin has no value un-indexed, so this needs an explicit ruling rather than a quiet env flip. Owner ruling 1 parks the indexing half, so this blocks finishing ADV-4, not starting it. |
| O12 | Notification consent basis for opportunity routing: affirmative opt-in or opt-out, per channel | ADV-2 external channels | The strategy records an owner preference for opt-out and flags that Saudi law and the electronic-communications rules may require affirmative opt-in before any email, push, SMS or WhatsApp message. Counsel question. Until it is ruled, ADV-2 ships in-product notification only, external channels disabled in code, consent receipts recorded from the start. **Owner ruling, PKG-A11Y-1 commission: outbound notifications remain held. Do not activate automatic email, SMS, WhatsApp, push or other external match notifications. The authenticated in-product opportunity or matches inbox may remain available if it respects organization permissions and does not expose confidential information. The notification preference and consent model may be prepared. External delivery requires all ten of: explicit per-channel opt-in where applicable; clear purpose; brand and asset preferences; frequency controls; quiet periods; immediate unsubscribe or suppression; organization-role authority; auditability; Saudi privacy and communications review; zero cold outreach generated from inferred interest. Acceptance of general platform terms is not consent to automated opportunity marketing.** |
| O13 | Whether SAT Markets holds or seeks the separate REGA real-estate analytics and consultation licence, distinct from FAL 1200025510 | ADV-0, ADV-4, HBU, investment scenarios | REGA issues a specific licence for real-estate analysis and consultation services, and requires an accredited valuer licence where valuation is involved. Law 1 fixes our licence number, not its activity scope. Determines whether the bulletin, HBU, investment scenarios and public market commentary are permitted at all. Needs a Saudi counsel memorandum mapping 1200025510 surface by surface. |
| O14 | Organization and role authority: who inside a landlord or tenant organization may release contact details, accept a viewing or bind the organization | ADV-2 mutual-interest contact release | The role lists exist in the strategy; the authority rules do not. Needed before progressive disclosure and contact release can be specified without exposing a party who never agreed. |
| O15 | Attribution scope for the Rent Index: which classes of string are citations and must carry "REGA Rental Index (Ejar)" in full, and which are navigation to a page that carries it | Register rank 45, ADV-4 | Owner ruling 2 requires the attribution on every Rent Index reference. ADV-1 corrected the places where the index is cited as a source beneath a figure, and the one place it was claimed as ours. 36 remaining English values and 59 Arabic values name it with no attribution, and most cannot hold one: `nav.rentIndex` is a tab label, `rentIndex.eyebrow` is an eyebrow, and nine `metaDesc` values would spend 44 Arabic characters on the attribution. Proposed rule for approval: source labels, disclaimers, figure captions and structured data carry the full attribution; navigation labels, page titles and eyebrows that link to a page which itself carries it do not. |
| O16 | Whether availability freshness keeps the reserved green now that verification uses it on the same card, and what the availability label must state | Register rank 46, D24 follow-up | On one browse card `#1B7A50` currently means two unrelated things: an evidence-backed verification dimension on the tick, and a confirmed availability date on the dot. `availabilityShortLabel` returns "Available" / "متاح" for both the fresh and the aging state and returns no date, so colour alone separates a listing confirmed this week from one last confirmed months ago. D24's own clause requires availability green be shown with a non-colour label or date. Options: move availability off green entirely, or keep it and print the confirmation date or age in days. |
| O17 | The lawful basis, the retention position and the user disclosure for first-party behavioural measurement: whether contract performance covers product measurement of an account the person asked for, what the raw retention window may be before aggregation, and what the person must be told and where | Every one of the 46 events in `src/lib/analytics/events.ts` and the twelve measures in `src/lib/analytics/scorecard.ts` | **Owner ruling, PKG-A11Y-1 commission: collection remains disabled. Keep `COLLECTION_AUTHORISED = false`. The owner authorizes the event catalogue, the scorecard design and preparation of a first-party collection architecture. He does not authorize production behavioural collection.** Before O17 can be enabled, a data-collection readiness record must cover fourteen items: exact events to be enabled; identifiers used; storage location; access roles; encryption; raw-event retention; aggregation retention; deletion and correction; user opt-out where applicable; privacy-notice changes; lawful-basis or consent dependency; research-session treatment; employee and administrator access; and confirmation that prohibited properties cannot enter through generic metadata. No analytics vendor may be installed and no data may be sent to one. ELITE-1 sessions use consented research notes and manual observation; product telemetry stays off unless a separate, explicitly bounded research authorization is approved. `COLLECTION_AUTHORISED` is a module constant, so enabling it is a commit rather than a dashboard setting. Answering O17 would open 39 of the 46 events and would still leave the notification family shut, because O12 holds that separately. |
| O18 | Which identity mechanism authorises an anonymous requirement poster to withdraw the consent that shares their contact details (finding 193) | Finding 193, and through it the withdrawal right on every requirement already posted. O5 and O12 gate three of the four options | **The identity problem.** A requirement is posted with no account and no session, so at the moment of withdrawal there is nothing to authenticate against and a secret has to be introduced from somewhere. Neither secret that already exists will serve. `ref_code` is `'R-' || nextval('public.requirement_ref_seq')`, so it is strictly sequential and therefore guessable, and it is published on the public board inside the `/api/requirements` payload, so it carries no secrecy at all. The recorded contact email is not published, but it is low-entropy: for a known company it is guessable in a few attempts, and a route that accepts a code-and-email pair therefore confirms who posted which requirement to anyone who asks, which is a disclosure in its own right and not merely a weak login. **The textbook mechanism is blocked twice.** Emailing a one-time withdrawal link is the standard answer and cannot be built here: `package.json` carries no email dependency of any kind, and O12 independently holds all outbound notification. SMS is held by the same row. **Four mechanisms, for the owner to choose between.** (1) MANUAL, through a published privacy address: no new write path, no new secret, and available today except that O5 leaves `/contact` carrying COUNSEL placeholders, so the request has nowhere to land. (2) WITHDRAWAL TOKEN issued when the requirement is created, shown once on the confirmation screen and stored only as a hash: needs no message to be sent, and it is the strongest self-service option open to a product with no mail capability. (3) REFERENCE CODE PAIRED WITH THE RECORDED EMAIL: cheapest to build, and it carries the enumeration surface described above; it is additionally unprotected today because `allow()` in `src/lib/ratelimit.ts` is per-instance and in-memory, describes itself as close to decorative on serverless, and `allowShared()` needs an Upstash or Vercel KV endpoint that is not configured. (4) MOVE POSTING BEHIND AN ACCOUNT: strongest identity, but it is a product change rather than a privacy fix, an anonymous board stops being anonymous, and it does nothing at all for anyone who has already posted. **A recommendation, which is not a decision.** Take 1 as the floor the moment O5 clears, because it discharges the obligation with no new attack surface and it is the only option that needs no ruling. Rule on 2 as the self-service mechanism. Do not build 3 unless a shared rate limiter and an indistinguishable failure response are both in place first. **Risks that bind whichever is chosen.** The token or the pair is entered into a form and never carried in a URL, because a query string reaches browser history, a referrer header and every log on the path. The response is identical whether or not anything matched, or the route becomes a lookup oracle for which email posted which requirement. The write clears personal data rather than reading it, so a failure refuses without saying which half of the pair was wrong. **Recovery path.** If the token is lost there is no self-service recovery, because every recovery channel is a message and this product cannot send one. The manual route is the recovery, which is a second reason it has to exist first rather than instead. **Simplest acceptable implementation, once ruled.** One security definer function that matches, clears `contact_name`, `contact_email` and `contact_phone`, closes the brief and returns nothing distinguishable on failure, applied by the owner because every Supabase write tool in this environment is permission denied; one public route; one small page in both languages; and the consent label reverting to a statement of the control once the control exists. Nothing is implemented until this row is ruled. |
