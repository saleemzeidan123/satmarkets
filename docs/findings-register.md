# Codex audit findings register, status tracking

Source: Codex audit 2026-07-22, snapshot `6c6797e`. Statuses per Codex direction:
Confirmed open | Partially addressed | Blocked by evidence or decision |
Fixed and awaiting deployment verification | Closed with live evidence |
Not independently reverified. "Partially addressed" never counts as fixed.
Original rank and severity are preserved. Update this file in the same commit as
any fix; move to "Closed with live evidence" only after live EN and AR checks.

| Rank | Sev | Finding (short) | Status after PKG-0A | Evidence / note |
| --- | --- | --- | --- | --- |
| 1 | P0 | Arabic body/UI inherits Hanken via --sans | Closed with live evidence | PKG-1A WS08: direction-aware `--sans`/`--serif` tokens plus `html[dir=rtl]` body/footer/skip-link/form-control family. Live computed-style sweep on 5 AR pages (home, listings, rent-index, requirements, brokers; 1,589 Arabic nodes) shows 0 Hanken-Latin nodes. |
| 2 | P0 | Inline negative tracking survives RTL reset | Closed with live evidence | PKG-1A WS08: inline tracking removed at source on the flagged pages plus an authoritative `!important` RTL letter-spacing reset (figures/mono-numeral/lang-toggle excluded). Same live sweep shows 0 tracked Arabic nodes across all 5 pages. |
| 3 | P0 | Verification states merged into Owner-verified | Closed with live evidence | ADV-1 (`1047ee6`, `bbed926`, `0625309`, `0c4e615`). The merged claim is gone at its source, not renamed at its surfaces. `src/lib/evidence.ts` types the dimensions (ownership, authorization, identity, permit, listing operator) and their states; `src/lib/listingVerification.ts` resolves one row into those dimensions behind four independent fail-closed conditions (not a fixture, a method that names a real check, a date of check, a countersigning actor), so no single column can carry a badge and `gate.ts` stays the narrower truth source. On the 88 published rows every dimension resolves unverified, which is the correct reading of a corpus whose `verification_method` is `seed` on 50 rows and null on 38, whose `verified_by` is null on all 88 and whose `is_demo` is true on all 88. Live EN and AR on `0c4e615`: `retired_en_verified_owner: 0` and `retired_ar_verified_owner: 0` across all twelve saved public pages, against 4 and 4 at `0625309`; the PDP verification panel reads "Not verified" / "غير موثّق" in `var(--ink)`, and the lister byline reads "Identity verified" / "الهوية موثّقة" only when the identity dimension itself resolves. Owner decision O3 is answered by that dimension model. |
| 4 | P0 | Internal reference H1, N/A in metadata | Confirmed open | Verified: title falls back to reference_code (2 published AR titles missing); grade label renders N/A in description. WS12/WS17. |
| 5 | P0 | Q2 vs Q1 reporting-period mismatch | Closed with live evidence | DB evidence: rent_index_published carries only 2026-Q2 (7 rows, sufficient). Arabic corrected to الربع الثاني in both strings; advisor now renders via formatPeriod; parity test added. |
| 6 | P0 | وسطاء الأحياء mistranslation on Pricing | Closed with live evidence | Copy verified live in PKG-0A. Codex reopened the API layer; PKG-0A.1 added the toPublicSegment contract. Live on 958d7e2: /api/index/segments returns 7 rows whose keys include `average` and exclude `median`; advisor band payloads mapped the same way; EN analyser renders "Published band: ... average 1,420.5 · Q2 2026". Contract test guards regression. Internal column rename stays under rank 31. |
| 7 | P0 | Preview canonicalizes to unowned satmarkets.sa | Closed with live evidence | site.ts now env-aware (NEXT_PUBLIC_SITE_URL, then VERCEL_PROJECT_PRODUCTION_URL, then VERCEL_URL); no satmarkets.sa fallback. |
| 8 | P0 | /compare in sitemap while noindex | Closed with live evidence | Removed from sitemap ROUTES. |
| 9 | P0 | Counsel placeholders in legal pages | Blocked by evidence or decision | Needs Saudi counsel (WS30). Pages remain noindex. |
| 10 | P0 | Developments flow through district parameter | Partially addressed | WS04 done: docs/taxonomy.md + typed src/lib/locationKind.ts (a development is never labelled a district; unknown kinds coerce to neutral area; mixed lists use the Location umbrella), tested. The district URL parameter rename is Phase 2 (WS19), launch-adjacent. |
| 11 | P0 | Availability shown as static trust statement | Fixed and awaiting deployment verification | Freshness thresholds are WS13/WS18. PKG-AV1 removed the static part of the statement: `availability_confirmed_at` decays into three states that read differently in words, and the aging state no longer says "Available" or "متاح" at all, because a two-month-old affirmation is not a statement SAT can make in the present tense. Every state now carries the age of the affirmation in Western numerals in both locales. What remains open is the other half, which is not a display problem: there is no re-confirmation cadence and no lister-side prompt to re-affirm, so an affirmation can only decay, never refresh. That belongs to the lister workspace package, not here, and PKG-AV2 is that package. The deployed evidence taken at PKG-AV1 closure dates it: every record in the sampled preview corpus has `availability_confirmed_at` equal to `published_at`, none has ever been re-affirmed, and the oldest cross the 60-day stale threshold on 15 August 2026. **PKG-AV2 built that half, in `ee9f3a9`, deployed on `dpl_9hctvptNTHBwpPQvBQKKhxScez4D`.** `listerAvailability()` shows the lister the exact sentence an occupier reads, in the exact colour, plus what changes next and in how many days, computed from the same two threshold constants the public label reads. `AvailabilityReaffirm` writes `availability_confirmed_at` and nothing else, one listing at a time, on published listings only. Deliberately still not built, and this is a design position rather than an omission: no re-confirmation cadence, no reminder, no expiry, no bulk affirm. Law 3 says the date must be a real event, and an affirmation SAT prompted into existence across a portfolio with one click is the same guess with a newer timestamp. The status is not "Closed with live evidence" because it cannot honestly be: the display half is closed with live evidence (browse-page counts identical in EN and AR at closure, 168 aging and 8 fresh in each, Western numerals in Arabic), but the re-affirmation half lives behind the session gate, and the only live channel in this environment is GET-only and unauthenticated, so the rendered lister surface cannot be fetched. Record-level state at closure, from the deployed public API: 50 published rows, `availability_confirmed_at` equal to `published_at` on all 50, ages 17, 19, 33, 34 and 46 days, so 3 fresh, 47 aging, 0 stale. Closing this row requires one authenticated check of the lister dashboard. |
| 12 | P0 | Map/Advisor/Requirements inherit root metadata | Partially addressed | WS12 metadata factory upgrade, Phase 1. `/list` was a fourth route with the same defect and was fixed in PKG-SUP1: it now exports `generateMetadata` returning `localeMeta`, so a shared link and a browser tab carry the route's own bilingual title and description rather than the root layout's site name. Map, Advisor and Requirements are untouched and the finding stays open on them. |
| 13 | P1 | Area/lister missing hreflang alternates | Confirmed open | WS12. |
| 14 | P1 | x-default missing from page metadata | Partially addressed | Sitemap emits x-default; pageMeta does not yet. WS12. |
| 15 | P1 | OG/Twitter images absent | Confirmed open | WS12/WS15. |
| 16 | P1 | Arabic uses Latin m² | Confirmed open | WS11 unit formatter. |
| 17 | P1 | Arabic month plurals wrong | Confirmed open | WS11 with editorial review. |
| 18 | P1 | Map controls English in Arabic | Closed in PKG-A11Y-1 | WS19. The legacy row carried no detail; this is the systemic root cause behind it and behind finding 160. Every map on the platform is built by calling `new maplibregl.Map({...})`, and MapLibre writes the accessible names of everything it constructs itself: the canvas, the zoom buttons, the attribution toggle, the draggable marker, the logo link and the popup close button. None of those strings originate in this codebase, so none was translated, and no site had ever passed a `locale` option. Four components each shipped an Arabic page whose only map controls announced in English. SC 3.1.2 language of parts and SC 4.1.2 name, role, value. `src/lib/mapLocale.ts` builds MapLibre's `locale` object from a new `mapControls` dictionary section in `src/i18n/dictionaries/en.json` and `ar.json`, and all four construction sites now pass it: `src/components/ListingsMap.tsx`, `src/components/MapExplorer.tsx`, `src/components/LocationFacts.tsx` and `src/components/LocationPicker.tsx`. The merge above is why a partial object is safe: an omitted key keeps MapLibre's own default instead of throwing at `_getUIString`. The table covers `Map.Title`, `Marker.Title`, `NavigationControl.ZoomIn`, `NavigationControl.ZoomOut`, `NavigationControl.ResetBearing`, `AttributionControl.ToggleAttribution`, `AttributionControl.MapFeedback`, `LogoControl.Title` and `Popup.Close`, and deliberately omits `ScaleControl`, `FullscreenControl`, `GeolocateControl`, `TerrainControl` and the cooperative-gestures strings because no site in `src` constructs any of them. Guard tests in `src/lib/formGroups.test.ts` assert that each of the four sites builds exactly one map, that each map is passed `mapLocale(...)` from the shared module, that every `NavigationControl` still passes `showCompass: false` so the one translated string that is not currently drawn cannot start rendering unnoticed, and that none of the five untranslated controls is introduced. Evidence classification: automated source scan, guard tests and source-level verification of the installed maplibre-gl 4.7.1 bundle at `node_modules/maplibre-gl/dist/maplibre-gl-dev.js`, where the `defaultLocale` table, the `_getUIString` resolver, the `setAttribute('aria-label', ...)` call sites and the `this._locale = Object.assign(Object.assign({}, defaultLocale), options.locale)` merge were each read directly. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 19 | P1 | Six ASCII commas in Arabic dictionary | Confirmed open | Verified count = 6. WS11 with punctuation lint. |
| 20 | P1 | Claims outrun evidence (No one else, live now, 100%) | Confirmed open | Claim ledger created (docs/claims-ledger.md); wording changes are WS11/WS15 after Saleem approves preview-state language. |
| 21 | P1 | Pricing looks purchasable | Blocked by evidence or decision | Saleem decision: concept label vs hide (deferred per Codex 9). |
| 22 | P1 | English leakage in Arabic new-listing flow | Closed in PKG-A11Y-1 | The register row carried no detail, so the finding was re-established against the current tree before it was touched, and it is not what the short title suggests. Every visible string on the intake form is already bilingual: a scan of `src/components/ListingStudio.tsx`, `src/components/MediaBrief.tsx`, `src/components/LocationPicker.tsx` and `src/app/[locale]/dashboard/new/page.tsx` found one hardcoded English sentence in the four, the two words `Not configured.` on the misconfiguration branch of the route. The leakage is on the unhappy path. `/api/listings` and `/api/listings/[id]` between them refuse a save for twenty-six distinct reasons, every one of them an English sentence composed in the route and returned as `{ error: "Enter a valid area." }`, and `ListingStudio` rendered it verbatim through `setError(json.error || ...)`. So an Arabic lister who filled in an Arabic form, on an Arabic page, under an Arabic heading, was refused in English, on the one path a first-time lister is most likely to meet and least able to recover from. Two of those sentences were worse than untranslated: the edit route composed `${label}: ${message}` from `field.label_en`, sending the English registry label even where the reader's language was known, and both routes returned PostgREST's own `error.message` on a failed write, which is an internal database sentence shown to a member of the public and an information-disclosure smell as well as a language defect. SC 3.1.2 language of parts and SC 3.3.1 error identification are engaged; severity P1, journey 2, Listing Studio and inventory management. Fixed by following a precedent already in the tree rather than inventing a convention: `src/app/api/listings/[id]/route.ts:185` already answered a location contradiction with `{ error, error_ar, code }`. Every error response in both routes now carries a stable `code`; `src/lib/listingIntakeErrors.ts` names the twenty-six codes as `[en, ar]` pairs and is shaped exactly like `documentKinds.ts`, `planTypes.ts` and `labels.ts`; and both clients render the named sentence instead of `error`. A route knows what happened and does not know who is reading, because the reader's language is a property of the page and not of the request body, so the division of labour is deliberate. `error` stays on the wire in English for logs and API consumers and is rendered by nobody. An unrecognised code falls to the generic failure in the reader's language, so a route added later cannot reintroduce the defect by forgetting the table; the worst it can do is be vague. Two refinements beyond the base repair: where the edit route sends a record-specific bilingual pair it is preferred over the table entry, and a rejected registry attribute is prefixed with the label from `fieldLabel`, the same function that drew the box the lister typed into, rather than with the route's `label_en`. Four guard tests in `src/lib/formGroups.test.ts` assert that every brace-matched error payload in both routes states a code, that the emitted codes and the named sentences are the same set in both directions, that neither client renders the route's sentence, and that the misconfiguration branch is bilingual. The sixteen further client sites in this repository that still render a server-composed English sentence are a wider class and are recorded separately as finding 203. Evidence classification: automated source scan and guard tests, 1633 tests passing, Arabic lint clean, prose gate 0. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 23 | P1 | Search q ignored, returns full inventory | Closed with live evidence | PKG-2A (b3e2dfa). `q` is parsed server-side by the deterministic bilingual parser (`src/lib/search/queryParse.ts`, 22 tests) and narrows the result set. Live EN: a six-constraint sentence returned 0 and withdrawing two readings returned 6; `?q=office+in+Al+Olaya&deal=lease&sort=price` returned 8 with the other constraints intact. Live AR: same query, six Arabic chips, zero Latin characters, same counts. Unrecognised terms render a "not used" line rather than being silently dropped. See docs/pkg-2a-closure.md. |
| 24 | P1 | Lister type contradicts badge | Closed with live evidence | ADV-1 (`bbed926`, `0625309`). The contradiction was measurable and was measured: `listings.lister_type` and `accounts.type` are different vocabularies, and they disagree on 9 of the 88 published rows (4 claim `broker_authorized` while the filing account is an owner, 4 claim `owner_direct` while the filing account is SAT, 1 claims `broker_authorized` from SAT). `normalizeListerRelation` and `normalizeAccountType` in `src/lib/listingVerification.ts` keep them apart by type, and no surface now derives a party badge from the other vocabulary: the byline states the filing relation, the role tag states the account role, and the verification badge states a named dimension, so the three can differ without any of them being wrong. Live EN and AR on `0c4e615`: no page prints a verification badge naming a party who did not file the listing. |
| 25 | P1 | Title and district can disagree | Not independently reverified | WS17. |
| 26 | P1 | Touch targets below 44 px | Closed in PKG-A11Y-1 | PKG-2A measured the listings query surface live at 320, 360, 390, 430, 768 and 1280px in both languages: minimum interactive height 44px at every touch-capable width, in EN and AR, with zero horizontal overflow and no control rendered outside its containing block. That evidence covered `/listings` only, which is why it stayed partially addressed: the shared chrome that appears on every other surface was never measured, and finding 139 turned out to be exactly the case it missed. PKG-A11Y-1 RC5 closes the remainder at the root. The gap was not a set of short controls; it was that nothing in the repository could measure the floor at all. `scripts/responsive-probe.mjs` launches a plain desktop page, so `(pointer: coarse)` never matched there and the whole floor was invisible to it: the probe would have reported the desktop sizes and passed whatever the floor said. `scripts/touch-probe.mjs` closes that gap, emulating touch, hard-failing with exit 2 if the coarse query did not actually match before it asserts anything, and running the identical page a second time with touch off so the desktop rendering can be read rather than trusted. The four enumeration gaps found and closed are recorded in full against finding 139. Result: 224 control measurements, every control at or above 44px wherever the coarse query matched, overflow 0 at every width in both locales, and the fine-pointer table unchanged, which is the evidence that no desktop layout moved. Two related items are deliberately NOT claimed by this closure. The width-keyed block at `src/styles/globals.css` (`@media(max-width:1024px)`) still keys the checkbox and radio floors on viewport width rather than on pointer type; that is finding 188, which stays open on its own terms. And the 29px fine-pointer measurement at 1280 noted in the original evidence is a precise-pointer desktop, where SC 2.5.8 asks 24px, so it was never a failure and is not one now. EVIDENCE CLASS: automated scan plus browser-emulated touch measurement, EN and AR, six widths. NOT tested on a physical device and NOT independently audited; physical-device verification remains outstanding and is recorded as such in `docs/a11y-p1-triage.md`. |
| 27 | P1 | Arabic mobile text below reading size | Closed in PKG-A11Y-1 | The 2026-07-20 uplift is in the source and has no effect. `globals.css:122` sets `[dir="rtl"]{--fs-cap:11px;--fs-3xs:12px;--fs-2xs:13px;--fs-xs:14px;--fs-sm:15px;--fs-base:17px}` and `globals.css:395` sets `:root{--fs-cap:9px;...--fs-sm:13px;--fs-base:14px;...}`. `:root` and `[dir="rtl"]` are both specificity (0,1,0), so the later declaration wins, and the `:root` block is 273 lines later. Every one of the six tokens the RTL block declares is overridden; the Latin scale is what Arabic renders. Measured, not inferred: `scripts/responsive-probe.mjs` on the hero chip row reports computed `font-size: 13px` under `dir="rtl"` at all six widths, and the ordering is confirmed in the compiled output (`--fs-sm:15px` at byte 29783, `--fs-sm:13px` at byte 48444). The fix is one line, but its blast radius is every Arabic surface and neither channel that could measure that blast radius is currently available, so it is not being shipped blind. First item of the parked visual-quality package (type scale lock). Closed in PKG-A11Y-1 RC4, and it turned out not to be one line. The selector is now html[dir="rtl"], specificity (0,1,1), which beats :root at (0,1,0) on specificity rather than on position, so a future reorder of globals.css cannot silently discard it again; dir is set on the html element itself at src/app/layout.tsx:57, so the selector matches the same element :root does. Making the block live then exposed a second defect the dead block had been hiding: the uplift declared only six of the thirteen steps, so a live Arabic --fs-base would have equalled --fs-lg and exceeded --fs-md, inverting the scale. The remaining steps are therefore raised too, at the smallest values that keep the Arabic scale strictly increasing and keep each step's order relative to its Latin counterpart: md 1.125rem, lg 1.1875rem, xl 1.375rem, 2xl 1.8125rem, 3xl 2.25rem, 4xl 3.0625rem. --fs-input is deliberately not raised because it is the iOS focus-zoom floor, not a reading size. The blast radius the earlier entry could not measure was measured: scripts/responsive-probe.mjs against container Chromium reports the hero chip row computing 13px under dir=ltr and 15px under dir=rtl for the first time, the Arabic row growing from 208.3 to 236.6 pixels wide inside a 284 pixel content box at the 320 pixel viewport, and no overflow at any of the six widths across all 13 fragments. Evidence class: browser-emulated fragment measurement, not a physical device and not a whole page. Remaining independent-verification item: Arabic reading size judged by an Arabic reader on a real phone. |
| 28 | P1 | Post-requirement sentence unclear | Closed in PKG-DEM1 | WS11/WS23. Reopened with a measurement while scoping PKG-DEM1, because the original note recorded a wording problem and the route has a larger one underneath it. The form does not submit at all from its shipped state: see finding 100. The wording half stands on its own and is scoped into PKG-DEM1 with it, since the sentence a visitor is shown when their requirement is refused is the sentence this finding is about, and today it is one server sentence in a card at the foot of the page with nothing tying it to any of the fourteen controls. PKG-DEM1 closed it: the form now validates before it sends, names each problem against the control that carries it through `aria-describedby` and `aria-invalid`, moves focus to the first control the visitor has to return to, and keeps one `role="alert"` summary above the fields so a screen reader hears that something happened at all. The five messages are dictionary values in both languages, so the sentence an Arabic visitor reads is written rather than translated by the server. |
| 29 | P1 | English comma splices | Confirmed open | WS11. |
| 30 | P1 | Arabic calques | Confirmed open | WS11 editorial pass. |
| 31 | P1 | Key names disagree with displayed terms | Partially addressed | Display now says averages; internal `median` column rename deferred (schema change, supervised). |
| 32 | P1 | Listing alternates omit x-default | Confirmed open | WS12. |
| 33 | P1 | /verify not covered by noindex middleware | Closed with live evidence | Verified all three /verify pages already 404 for non-SAT (su.isSat gate); /verify, /ops, /proto added to noindex prefixes. |
| 34 | P1 | Footer advertises SATMARKETS.SA | Closed with live evidence | Footer shows bilingual brand name only; law test bans the domain in dictionary copy. |
| 35 | P1 | /list looks like a working form | Closed in PKG-SUP1 | Reverified on the deployment before it was fixed, not taken on trust: `/en/list` and `/ar/list` each returned 8 `<label>` elements over 0 `<input>`, 0 `<textarea>`, 0 `<select>` and 0 `<form>`, with every field a `<div class="input">` holding another party's answer and a four-step bar showing step 1 ticked and step 2 active. Three harms, not one. An owner believed they were mid-submission and lost their work on leaving; a photograph dropped on the "Drag photos here" zone vanished with no message; and a screen reader announced a "Listing title" field with nothing to focus. The four steps also described an intake that does not exist, the real one having ten stages. The replacement describes the real intake by computing it. `src/lib/listIntake.ts` reads `studioSteps()` for the stages, and `DRAFT_REQUIRED_CHECK_KEYS` with `assessListing()` for the seven facts the write path refuses to save a draft without, so the public description and the intake are the same object and cannot drift. A stage is listed only where every asset type has it, the step range is stated as 10 to 11 rather than at the flattering end, and the asking figure names both deals because lease and sale genuinely disagree. Thirty-three dictionary keys holding fabricated answers were deleted in both locales. `src/lib/listIntake.test.tsx` carries eleven tests including a general orphan-label guard with its own sensitivity test. |
| 36 | P2 | WebSite/SearchAction schema absent | Closed with live evidence | PKG-2A (b3e2dfa). `website(locale)` in src/components/JsonLd.tsx is emitted per locale from the locale layout, so the Arabic document advertises an Arabic entry point (`inLanguage: "ar-SA"`, `/ar/listings?q=`). The SearchAction was deliberately withheld until rank 23 was fixed, because a declared search endpoint that returns the same page for every term is a claim the platform cannot honour. |
| 37 | P2 | Entity schema lacks verified fields | Not independently reverified | WS31. |
| 38 | P2 | No kind-aware entity pages | Confirmed open | WS31/Phase 5. |
| 39 | P2 | Quality rules not in automated gates | Partially addressed | laws.test.ts + analyser.test.ts + taxonomy.test.ts add forbidden-term, period-parity, key-parity, analyser-guard, taxonomy and release-state gates; raw-color-scan reports the hex baseline; metadata/font/tracking CI automation is WS36. |
| 40 | P2 | No repository CI workflow | Confirmed open | Gate is local ship.py; GitHub Actions is WS36. |
| 41 | P2 | 1 high + 1 moderate dependency finding | Confirmed open | Verified via npm audit. WS34, patch without breaking build. |
| 42 | P2 | No Content Security Policy | Confirmed open | WS34, report-only first. |
| 43 | P2 | No formal English house style | Confirmed open | British English adoption is WS11. |
| 44 | P2 | No promise ledger governance | Partially addressed | docs/claims-ledger.md created; enforcement (no claim ships without entry) becomes real in WS11/WS36. |
| 45 | P1 | Rent Index named without its attribution in most of the dictionary | Partially addressed | Found by the ADV-1 value-level audit, which corrected the four places where the index was cited as a SOURCE beneath a figure and one where it was claimed as ours. Those are fixed. What remains is the rest: 36 English values and 59 Arabic values name the Rent Index with no REGA attribution anywhere in the string. Most are structurally incapable of carrying it (`nav.rentIndex` = "Rent Index", `footer.rentIndex`, `pricing.rowRentIndex`, `rentIndex.eyebrow`, `building.rentIndexLink`), and some are page metadata where a 44-character Arabic attribution would consume the description (`rentIndex.metaTitle`, nine `metaDesc` values). Owner ruling 2 requires that "every Rent Index reference must retain the required attribution to the REGA Rental Index (Ejar)", and a nav label is a reference. The ruling therefore needs a scope line the code can enforce: which classes of string are citations (source labels, disclaimers, figure captions, structured data) and must carry the full attribution, and which are navigation to a page that itself carries it. Recorded as a decision needed rather than swept, because sweeping it would put a 44-character Arabic string into a tab label. ADV-3A.1 moved this from open to partially addressed on the citation class only: every place the index is cited beneath a figure, in a source label or in a runtime-composed sentence now names it through `src/lib/market/attribution.ts` (finding 61), and a source-tree scan fails the build if a second spelling appears. The metadata objection was measured rather than asserted: the longest Arabic `metaDesc` carrying the full attribution is 206 characters against an English 207, so the description class can carry it and is no longer an excuse. What remains open is the navigation and label class (`nav.rentIndex`, `footer.rentIndex`, `pricing.rowRentIndex`, `rentIndex.eyebrow`, `building.rentIndexLink`), which is a scope decision for the owner under ruling 2, not an engineering blocker. |
| 46 | P1 | The browse card paints two different meanings the same green, and the availability label carries no date | Closed | `src/app/[locale]/listings/page.tsx:485` colours the availability dot and label `#1B7A50` when `availabilityOf(availability_confirmed_at)` is fresh, and `src/components/ListingCard.tsx` paints the verification tick `#1B7A50` on the same card. D24 permits both (availability keeps green only when backed by `availability_confirmed_at`), but on one card the reader sees the reserved colour twice for two unrelated claims. Worse, `availabilityShortLabel` returns "Available" / "متاح" for BOTH the fresh and the aging state and returns no date in either, so the only thing separating a listing confirmed this week from one last confirmed months ago is a colour, which is exactly what D24's own "shown with a non-colour label/date" clause exists to prevent. Needs a D24 follow-up ruling: whether availability keeps the reserved green at all now that verification uses it on the same card, and what the label must state (a date, an age in days, or a distinct word per state). **Closed in PKG-AV1.** The follow-up ruling needed no owner decision after all: the standing quality rule "verified green appears only for evidence-backed verification" already answers it, and a date the lister typed is not a check anybody ran, so availability gives the colour up. `availabilityTone()` is now the single writer of the availability colour and cannot return the reserved green; both surfaces call it instead of composing a colour inline; the availability exception is deleted from `greenReservation.test.ts` rather than narrowed, and both listing surfaces left its allowlist. On the label, all three answers the finding offered are taken at once: a distinct sentence per state, each carrying the age in Western numerals, in both locales. The aging state stops claiming "Available". Gated by ten tests in `src/lib/availability.test.ts`, including a source scan proving neither surface branches on `av.state` to pick a colour. |
| 47 | P2 | /building/[id] renders edge to edge, its own loading skeleton does not | Confirmed open | The route returns a bare `<section>` (`src/app/[locale]/building/[id]/page.tsx:75`) into `<main>` (`src/app/[locale]/layout.tsx:75`), which has no max-width and no side padding, and `ChromeGate` adds no wrapper. Its own `loading.tsx` renders inside `max-width:1280px; margin:0 auto; padding:24px 24px 64px`, so the skeleton is inset and the page that replaces it is not. Measured, not inferred: the `band-source` fragment in `scripts/responsive-probe.mjs` reports the card's content box equal to the viewport width at 320, 360, 390, 430, 768 and 1280 px in both locales. Not fixed inside ADV-1 because the correction changes every section on the route and its blast radius is layout, not claims; it joins the parked visual-quality package. |
| 48 | P2 | `listings` carries two advertising permit columns and the platform reads both | Confirmed open | `information_schema` shows `ad_permit_no` and `ad_permit_number` side by side, both `text`, both nullable. Measured on production: of 93 rows, 92 state `ad_permit_no` and 0 state `ad_permit_number`, and no row disagrees because no row fills the second column at all. It is dead and it does not read as dead: `src/app/[locale]/verify/page.tsx:47`, `src/app/[locale]/dashboard/page.tsx:49` and `src/app/[locale]/dashboard/listings/page.tsx:48` all select both, and `src/components/AdPermit.tsx:20` accepts both and falls back from one to the other. Nothing on the write path fills `ad_permit_number`: `src/app/api/listings/route.ts:56` validates and writes `ad_permit_no` only. So a reader of the schema, or a future query, has two plausible answers to "where is the FAL advertising permit" and one of them is empty on every row. The correction is a column drop and four call-site edits, which is a supervised schema change rather than a package edit, and the permit is a Law 1 surface so it is not being altered in passing. |
| 49 | P2 | `scripts/seed-world.mjs` writes an `ar_translation_status` the check constraint rejects | Confirmed open | Line 92 writes `ar_translation_status: "done"`. The constraint on production is `listings_ar_translation_status_chk CHECK (ar_translation_status = ANY (ARRAY['pending','machine','in_review','approved']))`, and the column is NOT NULL. `done` is not a member, so every insert the script attempts fails on the constraint and the seeder cannot run against the current schema. Latent rather than live: the published fixtures were loaded before the constraint reached its present form and no seed run is part of the ship gate, so nothing broke and nothing announced itself. Recorded rather than corrected because choosing the replacement value is a content decision, not a typo fix: `approved` asserts a human approved the Arabic and would put a false claim on 88 fixture rows, so the honest substitution is `machine`, and that should be made deliberately with the seeder's other fixture-honesty questions rather than silently here. |
| 50 | P1 | 152 Tailwind numeric-shade classes across five files compile to nothing, so the elements that use them render with no colour | Closed in PKG-A11Y-1 | `tailwind.config.ts` assigns a scalar hex string to `red`, `slate`, `amber`, `green`, `gold`, `signal`, `azure`, `harbor`, `charcoal`, `ivory`, `silver`, `ink`, `cool`, `paper`, `brass`, `stone`, `mid` and `line` under `theme.extend.colors`. A scalar replaces Tailwind's default palette OBJECT of the same name rather than adding to it, so every numeric shade of those eighteen names stops existing and any class naming one is dropped at build time. This is not inferred from the config: it was checked against the compiled stylesheet, where `text-red-600`, `border-red-600`, `text-slate-600`, `text-slate-900`, `text-slate-500`, `text-slate-400`, `border-slate-200`, `border-slate-100`, `text-amber-900`, `border-amber-300` and `bg-amber-50` all return zero rules, while `text-emerald-600` returns one because `emerald` was never overridden. The remaining sites are `src/app/[locale]/ops/page.tsx` (118), `src/app/[locale]/find/page.tsx` (29), `src/components/SampleBanner.tsx` (3 on one line, so the sample-data banner has no border, no wash and no text colour at all), `src/app/[locale]/login/page.tsx` (1, the error paragraph) and `src/app/[locale]/building/[id]/page.tsx` (1, `text-red-500` on the negative week-on-week figure, whose positive twin is `text-emerald-600` and does render, so a rise is coloured and a fall is not). Three sites were fixed inside ADV-2 because they were that package's own error states: the blocked-step marker, the contradictions panel border and the error paragraph in `src/components/ListingStudio.tsx`, all now on the scalar names `text-red` and `border-red/40`, both confirmed present in the compiled stylesheet. The other 152 are not fixed here because two of the five files are private operator surfaces whose intended palette has never been decided, and replacing 118 shade classes with scalar tokens is a colour decision on a surface, not a mechanical rename. It belongs to the parked visual-quality package alongside retiring the duplicate colour aliases. **Corrected in RC15.** This row said `Confirmed open, 3 of 155 fixed` for eight slices after the defect was closed. `beef75a` removed the remaining sites and `src/lib/palette.test.ts` holds the result at "no source file names a numeric shade of a colour the config overrides with a scalar". A scan of every `.ts`, `.tsx` and `.css` file under `src` at `7aaab03` for a utility prefix followed by any of the eighteen overridden names and a numeric shade returns zero matches in zero files. The register, not the fix, was out of date. Evidence class: automated. The ratios are arithmetic on the declared token values in the source at `beef75a`, recomputed at `7aaab03`, not a measurement of rendered paint, and `src/lib/palette.test.ts` recomputes them on every run so a future edit that reintroduces the step fails the suite. Not tested on a physical device, not tested with an actual screen reader and not independently audited. |
| 51 | P2 | A `flex:1` with `min-width` on the decision pack dimension rows pushed each row 16 px past its card at 320 px, in both locales | Closed with live evidence | Found by measurement, not by reading: the new `pack-dimensions` fragment in `scripts/responsive-probe.mjs` failed at `en@320` and `ar@320` with a 16 px row overflow inside a 230 px content box. `src/components/DecisionPackPanel.tsx` declared the detail span as `flex: 1` with `minWidth: 200`. `flex: 1` expands to `flex: 1 1 0%`, so the flex line-breaking algorithm decides whether the span fits as though it were zero wide, keeps the trailing state label on the same line, and only then applies the 200 px minimum, which pushes the assembled line past its container. Corrected to `flex: "1 1 200px"` with `minWidth` dropped, so the same 200 that constrains the span is the number the break is decided on. The probe now returns 120 measurements, 0 document overflow and no item wider than its content box. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 52 | P2 | Arabic counted-noun agreement is wrong for every count outside 11 to 99, in eight modules rather than the one first recorded | Closed with live evidence | Arabic agreement for a counted noun depends on the count: 1 and 2 each take their own form, 3 to 10 takes the plural genitive, 11 to 99 takes the singular accusative, and 100 or more returns to the singular. The original entry named `src/lib/decisionPack.ts` because that is where it was seen. Closing it meant looking for the class rather than the instance, and the class was in eight files. The formatter already existed and was already tested against `Intl.PluralRules`, so this was mostly an application problem, with one real gap in the formatter. CLDR gives Arabic a single `two` form, but Arabic marks the dual for case, and every one of these counted phrases sits after a preposition (`قبل`, `عند`, `خلال`) or under a governing noun (`مدة`, `البالغة`), where the oblique dual is the correct form and visibly different: `قبل يومين`, never `قبل يومان`. Rewriting each sentence so the dual never lands in that position is the patching the finding exists to stop, and would have to be redone for every sentence written afterwards. So `src/lib/format.ts` gained an optional `twoOblique` form on `PluralForms` and an `oblique` flag on `CountOptions`, threaded through `formatCount` and `formatCounted`. No other category needed one: the `few` and `many` forms already read correctly in both positions. A table-wide invariant test fails if a counted noun is added later without its oblique dual. `formatCount` also stopped rounding: it used `formatInteger`, so routing a fractional count through it would have turned 1.5 months into 2 months, which is a display layer altering a figure. Call sites corrected in both languages: `decisionPack.ts` (all eight, the six named plus the two the entry called correct only by accident), `listedSince.ts` (the badge lives for fourteen days, so the wrong form was the one nearly every reader saw), `matching.ts` (three timeline reasons), `listingQuality.ts` (two contradiction statements, four counts), `attributeDisplay.ts` (the `months` and `years` unit map, which nineteen registry fields use), `AdPermit.tsx`, `market/underwrite.ts` (the payback line) and `invest/page.tsx`. The English side was wrong at four of these too and was never recorded: `1 rent free months`, `1 months`, `1 day${s}`. Evidence: 773 tests pass, of which 15 are new and assert the RENDERED sentence rather than the formatter, at 1, 2, 3, 10, 11, 99 and 100 in `format.test.ts`, `decisionPack.test.ts`, `listedSince.test.ts`, `matching.test.ts`, `listingQuality.test.ts`, `attributeDisplay.test.ts` and the new `market/underwrite.test.ts`. Each Arabic set also asserts the nominative dual is absent where the phrase is governed. `npm run ar-lint` still cannot see agreement, and that has not changed: it matches banned constructions over a narrow file list. The tests are the gate for this class, not the linter. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 53 | P2 | `html,body{overflow-x:clip}` clamps the responsive probe's document-overflow assertion | Partially addressed | `src/styles/sat-platform.css:550` sets `html,body{max-width:100%;overflow-x:clip;}`. That is correct for the product, because it stops a stray wide element from making the whole page pan sideways on a phone. It also means `document.documentElement.scrollWidth` can never exceed the viewport, so the probe's `docOverflow` measurement reports 0 whether or not anything actually overflows. The consequence was visible in finding 51: the decision pack defect surfaced as `row ovf 16` with `doc ovf 0`, and a probe that trusted only `docOverflow` would have passed it. ADV-3A.1 kept this as a limitation rather than a defect, on the condition that the element-level assertions are the actual passing gate, and verified that they are. `scripts/responsive-probe.mjs:704` fails a run on `rowOverflow > 0 && !rail` or `widest > innerW`, both of which read element geometry; the `docOverflow > 0` term is retained only as a backstop should that CSS rule ever change. Two overclaims in the probe were corrected at the same time, because the script itself was asserting more than it checks: a comment stated that a leaking scroll rail is caught by the document clause, which it cannot be, and the PASS line announced `0 document overflow`, which is not a measurement anyone can act on. The comment now names the limitation at the point of the check and directs any future assertion to the element-level pair, and the PASS line no longer claims a document-level result. This stays open rather than closed because the underlying measurement remains unavailable; what is closed is the risk of a reader mistaking it for evidence. |
| 54 | P1 | Two provider call paths reached external models with no boundary call at all, and the ADV-0 closure record described the advisor's `llm()` as the platform's single choke point | Closed with live evidence | `docs/adv-0-closure.md:81` states that the boundary sits in `llm()`, the single choke point through which both the primary and fallback providers are reached. That was true of `src/app/api/advisor/route.ts` and false of the platform. `src/app/api/search/route.ts:32-70` carried its own inline array of base-url, key and model tuples and posted to two providers directly from `llmParse`, and `src/lib/translate/translateToArabic.ts:149-178` carried its own `callClaude` with its own key read, its own version header and its own error shapes. Neither file imported `aiBoundary` at all, so neither ever asked whether what it was sending was permitted. What each actually sent happened to be allowed, which is why nothing failed and nothing announced itself: a search box query is the user's own words, and listing copy is the owner's own text submitted by that same owner under their own row-level security. The defect is that permission was never established. A second path around a boundary is a defect even when the material on it happens to be allowed, because what travels on a path changes and the absence of a check does not. A third drift sat alongside it: the advisor declared what it sends by hand in `ADVISOR_PROMPT_PARTS` and then sent a different array, so the checked list and the sent list could disagree with nothing to notice. Corrected in ADV-3A by making the check a precondition of the call rather than a step inside it. `src/lib/ai/transport.ts` is the only module in the repository that opens a socket to a provider; it is reachable only from `src/lib/ai/gateway.ts`; the gateway runs the boundary before it selects anything, so a denial can never be recorded as a provider failure or retried against a different vendor; and it derives the parts it checks from the messages it is about to send, through `partsOf`, so the two are the same objects and cannot differ. `ADVISOR_PROMPT_PARTS` was deleted rather than kept in sync, and a source scan now fails if any standing `*_PROMPT_PARTS` declaration returns. All three call sites go through `callModel`. Three structural tests in `src/lib/ai/gateway.test.ts` read the whole `src` tree and fail if any file outside the transport carries a provider request needle, if anything but the gateway imports the transport, or if the package index re-exports it, so a fourth path reopened later fails the gate instead of passing unnoticed. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 55 | P1 | A city the person typed was replaced by one arbitrary district of that city in English, and dropped entirely in Arabic, so `/api/search` answered a Riyadh query with a Dammam listing | Closed with live evidence | Found by the live EN and AR exercise Codex item 5 required, not by reading. The deployed advisor was asked "warehouse for lease in Riyadh" and answered: no exact matches, so here are the closest 6, some are outside KAFD. Nobody typed KAFD. The mechanism was three lines in `src/app/api/search/route.ts`: the district matcher's fallback branch tested the raw query against the districts table's `city` COLUMN, `.find` returns the first row satisfying the predicate, and the resulting filter was `.eq("district_id", <that row>)`. So every English Riyadh query collapsed to whichever Riyadh district sorted first, and the relaxation note then named it. The Arabic half failed the opposite way and was worse: `الرياض` matches no district's Arabic name and contains no Latin text, so nothing matched, no place filter was applied at all, and a Riyadh query returned a Dammam listing. One fault, two faces: invent a constraint the person did not give, or drop the one they did. Corrected by deleting the city clause from the district matcher and resolving the city separately. `rulesParse` now returns a `city` key, read from `CITY_SPELLINGS` in `src/lib/labels.ts`, which is the same closed vocabulary every other surface renders, so a second drifting list cannot exist. `llmParse` validates any model-supplied city through `cityKey` and drops an unrecognised one. The route filters by every district id in that city. Resolving a city this way is recognition, not a guess, because the vocabulary is closed, finite and already published; a district vocabulary is open and lives in the database, which is why `rulesParse` still returns `district: null` rather than guessing one from a word list. Place resolution moved into `src/lib/search/place.ts` because a route module may only export HTTP handlers, so the resolver that shipped was untestable by construction. Two latent faults were caught while moving it: `"anything".includes("")` is true, so a districts row with a null `name_en` matched every query ever typed and won by being first, now guarded; and `.in("district_id", [])` is not expressible in the query builder, so `build()` returns null for a city we hold no districts in and the loop treats it as zero rows, rather than silently dropping the filter, which is the same defect class. Evidence: 11 new tests in `src/lib/search/place.test.ts`, one of which asserts the exact live sentence can no longer be produced. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 56 | P1 | An English relaxation phrase was composed on the server and interpolated into the middle of an Arabic sentence | Closed with live evidence | `src/app/api/search/route.ts` built `relaxedReason` as English prose, for example `outside ${dMatch.name_en}`, and `src/lib/useAdvisorChat.ts` dropped it into `بعضها ${j.relaxedReason || ...}`. An Arabic reader whose search had been widened was therefore told, inside their own sentence, that some results were "outside KAFD", in Latin script. Bilingual parity is not a translation of a finished sentence; it is each language owning its own sentence while the server says only which constraint moved. The route now returns a `relaxedBy` token of `budget`, `size`, `place` or null, plus a bilingual `place` object naming what it actually filtered by, and `src/lib/search/searchNote.ts` writes each language separately. The place name printed can only be the one the route says it applied, with no fallback reaching for a district name from anywhere else, so finding 55 cannot be reintroduced through the wording. Evidence: a test asserting the Arabic sentence contains no Latin script under every relaxation state. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 57 | P2 | The advisor's search notes bypassed the counted-noun formatter that closed finding 52, and shipped `6 مطابقة موثّقة` to the live site | Closed with live evidence | Finding 52 was closed by applying `formatCounted` across eight modules. This was a ninth, and it was photographed live. `src/lib/useAdvisorChat.ts` wrote `${results.length} مطابقة موثّقة` inline, which is correct only for 11 to 99. It was missed because the hook is `"use client"`, sits outside `scripts/prose-scan.mjs` scope, and was not in the Arabic lint's file list, so public Arabic prose lived where no gate looked. Corrected by moving the wording to `src/lib/search/searchNote.ts`, which imports only `@/lib/format` and is therefore unit-testable, and adding that module to the `ar-lint` file list so the voice gate now sees it. A `verifiedMatch` counted noun was added with the adjective inside each form, because Arabic agrees an adjective with its noun and the dual takes a dual adjective, so `${formatCounted(n, "match", "ar")} موثّقة` would have been correct at every count except two. The trailing descriptors were rewritten as verbal nouns so nothing downstream has to agree with the count. Evidence: 17 tests in `src/lib/search/searchNote.test.ts` asserting the rendered sentence at 1, 2, 3, 10, 11, 99 and 100 in both languages, that the dual carries no numeral, and that a budget cap renders in Western numerals inside Arabic. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 58 | P2 | The advisor's own suggestion chips did not reach the path their words promised | Closed with live evidence | Found live during the ADV-3A.1 EN and AR exercise. A chip that reads like a search was routed by the mode the chip carried rather than by what the sentence asks for, so a person following the platform's own suggestion landed somewhere other than where the words pointed. A suggestion is a promise the product makes to itself, and it is the one query every new reader is most likely to run. Fixed in `259792d` with the chip intent resolved from the same deterministic parser the typed query uses, so a chip and the identical typed sentence cannot diverge. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 59 | P1 | A ceiling typed into a query was read as an offered rent rather than as a search constraint | Closed with live evidence | Found live during the ADV-3A.1 EN and AR exercise. "under 1,600 SAR/m2" is a cap on what the person will pay. The parser recorded it on the field that means what the person is offering, so the number changed meaning between the sentence and the filter, and the results were narrowed on the wrong side of it. This is the figure-integrity class rather than a parsing nicety: a number the reader supplied was restated as a different claim. Fixed in `259792d` by separating the cap from the offer in the parse result and filtering on the cap. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 60 | P2 | A bracketed placeholder inside a suggestion was parsed as a district name | Closed with live evidence | Found live during the ADV-3A.1 EN and AR exercise. A suggestion string carrying a placeholder was handed to the district matcher, which does not know that a placeholder is not a place, so the platform searched for a district whose name was the placeholder text and then reported the result as though a real constraint had been applied. Same class as finding 55: a constraint nobody gave. Fixed in `259792d`. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 61 | P1 | Arabic dropped the REGA attribution from the Rent Index everywhere a sentence was composed at runtime, in five different spellings | Closed with live evidence | Owner ruling 2 requires the REGA Rental Index (Ejar) attribution on every Rent Index reference. English carried it everywhere. Arabic carried it nowhere a sentence was built in TypeScript at request time, and disobeyed differently in each place: five spellings of one source across `valueEvidence`, the advisor route, the analyser, `/ops` and `/proto`, none of them naming the General Real Estate Authority, all facing a single fixed English form that does. The rule was already tested, and both tests read shipped dictionary strings while all five offenders were composed at request time, which is the same shape as finding 57: the gate held exactly where it was easy to hold. Fixed in `2040a50` by making `src/lib/market/attribution.ts` the one place the source is named, with a source-tree scan that fails the build if a sixth spelling appears, and in `411f205` by extending the guard over the eight shipped dictionary strings that were also short. Confirmed live on the deployed preview in EN and AR during ADV-3A.1; see docs/adv-3a1-closure.md, section "Live evidence". |
| 62 | P1 | The rendered band shows mixed and false decimal precision on a published rent figure | Blocked by evidence or decision | Found live during the ADV-3A.1 exercise: the band renders `1,250.04` and `1,590.96` beside an average of `1,420.5`. Two decimals on the bounds and one on the average is mixed precision on one figure surface, and two decimals on a SAR/m2 annual rent is precision the source does not carry. Not rounded in passing, because deciding how precisely a published rent figure may be stated is a figure decision under Law 3 and belongs to whoever owns the index presentation, not to a formatting pass. Needs an owner or evidence ruling on the significant figures the Rent Index band may claim, after which the correction is one formatter. |
| 63 | P2 | A stored advisor thread can be replayed under a renderer that no longer agrees with it | Confirmed open | The advisor persists its thread in `sessionStorage` under `${storageKey}:${STATE_VERSION}:${locale}`, keeping the last thirty messages. The stored blob holds rendered text and result arrays, so a change to how a sentence is composed applies to new turns and not to restored ones, and a reader can be shown an old sentence beside new behaviour with nothing marking which is which. `STATE_VERSION` was deliberately not bumped for finding 65, because an old blob stays self-consistent under a renderer that no longer slices, so that specific case is safe. The general hazard is not: the version key is the only defence and nothing forces a change to it when a composer changes. Recorded rather than fixed because the correct answer is either storing the inputs rather than the rendered output, or making the version a hash of the composers, and both belong with the conversation-envelope work item 3 already points at. |
| 64 | P2 | RSC prefetch returns 503 seven times in a single page load, in both locales | Confirmed open, cause narrowed | Observed live in both locales, seven 503 responses on RSC prefetch requests during one page load, with no user-visible failure. Narrowed during ADV-3A.1: no 5xx appears in the Vercel runtime logs for those requests, so the 503 is produced before the function runs and never reaches application code. That rules out the route handlers and points at the platform edge or the deployment-protection layer answering a prefetch it will not serve. Not closed, because a repeated 503 on a live site is not something to leave unexplained even when nothing visibly breaks; what changed is that it is no longer attributed to the application. |
| 65 | P1 | The advisor's search note counted the rows the server returned while the screen showed fewer, so the sentence and the screen disagreed | Closed with live evidence | Found by exercising the deployed advisor in both languages. The note read "7 verified matches" and `7 مطابقات موثّقة` above four visible rows. `useAdvisorChat` stored everything `/api/search` returned and passed that length to the sentence, while the advisor page sliced to four and the widget to three. Three places each held a different idea of how many results there were and the one that spoke aloud held the wrong one. This is the figure-integrity class, not a display nicety: a count is a figure, and a figure that does not describe what the reader can see is the same untruth as an invented one. Fixed in `7a3c995` by truncating once in the hook, so the stored array is exactly what is rendered and the sentence counts that array, and by declaring the withheld rows rather than dropping them: "These are the closest 4 results of 18." / `هذه أقرب 4 نتائج من أصل 18.` English drops the leading numeral at one. Evidence: 8 tests in `src/lib/search/searchNote.test.ts` on the withheld-total branch, plus the live EN and AR turns on `7a3c995`; see docs/adv-3a1-closure.md, section "Live evidence". |
| 66 | P1 | Every listing card fell back from a missing title to the reference code, so an Arabic reader was shown `SATM-A0DC83D0` where an English reader was shown a sentence | Closed with live evidence | Found by putting the deployed Arabic advisor beside its English twin on the same search. One row, two readers, and only one of them was told what the space is. A reference code identifies a listing; it does not describe one. A row whose Arabic title has not been written yet is not thereby nameless, because its asset type and its district are known and they are exactly what the English sentence is made of. The class had already been found once and fixed once, in the share metadata of `listings/[id]/page.tsx`, whose own comment records the identical defect, and that fix stopped at one function while fifteen other call sites kept the idiom by hand. Per Codex item 6, corrected as a shared function with a source-tree guard rather than another local repair: `src/lib/listingTitle.ts` owns a four-rung ladder (own-language title, then "{type} in {place}" filled from the same dictionary key the share metadata uses, then the type alone, then the code, which is now reachable only by a row with neither a title nor an asset type and no such row can be published), the other language's title is deliberately not a rung, thirteen call sites import it, and two selects were widened so surfaces that had never been given an asset type can describe a listing at all. Evidence: 11 tests in `src/lib/listingTitle.test.ts`, of which one scans the whole `src` tree and fails if any file falls back from a title to a reference code again, with the needles assembled from fragments so the file does not match its own scan. Live on `7a3c995` the first Arabic card reads `مكاتب في العليا`; see docs/adv-3a1-closure.md, section "Live evidence". SECOND HALF, PKG-NM1, `0e490d8`. The ladder's own law, that the other language's title is deliberately not a rung, was enforced nowhere outside `listingTitle.ts`, and the hand-written form `(ar ? x.name_ar : x.name_en) || x.name_en` survived in twenty-two files including the public `/brokers` page. The correction separates the three things that idiom was being asked to do, because they have three different right answers. A district name is a DESCRIPTION, so `placeName` widens to the city, which is wider than the district, never false, and held in both languages. An account, lister or building name is an IDENTIFIER with no wider true form, so `entityName` keeps the one registered spelling and that is the single authorised borrow on the platform. The `district_label` family is somebody else's published statistical label and is out of scope by substance, recorded as finding 92. Both policies and the reason they differ live in one header, `src/lib/displayName.ts`, so a future call site inherits a decision instead of making one. Twenty-three call sites migrated, three Supabase selects widened to carry `city`, and the source guard in `listingTitle.test.ts` extended from the reference-code pattern to the other-language pattern with three constraints, one line, word-bounded field names and different-language pairing, because its first form reported twenty files with twelve innocent and a scan with that hit rate earns an allow list rather than a fix. The guard now has its own sensitivity test over the eight shapes actually deleted and seven that must stay legal. Live on `dpl_Av7coZHQkTJ1tJXvVw3vqaWuRaty`: the one published row with a blank `title_ar`, `SATM-BB3FCB59`, reads `Serviced offices, Al Aqiq` in English and `مكاتب مخدومة في العقيق` in Arabic on both its detail page and the public browse page, with no reference code and no Latin text in the Arabic rendering; `/locations` renders 77 district cards per locale with zero cross-language names in either direction; `/brokers` renders the same three sample cards in strict parity. |
| 67 | P2 | The verification page enumerated the five states and printed one label twice, because `unknown` and `not_verified` share badge text on purpose | Closed with live evidence | Found by reading the deployed DOM of `/en/verification` after `3011d99`, not by a gate. Every local gate was green and correctly so: the page is generated from `verificationStateLabel` rather than from copy, so the page rendered exactly what the engine says. What the enumeration exposed is a deliberate property of the engine that reads as a defect once the states are listed side by side. `src/lib/evidence.ts` maps `STATE_LABEL.unknown` to the same pair as `STATE_LABEL.not_verified`, so the states table printed `Not verified` twice, immediately beneath narrative copy reading "There are five, and two of them are not failures". The collapse is protective where it actually matters: on a listing badge, a dimension nobody has looked at must never read better than one that was looked at and could not be confirmed. Changing the engine would have made the explanatory page tidier and the product weaker, so the engine is unchanged and the page names the collision instead, as its own card in both languages, pointing the reader at the raw key beside each row as the thing that differs. The guard is the part worth keeping: the test computes label collisions from `verificationStateLabel` at runtime rather than hardcoding the pair, and requires the published explanation to name every colliding key, so a future relabelling that creates a new pair fails the suite instead of shipping a second unexplained duplicate. Second package in a row where the live surface caught what no local gate could, both times because the gate was scoped to the wrong question rather than because it was wrong. Fixed in `d5d1dfa`. Live on `dpl_HBmhMj5zZkSWxURPwrykMM5aWH5H` in both languages; see docs/adv-4b-closure.md, section "Live evidence". |
| 68 | P1 | A public listing page rendered a Mapbox-derived drive time and cached it for a day, while the register records `foursquare_mapbox` as `derived_display_policy: none` | Closed | Found by auditing egress rather than by any gate, because no gate was looking. `src/lib/locationFacts.ts:driveMinutes` called the Mapbox Directions API, held the answer under `next: { revalidate: 86400 }`, and returned `number | null` which the listing page rendered beside the airport and rail rows. Three separate defects in one function. It consulted no permission at all, so a value the register says may not be displayed in derived form was displayed in derived form on a public page. It stored a routing result for 86,400 seconds, which Mapbox forbids and which D27(a) forbids twice over: travel time is computed at request time and is never stored as a property fact, and a 24-hour cache is storage whatever the variable is called. And its return type carried no method and no time context, so a number that means "driving, typical off-peak, per one routing engine" reached the reader as a bare figure. The reason it survived is the part worth recording: with `MAPBOX_TOKEN` unset the function returned null, the page degraded cleanly to straight-line distance, and everything looked correct. The only thing standing between a public page and an unlicensed value was an unset environment variable. Fixed in ADV-5A by deleting the function and rebuilding the path as `src/lib/location/`, where the rights decision runs before the credential check on purpose, so this denies as `use_denied` rather than `no_credential`: a rights denial survives someone adding a token and a credential denial survives nothing. Travel time is now a typed union carrying its own method, time context and attribution. Evidence: `src/lib/location/boundary.test.ts` (18 tests, including one that adds every declared credential and asserts nothing opens) and `src/lib/location/gateway.test.ts` (19 tests, including three that install a throwing fetch and assert zero sockets). |
| 69 | P1 | Two API routes sent the visitor's typed query to Google Places, Mapbox Search and Photon with no consultation of the source register | Closed | `src/app/api/places/route.ts` read two API keys at module scope and tried three vendors in turn; `src/app/api/geocode/route.ts` called Photon directly. Neither looked at `source_registry`. `google_places` and `photon_osm` have no row in it at all, so under the register's own first rule they have no rights, and `foursquare_mapbox` records `redisplay_policy: none`. Separately from the display question, every one of those requests carried text a person typed into a search box, and a person typing text does not establish that the text is public: a tenant query can name a brand, a budget or an unannounced expansion. That is the same reasoning Codex applied to `user_own_words` in ADV-3A.1 item 1, and it is now enforced the same way, by a second gate. `PROCESSING_AGREEMENTS_IN_FORCE` is a compile-time constant rather than an environment read, because an environment variable is a deployment setting and this is a contractual fact. It is deliberately separate from `AI_AGREEMENT_IN_FORCE` and is not implied by it. Both routes now call the location gateway, all three vendors are denied, and the degraded states are documented in the files: autocomplete serves our own indexed districts alone, and geocode returns an empty list, which `LocationPicker` already tolerates because pasted coordinates, pasted Maps links and manual pin placement all still work. One live consequence was un-gating `indexedPlaces` from the `v=1` flag, since with the external list correctly denied a flag-less caller would have seen an empty box, and an empty box invites the reader to conclude we have no coverage rather than that we hold no licence. The AI structural guard's comment had waved past this exact route as "a legitimate maps integration", which was true of the header and quietly wrong about the route; that comment is corrected in place. |

| 70 | P1 | Two surfaces asserted a drive-time capability that D27(a) says this schema does not hold and the vendor terms forbid caching | Closed | Found by auditing every claim attached to a location source while building ADV-5B, not by a gate. `/ops` listed a row reading "Foursquare / Mapbox (geo)" with the note "POI + isochrones", rendered with `ok: true` so it showed as a green dot and counted toward the freshness total, and `src/lib/assetFields.ts` described `catchment_population` as "GASTAT population in a drive-time isochrone, once wired". D27(a) records that no isochrone table exists in this schema, that the server holds no Navigation-scoped token, and that the vendor forbids caching isochrone results at all. So the operations console reported a working capability that does not exist and the field catalogue documented a plan the terms prohibit. Fixed by renaming the row to "Foursquare open POI (stored)" with its Apache 2.0 basis and the note that no travel time and no isochrone is stored, and by rewriting the field help to a published district with an explicit "no isochrone is computed or stored (D27a)". |
| 71 | P1 | The sample trade-area page represented telecom partnerships, payment-gateway feeds and data partnerships that do not exist | Closed | Found by auditing `/area` before excepting it from the ADV-5B claims gate. Four source cards described where the numbers came from: "Anonymised GPS / Wi-Fi from Saudi telecom partners", "Anonymised transaction patterns from payment gateways feed revenue potential", "the live Saudi data partnerships behind them are described below", and an intro saying live sources "are being onboarded". None of it was true. Owner ruling 7 forbids representing that data rights exist, and "being onboarded" is the most damaging of the four because it dates the claim: a reader takes it to mean a signature has happened and a feed is in flight. A fifth card said published transaction records "calibrate price benchmarks against real deals", which contradicts D26, that the transaction portal is a service to read and is never scraped. Twelve strings were rewritten in both locales to describe what a source would have to satisfy rather than what exists, and `src/lib/location/claims.test.ts` pins the seven removed phrases in EN and AR so they cannot return. |
| 72 | P0 | The building profile manufactured this building's movement, catchment, dwell, daytime and spend figures from a generator seeded on the building id | Closed | Found by the ADV-5B structural claims gate on its first run, and it is the most serious figure-integrity defect found on this platform. `src/app/[locale]/building/[id]/page.tsx` hashed `b.id`, fed it to a linear congruential generator and produced a weekly visitor curve, a week-on-week percentage, a 24-hour rhythm with prayer-time dips, three drive-time catchment rings, a dwell figure, a working-age share, a daytime index and a spend index. It was per building and stable across reloads, which is worse than obvious noise: a number that does not move is a number a reader concludes was measured. Three of them rendered in the overview row as `tone="live"` with no sample label at all, in the same colour as the counted unit and brief totals beside them. The catchment rings additionally asserted a travel-time computation that D27(a) prohibits. This breaches "no invented figures" and Law 3. Fixed by deleting the generator and both panels, reducing the overview row to the three counted tiles, and rendering one panel resolved through `districtMobilityPanel`, which today states that no movement source is licensed. The page note, which promised "real movement data via a partner", was rewritten. `claims.test.ts` asserts the generator constant and `Math.random` are absent and that the page reaches a figure only through the panel module. |
| 73 | P1 | The location package's barrel re-exported the mobility verdict, putting licence reasoning one plain import away from any page | Closed | Found while wiring the building profile to the ADV-5B interface. `src/lib/location/index.ts` re-exported `mobilityFigure`, whose `MobilityUnavailable` carries a `reasons` array quoting licence and contract reasoning, a register denial `code` and a list of unanswered clause identifiers, all internal under the `denialReason` rule. The barrel already omits `transport.ts` for the same class of reason, so the omission argument was established and the mobility export was simply inconsistent with it. Fixed by exporting the mobility names as types only, so nothing callable crosses, and exporting `districtMobilityPanel` instead, whose view type has no field for a reason, a code or a clause. `claims.test.ts` asserts the barrel's mobility statement is a `export type {` form and that `panel.ts` is the only runtime importer of the module. |
| 74 | P2 | The building profile is a public, indexable route that appears in none of the three route lists | Confirmed open | `/building/[id]` generates metadata, emits a BreadcrumbList and is reachable from the map, yet `src/lib/routePolicy.ts` has it in none of `SITEMAP_ROUTES`, `HELD_ROUTES` or `PRIVATE_PREFIXES`. So it is neither declared for indexing nor held back from it, and the decision about it has never been recorded. Raised while fixing finding 72 and left open deliberately: the route's indexing status is an owner and Codex decision about what SAT publishes, not a defect to be settled inside an engineering package. Whichever way it goes, it should be an explicit entry rather than an absence. |
| 75 | P2 | The platform's two habitual muted text tiers fall below WCAG AA, and one of them was carrying the entire content of the new movement panel | Corrected where it was load bearing, otherwise open | Found by the accessibility check on the ADV-5B panel rather than by any gate, because no gate measures contrast. `text-charcoal/45` composites over the paper to rgb(149,151,152), which is 2.93:1, and `text-charcoal/55` to rgb(126,128,130), which is 3.96:1. WCAG AA for normal text is 4.5:1, so both fail, and they appear 21 and 19 times across `src`. In most of those places the tier sits on a footnote beside a figure the reader can already see, which is a legibility complaint. On the movement and catchment panel it was not: the status line at /55 was the only thing telling a reader why there is no figure, and the rule paragraph at /45 was the publication rule itself, so the muted text was the whole content of the panel. A footnote nobody can read is a footnote; an explanation nobody can read is an absence. Raised to /70 (6.61:1) and /60 (4.69:1) on that panel only, in `df2f216`'s successor, with the reasoning recorded in the JSX so it is not read later as the opening move of a colour sweep. The other 40 uses are not touched here: correcting them is a platform-wide visual change and belongs to the parked visual-quality package, and the standing rule against another broad cosmetic sweep applies. The wider point is that contrast has no gate at all, which is why a defect of this class survived every green run; adding one is the natural first item of that package. |
| 76 | P1 | The Evidence Passport and the provenance tier are built, typed and tested, and nothing on any rendering surface or request path reaches them | Closed | Found by the second strategy reconciliation, by applying ADV-5B's own method to the reconciliation itself: scan the surfaces rather than trust the module. `src/lib/evidence.ts` defines `EvidencePassport` with field, value, source, period, geography, entity kind, unit, statistic, transformation, sufficiency, verification scope and correction history, plus `freshnessOf`, `confidenceOf`, `isKnown`, `publishability`, `latestCorrection` and `isRetracted`, behind 567 lines of test. Call sites of any of those six functions outside `src/lib/evidence*` number zero, and no code in `src` constructs a passport. The six modules importing `@/lib/evidence` import only its verification vocabulary. One level down `src/lib/provenance.ts` is in the same state: every `ASSET_FIELDS` entry declares the tier it is born at, the only runtime reader of that tier is `src/lib/attributeDisplay.ts:122`, which reads it to skip computed fields, and `ProvenanceChip.tsx`, the component that would draw it, is imported by nothing. The strategy's own line is that a citation chip is not enough; at HEAD there is no citation chip either. This is not the same class of defect as the dormant AI agent layer, which is unreached because a documented gate refuses to activate it while `AI_AGREEMENT_IN_FORCE` is false and no provider agreement is recorded: that is a refusal working as designed under owner ruling 7. The passport is unreached because nobody wired it, and nothing gates it. No permission, no vendor, no contract and no owner decision stands between here and a rendered passport, which is why the producer, the rendering and a structural gate that fails when a surface states a figure no passport reaches are the next package rather than a backlog item. FIXED, ADV-1C. The passport now has a producer, a renderer and a gate. `src/lib/evidenceView.ts` turns an `EvidencePassport` plus a source rights row into a `PublicEvidenceView`, which is the only shape a page is handed; `src/lib/listingEvidence.ts` turns a listing row into one passport per field; `src/components/EvidencePassport.tsx` renders it as a native disclosure, compact chip first and the eleven boundary 4 facts behind it, in both languages. It is mounted at the listing detail page's overview tiles and lease terms grid, which is the surface where a reader meets a figure rather than a page about figures. `src/lib/reachability.test.ts` is the structural half of the answer to how this was allowed to happen: it walks the import graph from every App Router root and fails when a component or producer named as production has no runtime consumer, so the specific shape of this finding cannot recur silently. `EvidencePassport.render.test.tsx` is the behavioural half, asserting against rendered HTML rather than against the object, because every test that existed while this finding was open passed. |
| 77 | P2 | Five components have no importer anywhere in the codebase, and two of them are the Evidence Passport's rendering surfaces | Closed | A sweep of all 57 files under `src/components` for a non-self importer anywhere in `src` returns five with none: `HeroSearch`, `LocationFilter`, `LocationScore`, `ProvenanceChip` and `RentBand`. That `ProvenanceChip` and `RentBand` are two of the five is not a coincidence but finding 76 seen from the component side. The cost is not tidiness. An orphan is how a retired pattern survives every sweep, because no gate that reads rendered output can see it: `RentBand.tsx` still carries a `badge-gold` class name, whose CSS rule was migrated to Harbor paint but kept its old name and whose only user is this orphan; an en dash range separator with no `<bdi dir="ltr">` isolation, which D20 requires under RTL; muted tiers at `/40`, `/45` and `/55`, two of which finding 75 measures at 2.93:1 and 3.96:1 against a 4.5:1 threshold; and a bare `toLocaleString()` with no locale argument, which is a Law 7 hazard the moment anything renders it. `.badge-verified` in `globals.css` has zero usages anywhere and is the same class of residue one level further out. `LocationScore` is already the second recorded exception in `claims.test.ts`, carrying a test that nothing imports it, which is the correct treatment and the pattern the rest should follow: each orphan wired, deleted, or excepted with a test that the reason for the exception is still true. DISPOSED, ADV-1C. Four of the five are deleted: `HeroSearch`, `LocationFilter`, `LocationScore` and `RentBand`. `RentBand` was deleted rather than repaired, on Codex boundary 8's instruction not to retain it merely because it exists; its `badge-gold` class name, its unisolated en dash range and its locale-free `toLocaleString()` were the argument for deletion rather than a repair list. `LocationScore`'s exception in `claims.test.ts` went with it. The fifth, `ProvenanceChip`, is integrated rather than removed: it is the compact indicator on every Evidence Passport and now has a real consumer on a rendered route. `.badge-verified` in `globals.css` went with the same sweep. What replaces the sweep as a standing answer is `reachability.test.ts`, so the next orphan is caught at the gate rather than by a hand audit two packages later. |
| 78 | P1 | No public inventory count excludes demo or simulated listings, so the sample-data label holds by accident of the data rather than by construction | Closed | Every public surface that publishes an inventory count queries `listings` on `status = 'published'` alone. A scan of all 28 published-listings query sites finds `is_demo` used in exactly two places, `src/app/[locale]/listings/page.tsx` inside `verifiedOnly()` and `src/app/[locale]/page.tsx` inside the verified-count chain, and in both it is one clause of the four-part VERIFIED test rather than a demo exclusion. So no count on `/listings`, `/locations`, `/market`, `/map` or the home page excludes demo rows; they agree at 88 today only because no `is_demo = true` row is currently published. Two consequences follow. First, `scripts/seed-demo.mjs` publishes rows with `is_demo = true` through the product's own front doors by design, so running it inflates every public count with nothing anywhere to exclude the rows again. Second and worse, `scripts/seed-world.mjs` inserts 24 rows at `status: "published"` and NEVER SETS `is_demo` AT ALL; its own header calls the `SIMW1-` reference prefix the sim tag, and no query in `src` reads that prefix, so its synthetic rows would enter the public count as ordinary inventory and would not be excludable even by the filter that does not yet exist. The rows are clearly synthetic at record level and invisible as synthetic at query level. This is the same defect class as finding 72: a figure whose integrity rests on the data happening to be clean rather than on a rule that keeps it clean. D31 rules the current 88 and 56 acceptable because the layout preview banner labels them and the route is `noindex`; this finding is the construction the label is missing behind it. FIXED, ADV-1C. `src/lib/inventory.ts` states the predicate once as `realInventoryOnly`, `is_demo IS NOT TRUE` rather than `= false` because the column is nullable and an equality test would drop every unflagged row and understate real inventory. Applied at 22 public query sites including `sitemap.ts`, the home count, the listings results and facets, locations, market, map, brokers, building, lister, compare, similar listings, the listings, search, advisor, shortlist and requirements APIs, and the saved-search match counts on `/me`. Six sites are excepted, each carrying a marker comment at the query and an entry with its reason in `SIMULATED_VISIBLE`: the two admin consoles, the shortlist and its route, the abuse report existence check and the requirement-interest ownership check. `scripts/seed-world.mjs` now sets `is_demo: true`, closing the latent half where 24 published rows carried only a `SIMW1-` prefix no query read. `src/lib/inventory.test.ts` is the structural gate, nine tests, registered in the explicit `npm test` list; mutation-checked by removing one filter and confirming the gate names the site. CORRECTED 2026-07-31. This record originally closed with the sentence that live counts were unchanged at 88 and 56, which is the regression evidence. That sentence was written before the deployment was read and it was false: the shipped filter took every public surface to zero spaces, because the exclusion it applied was unconditional and every row in the corpus carries the flag. Finding 79 records the regression and the repair, and the construction described above now stands in its release-scoped form, `releaseVisibleInventory`, with `realInventoryOnly` kept unconditional for the sitemap alone. The gate is now eleven tests rather than nine. |
| 79 | P1 | The finding 78 filter was unconditional, and since every listings row is flagged simulated it removed the entire corpus rather than a simulated slice of it, taking the live preview to zero spaces on every public surface | Closed | Shipped in `5654802` and found by reading the deployment rather than by any gate, which is the point of the finding. `dpl_GP89Yea3oznqd183c9JyptimkQ25` reached READY and `/en/listings` returned a 198k character page rendering zero spaces, the empty-results panel, and all 77 facet counts at zero, against a 650k character page rendering 88 spaces with facet counts up to 19 on the deployment before it. The home page inventory tile disappeared entirely. Districts at 77 and Buildings at 75 were byte identical across the two deployments, and those are the two counts whose queries were never wrapped, which is what proved the database was reachable and the wrap was the cause. ROOT CAUSE. D31 clause (b) asserted that zero rows carry `is_demo = true`, the fix was built on that reading, and the reading was false. `docs/ruling-3-4-closure.md` line 37 and finding 3 both say the same thing: 93 rows, every one `is_demo`, 88 published and all 88 among them. So `not(is_demo, is, true)` applied everywhere did not exclude simulated rows from a real corpus, it excluded the corpus. Both Supabase read tools are permission denied in this session, so the ruling was made from the repository audit documents plus the differential live evidence, which is the same standard owner ruling 3 sets. THE DEEPER ERROR IS THAT THE FIX CHOSE THE WRONG BRANCH OF ITS OWN INSTRUCTION. Codex boundary 2 requires that a simulated row be clearly presented as sample data in the private preview OR suppressed. The preview banner in `[locale]/layout.tsx` is the first branch and it was already shipped on every route in both languages, so the correct construction was never to hide the rows the banner exists to label. REPAIR. `src/lib/inventory.ts` now states one predicate, `simulatedRowsAreLabelled()`, read at call time rather than captured at module load. `releaseVisibleInventory` passes rows through while that predicate holds and excludes when it does not; `realInventoryOnly` keeps the unconditional form for `src/app/sitemap.ts` alone, because a sitemap entry carries no banner into a crawler's index, which is the outcome boundary 2 forbids by name. `[locale]/layout.tsx` imports the same predicate to decide whether to mount the banner instead of reading the environment itself, so the banner and the filter cannot drift: simulated inventory is visible exactly when something is telling the reader it is simulated, and never when nothing is. Sixteen surface files were repointed. THE GATE LESSON. `inventory.test.ts` was green on the tree that emptied the exchange, because a structural gate proves a rule is applied everywhere and cannot prove the rule is right. Two behavioural tests were added, one exercising both release states through a traced query builder and one asserting the layout holds no second copy of the condition, and both were mutation-checked by restoring the unconditional body and confirming the suite names the defect. Eleven tests. D31 clause (b) is corrected on the record and the finding 78 closing sentence, which predicted unchanged counts, is corrected with it. LIVE EVIDENCE, `15939ec` on `dpl_FXFv4bRu5eYi5dtDkWnqRWkrxaT1`, READY. `/en/listings` is 650,344 characters and renders 88 spaces, against 198k characters and zero on the deployment before it. `/en/listings?city=riyadh` renders 56. `/ar/listings` is 639,884 characters and renders 88 مساحة, and carries the Arabic preview banner. Counts appear twice in the fetched output because the page is served once as SSR markup and once in the RSC payload, so the rendered figure is 88 and 56 rather than 176 and 112. Gates on the shipped tree: typecheck 0, 1165 tests passing, ar-lint clean, prose scan 0. |
| 80 | P2 | A lister's own figures have no recorded export or model-input permission, so the passport reports two of its three permissions as unrecorded on every first-party figure | Confirmed open | Raised by building the permission block itself. `FIRST_PARTY_PERMISSIONS` in `src/lib/evidenceView.ts` sets display to permitted, because a lister who publishes a listing has plainly agreed it may be displayed, and sets export and AI use to `unknown`, because nothing in the lister terms covers either. The passport therefore prints `Not recorded` twice on every entered or verified figure, which is the honest reading and also a visibly incomplete one. It is not softened to permitted and not hardened to refused: an absent decision reported as either would be a decision nobody made, which is the exact failure this package exists to prevent. Closing it is an owner and terms question rather than an engineering one, and it should be closed before any bulk export feature or any retrieval path that would feed a lister's figures to a model. |
| 81 | P2 | Five library modules are unreachable from every route root, and three of them are held in the tree only by a test asserting that nothing imports them | Confirmed open | Found by the reachability walk built for boundary 7 rather than by the components sweep that produced finding 77, and it is the same defect one layer down. Walking imports from every App Router root plus `middleware.ts` leaves `src/lib/location/index.ts`, `src/lib/location/address.ts`, `src/lib/locationKind.ts` and two others outside the reachable set. The first three are the awkward case: they are not dead in the ordinary sense, because `claims.test.ts` imports them to assert the barrel's shape and the district-versus-development law, so deleting them deletes the tests that hold two rulings. They are reachable from the test graph and not from the product. That is a real state and not obviously a defect, but it is not currently recorded anywhere, and an unrecorded exception is how finding 77 happened. Left open deliberately rather than resolved inside ADV-1C, whose boundary 11 forecloses widening the package. |
| 82 | P2 | The laws scanner read `.ts` and `.tsx` source but not `.test.tsx`, so a law could be broken in a rendered-component test and no gate would say so | Closed | Found while registering `EvidencePassport.render.test.tsx`, the first `.test.tsx` file the repository has ever had. `src/lib/laws.test.ts` walks `src` and applies the law patterns, and its file filter admitted `.test.ts` but not `.test.tsx`, which had never mattered because no such file existed. The moment one did, an entire file of markup assertions sat outside the gate that enforces Harbor, the em dash rule and Western numerals. Fixed by widening the filter, which immediately required the scanner's own self-matching exemption to be widened with it, since the new file quotes law-bearing strings in order to assert them. That is the same self-matching hazard as findings 51 and 68 and it is now the third time it has appeared, which is the argument for the exemption set being a tested list rather than a filename pattern. |
| 83 | P2 | A correction reason filed in one language was printed unmarked in the other, so an English sentence appeared inside an Arabic record as though it were Arabic | Closed | Found by rendering the passport in both languages and reading the Arabic output rather than by any gate, because no gate can tell a wrong language from a right one. `CorrectionEntry.reason` was a bare string with no record of the language it was filed in, so the Arabic page printed the filed English sentence with no `lang` attribute: a screen reader pronounced it with Arabic phonology, and bidi resolved it against the page's base direction instead of its own. The obvious repair is a translation and it is forbidden: boundary 10 rules that missing evidence is never filled with generated wording, and the stated reason a figure changed is evidence, so a machine translation of it is a sentence nobody filed sitting in the record as though somebody had. FIXED by recording the language instead. `FiledReason` accepts either a bilingual pair, for a correction genuinely filed in both, or a single string with `reasonLang`; `readCorrectionReason` returns the filed words plus the tag a surface must put on them, and returns that tag only when the filed language is known and differs from the reader's. An unrecorded filing language stays unmarked, because asserting a language nobody told us is itself a claim, and a wrong `lang` attribute makes a screen reader read the sentence as noise, which is worse than leaving it unsaid. |
| 84 | P1 | The public evidence view carried the role of whoever filed each correction, so boundary 6's separation held only for as long as no page chose to print it | Closed | Found by reading the type rather than the output. `PublicEvidenceView.corrections` was `readonly CorrectionEntry[]`, passed through by spread, and `CorrectionEntry` carries `actorRole`. No surface rendered it, which is why nothing failed; the field was one line of JSX away from the public at all times, and it would have been anything a future `CorrectionEntry` gained as well. Boundary 6 is a rule about the shape of the object a page is handed, not about how careful the page turns out to be. FIXED with `PublicCorrection`, a type that has no such field, and a builder that rebuilds the array field by field rather than spreading, so a new internal field on `CorrectionEntry` cannot travel outward by default. The test asserts absence with `!("actorRole" in ...)` and a serialisation check rather than asserting that it is not rendered, because not rendered is a property of today's markup and not present is a property of the type. |
| 85 | P2 | The passport's collapsed line was 180px of unbreakable pills inside a tile that narrows to 103px, so the compact indicator hung out of its own card at four of six widths | Closed | Found by the responsive probe, and only after the probe fragment was corrected. The first version reproduced one tile in the listing page's `repeat(auto-fit,minmax(150px,1fr))` grid, and `auto-fit` collapses empty tracks, so a single item stretched across the whole 800px column and the panel measured 740px wide, which is a width it is never drawn at. With three sibling tiles in the row the true widths appear: 238px at 320, where the two-column page grid has already collapsed, and 103 to 154px at 390, 430, 768 and 1280, where it has not and the tile grid splits two and four ways. The narrowest case is a phone at 390 and the second narrowest is a tablet, which is the opposite of the order this kind of defect is usually looked for in. Against those widths the verified chip was 180px and `.evi-state` up to 166px, both `white-space:nowrap`, overflowing by up to 51px. FIXED in three parts. The chip no longer repeats the date, which the panel already carries on its freshness row and per verification dimension, and which boundary 5 calls overloading the compact line. `ProvenanceChip` gained an opt-in `wrap` prop rather than the caller defeating its inline style with `!important` from a stylesheet, because how wide the chip sits is the chip's business and a rule reaching in to override it stops working the day the inline style moves. `.evi-state` wraps rather than hanging. A rendered-HTML test asserts the cause, since the probe is not in `npm test` and a `nowrap` restored by hand would otherwise ship unseen. |
| 86 | P2 | The platform's 44px touch-target rule is a hand-enumerated list of selectors, so every new interactive control ships short until somebody happens to read the stylesheet | Closed | Found while writing ADV-1C's accessibility section rather than by a gate, which is the finding. `globals.css` line 260 opens an `@media(max-width:1024px)` block headed against WCAG 2.5.5, and inside it every control is named by hand: `.chip`, `.tag`, `.tabs .t`, `.btn`, `.btn.sm`, then `.icon-btn`, `.iconbtn`, `button.icononly`, `.fab` and `.vtool span`. The block's own comment records that the `.vtool` controls were "the one set the earlier pass missed, so they stayed 36px on touch", which is the same defect being described here one pass earlier. `.evi>summary` is the passport's disclosure and the third instance. It is focusable, keyboard-operable, carries `cursor:pointer`, and tapping it is the only way to reach the evidence, so it is a control by the platform's own test; its base rule set `min-height:40px` and it was in none of the lists above, so it shipped four pixels short on every width the platform is tested at. FIXED with one line, `.evi>summary{min-height:44px;}` inside the same block, and guarded by a test that reads `src/styles/globals.css` rather than the rendered markup: the height is not in the HTML at all, so a render assertion would have passed while the control was 40px on every phone. The test locates the media block by its literal selector, asserts the touch floor is at least 44, then asserts the base rule still declares at least 40 for pointer devices, reading the remainder of the file rather than the head because the base `.evi` rules are declared at line 448, below the media block and not above it. NOT FIXED, deliberately: the general form. A gate over every `cursor:pointer` selector was scoped and rejected under boundary 11. The uncovered set is `.asset-arrow` at 34px in `globals.css`, and `.dnav a`, `table.dt tbody tr:hover` and `.seg button` in `sat-platform.css`; a gate would need a written exemption for a dashboard nav link, a hover-only table row and a segmented control before it could be green, which is a platform-wide change and belongs with finding 75's contrast gate in the parked visual-quality package. The class is registered here instead of being quietly repaired for a third time. CORRECTION: the FIXED sentence above was written before the outcome was measured and was false. The one-line fix was inert and the guard passed anyway. See finding 87. |
| 87 | P2 | The finding 86 fix was defeated by its own stylesheet's source order, and the test written to guard it asserted that two numbers existed rather than which one wins, so it passed while the control stayed 40px | Closed | Found by fetching the deployed CSS rather than by any gate, which is the second time in this package that reading the deployment is what produced the finding. Byte offsets in the built stylesheet: `.evi>summary{min-height:44px}` at 39789, inside the `@media(max-width:1024px)` block that opens at 39539, and `.evi>summary{display:flex;...;min-height:40px;padding-block:4px}` at 49771. Both selectors are `.evi>summary`, specificity (0,1,1). A media query contributes nothing to specificity, so at equal specificity the later declaration wins, and the 40px base rule ten thousand bytes further down beat the 44px floor at every width the query matched. The disclosure was still 40px on 320, 360, 390 and 430. SECOND MECHANISM, ALSO INERT: `sat-platform.css` already carries `@media (pointer:coarse){.btn,.chip,...,summary{min-height:44px}}`, which looks like a catch-all that should have covered this control all along. It does not. A bare `summary` element selector is (0,0,1) and loses to any class-qualified rule regardless of file order, so `.evi>summary` defeated it too. Neither mechanism ever protected the passport. WHY THE GUARD MISSED IT. The finding 86 test was written precisely because the height is not in the markup, so a render assertion would pass while the control was short; the assertion moved one level up, to the stylesheet, and still did not measure the outcome. It located the media block, asserted a 44 inside it, then asserted a 40 or better below it, and both were true of an arrangement in which the 40 won. Presence is not resolution. FIXED. The floor is declared after the base `.evi` rules under the marker `evi-touch-floor`, and the entry in the enumerated tap-target block is replaced by a comment saying why the rule cannot live there. The guard is rewritten as a scanner that strips comments, walks the file tracking open at-rules, collects every `min-height` declared for `.evi>summary` with the media conditions enclosing it, then for each of 320, 360, 390, 430, 768 and 1024 takes the LAST applying declaration and asserts it is at least 44, with a separate desktop assertion at 1280 that a 40px floor still holds. Mutation-checked by restoring the inert arrangement: the test fails with "the disclosure resolves to 40px at 320px wide, under the platform's 44px", which names the defect rather than merely going red. The comment stripping is not incidental: the new CSS comment quotes a rule with braces in it, and a brace counter cannot tell a quoted rule from a real one. |
| 88 | P1 | The source register loader collapsed four different outcomes into one empty map, so a register the public runtime cannot read was reported to the advisor as a register that is empty | Closed | Found by Codex correction 2, which asked us to reconcile the ADV-1C handback saying the source register is empty with earlier packages using `source_registry.rega_ejar`. The reconciliation is that the handback was wrong, and the reason it was wrong is structural rather than clerical. `src/lib/queries/sourceRights.ts` returned a bare `Map`, and four materially different situations produced the same empty one: the deployment has no Supabase credentials; the query reached PostgREST and returned an error; the query succeeded and this reader was permitted to see no rows; the register genuinely holds nothing. `/sources` rendered all four as one sentence, The register could not be read, and that sentence is what the handback paraphrased. It is finding 87's lesson in a different medium and Codex correction 5's defect one layer up: a single generic unavailable state standing in for several different facts, except that here the cost was not a confused reader but a false statement to the advisor. WHAT THE EVIDENCE ACTUALLY SAYS. `supabase/migrations/20260728_source_rights_ledger.sql` writes nine reviewed rows into `public.source_registry` and its own header records that it was applied to production. On the same deployed preview and through the same anon `getSupabaseServer()` client, `/en/rent-index` reads `rent_index_published` and returns seven rows, while `/en/sources` returns none and renders no source id anywhere in its payload. So Supabase is configured, reachable and readable, and `source_registry` specifically is not readable by anon. No committed migration enables RLS on that table or grants anon SELECT, and RLS with no SELECT policy returns exactly this shape: HTTP 200, no error, zero rows. Both Supabase read tools are permission denied in this session, so this is stated as the leading explanation supported by differential live evidence rather than as a confirmed server configuration; the owner can settle it with one `pg_policies` query. FIXED. `readSourceRegister()` returns a discriminated `SourceRegisterState` of `not_configured`, `read_failed`, `no_rows_visible` or `loaded`, and `/sources` states which of them happened in the reader's language, with six dictionary keys in both languages replacing the one collapsed pair. It does not guess between holds no rows and shows this reader no rows, because a PostgREST 200 with an empty body cannot tell those apart. THE BEHAVIOUR IS DELIBERATELY UNCHANGED. All four states carry an empty rights map and therefore deny every lookup, and `src/lib/adv4b.test.ts` asserts that each non-loaded state returns through the one denying constructor and that exactly one path reports `loaded`. The state is reported and never acted on: an unreadable register and an empty one must deny identically, because the alternative is publishing on the strength of a silence. The false claim in `src/lib/evidenceView.ts`, `docs/handback-adv-1c.md` and `docs/roadmap.md` is corrected in place rather than deleted, so the record shows what was said and why it was wrong. |
| 89 | P1 | A second copy of the licence rule lived inside the evidence engine, and the copy ignored the status ceiling, so the one source on the platform would have been authorised for public redisplay the moment its rights row became readable | Closed | Found while wiring the Rent Index passport, by reading the two modules side by side rather than by any gate. `src/lib/sourceRights.ts` is the module that owns permissions and its rule has two inputs: the policy column for the use in question, and `rights_status`, which places a downward ceiling on every column in the row. `evidenced` ceils at `public`, `asserted_unverified` at `internal`, `unknown` and `prohibited` at `none`. `src/lib/evidence.ts` carried its own three-line `permits(policy, audience)` that read the policy column and nothing else. It was not a paraphrase of the rule, it was a different rule, and the difference was the entire reason the status column exists. THE LIVE CASE IS NOT HYPOTHETICAL. `supabase/migrations/20260728_source_rights_ledger.sql` sets `rega_ejar` to `rights_status = 'asserted_unverified'` with `stop_condition` O10 unresolved, and deliberately does not set `redisplay_policy`, which therefore retains its pre-existing `public`. `mayRedisplay` answers no to a public audience for that row. The local copy answered yes. It has not shipped a wrong figure only because finding 88 means the public runtime reads zero rows from `source_registry`, so `publishability` never reached the rights branch with a row in hand; the defect was one readable row away from publishing a REGA figure whose own stop condition says the permitted-use language has not been read. THE SAME DEFECT HAD A DISPLAY HALF. `src/lib/evidenceView.ts` reported `permissions.display` as the raw `redisplayPolicy` column, so the panel would have printed Permitted beside a value that `publishability` had withheld for exactly that reason, and because the state set keys off the displayed permission, `restricted` would not have appeared either. Two statements drawn from one row, contradicting each other, is worse than either being wrong alone: it tells the reader the withholding is a bug. FIXED. The local `permits` is deleted and `evidence.ts` calls `mayRedisplay` or `mayDisplayDerived` depending on whether the transformation left the figure as the source published it. `sourceRights.ts` gained one exported `effectivePolicy(row, use)`, which is the same computation the enforcement path runs, and `evidenceView.ts` reports that rather than the column, so what is shown and what is applied come from one place. The header of `evidence.ts` already stated the rule it was breaking: it does not restate rights, it asks the module that owns them. |
| 90 | P1 | The Advisor prints its published-band figure in prose whatever the passport decides, so the passport can only ever be absent and never contradicting, and the withheld case is silent rather than stated | Closed | ESCALATED FROM P2 BY CODEX, who ruled it "a release-blocking truth and rights defect" and directed that it not be left open merely because O10 is external. The escalation is correct and the original P2 was wrong for a specific reason: the register recorded the defect as a question about which surface to change, and it was never that. It was a figure being published without publication permission, which is the same class of defect whatever surface it appears on. Registered deliberately during ADV-1D rather than fixed, because fixing it in this package would have made one surface stricter than the other and neither is right on its own. Codex correction 4 bounds what may be attached: no passport rides on an Advisor answer unless the figure is completely traceable through an authorized typed tool result. That boundary is enforced by dropping every view whose `value` the licence withheld, which is correct as far as it goes and leaves exactly one gap. `renderValue` composes the sentence from the row, not from the passport, so when the licence withholds, the reader still sees the number and simply sees no evidence beside it. That is the behaviour the Advisor had before ADV-1D, so nothing regressed, and it is also the behaviour the Rent Index district table still has at `src/app/[locale]/rent-index/page.tsx`, where `{d.figure}` and `{d.band}` are printed off the record. Only the evidence card on that page was made coherent in this package, by printing `avg.value ?? ri.na` so one object decides both the number and the panel. THE OPEN QUESTION IS NOT WHICH SURFACE TO CHANGE BUT WHICH RULE IS RIGHT. Gating the prose on the passport would mean a published, sufficient row whose source rights are merely unrecorded stops being quotable anywhere on the platform, which is a materially different product than the one shipped and touches the Rent Index table, the Advisor, the listing benchmark lines and every fixture that lacks the record-class markers. Leaving it means the platform continues to state figures whose evidence it will not show, which is defensible only while the global preview disclosure and `noindex, nofollow` hold and the launch gate stays closed. RESOLUTION IS NOT PURELY ENGINEERING: it depends on O10, because the moment `rega_ejar` carries a recorded permitted-use position the withheld case largely disappears for the one source the platform has, and the remaining cases are then genuinely about unrecorded rights rather than about an unreadable register. Recorded here so the choice is made once, on the record, rather than settled differently by each surface as it is built. FIXED IN ADV-1E, AND THE OPEN QUESTION ABOVE IS ANSWERED RATHER THAN DEFERRED. The rule that is right is the strict one, and the reason the choice looked hard is that the entry framed it as a trade between two surfaces when it is a trade between a figure and a permission. `decidePublicQuote` in `src/lib/publicQuote.ts` is now the one decision, taking data class, demo status, source-rights status, permitted display, environment and any recorded stop condition, and returning `authorized_public`, `labelled_sample`, `withheld` or `unavailable`. `renderValue` no longer composes the sentence from the row: the Advisor's prose and the passport beside it are produced from the same call, so the prose and the passport cannot disagree, which was the literal defect. The withheld case is now stated rather than silent, in both languages, and the figure is absent from the sentence rather than stranded beside an empty panel. THE COST THE ENTRY PREDICTED WAS REAL AND WAS PAID. A published, sufficient row whose source rights are unrecorded is indeed no longer quotable anywhere, and the Rent Index district table, the Advisor and the listing benchmark lines all went through the same decision, fourteen readers of `rent_index_published` in total. That is a materially different product than the one ADV-1D shipped and it is the truthful one: statistical sufficiency is not publication permission, and the entry's own defence of the loose rule rested on `noindex` and a preview banner, which Codex item 2 rules out as authorization by name. The synthetic rows that lost their unqualified figures did not go blank; they carry `SAMPLE_STATEMENT`, "Sample data for product testing. Not a published market figure." and its gate-passing Arabic twin, which travels into the prose with the number rather than sitting in a page banner. THE O10 DEPENDENCY IS UNCHANGED AND NO LONGER BLOCKS THIS ENTRY. O10 still gates whether a REGA figure may ever be published, and it is now recorded precisely, as ten clauses in `docs/regulatory-register.md` and as data in `src/lib/sources/o10.ts`, with `O10_RECORDS` empty under owner ruling 7. What ADV-1E changes is that the platform's behaviour while O10 is unresolved is now correct by construction rather than by each surface's discretion. Proven by 30 gates in `src/lib/adv1e.test.ts` and 9 in `src/lib/sources/o10.test.ts`, including that `published` and `sufficient` cannot override withheld rights, that the `X-Robots-Tag` header is not read as display authorization, that no unauthorized figure reaches an API or rendered payload, and that English and Arabic expose identical figures and evidence states. |
| 91 | P1 | ADV-1E made the figure decision canonical but left the source NAME ungoverned, so the Advisor introduced a synthetic figure as REGA market evidence in the same response whose passport called it sample data | Closed | Found by Codex item 7's live end to end run, not by any gate, which is the finding's second lesson. On `fd5aebc` the deployed Advisor answered "Price a Grade A office in Al Olaya" with prose ending "Source: REGA Rental Index (Ejar), average of registered rental contracts." while the Evidence Passport beside it, describing the same number, read "Source: Sample data for product testing. Not a published market figure." with `source: null` and a gate verdict of `labelled_sample`; the API payload for the same request shipped `band.source = "REGA Rental Index (Ejar)"`. Root cause: `buildValueEvidence` defaulted a null `source` column to the literal authority name and `renderValue` printed it on every branch, so the attribution was composed downstream of the decision that had already refused it. This broke Codex item 5 (prose and passport must never disagree), item 3 (synthetic data must not be described as REGA market evidence) and item 1 (one decision for rendering, APIs, prose, passports and machine-readable output) at once. Fixed by removing source naming from the composers rather than parameterising it: `renderValue` and `analyseDeal` no longer name any source, `rentIndexQuoteGate` gained `sourceText` and `proseSource` decided beside the figure, `advisorQuoteMessage` appends the one clause, and `toPublicSegment`'s second argument is REQUIRED so the compiler visits every call site and no payload can serialise the stored column again. A SECOND hole was found while fixing the first and is recorded as part of the same finding: the gate consulted `decidePublicQuote` but not `publishability`, so a passport that had already emptied its own value still authorised prose to print one; `mayShowFigure` is now `leadAllows && lead.value !== null`, degrading to `withheld` rather than `unavailable` because the row does hold a figure the public may not have. Guarded by `src/lib/finding91.test.ts` (8 tests over five record states x two locales) plus rewritten assertions in `attribution.test.ts`, `analyser.test.ts` and `laws.test.ts`; the composer scan reads code with comments stripped, matches banned identifiers on word boundaries so `REGA_RENT_INDEX_SOURCE_ID` is not a false positive, and exempts the Advisor system prompt per LINE rather than per file. KNOWN REMAINING DORMANT SCOPE: `makeRentBandTool` in `src/lib/agents/tools.ts` still appends the attribution to a band sentence with no gate. It is referenced only from `agents.test.ts` and `tools.test.ts`, reaches no route, and is recorded here rather than silently fixed so the next package that activates the agent tools cannot activate it unnoticed. |
| 92 | P2 | The `district_label` and `district_label_ar` columns on the rent index rows carry the same other-language borrow at twenty-seven sites in eighteen files, and it cannot be fixed the way finding 66 was fixed | Confirmed open | Found by PKG-NM1's source guard, which reported these files before its regex was narrowed, and kept as a finding rather than absorbed because the substance differs. `placeName` widens a district we cannot name in the reader's language to its city, and that is safe for OUR description of a place. `district_label` is not our description of a place: it names the geography that a published third-party statistic was measured over. Widening it would restate a band measured in one district as a band measured across a city, which is a false statement about someone else's figure rather than a kinder rendering of our own, and it would do so on the Rent Index surfaces where the REGA Rental Index (Ejar) attribution is carried. Shape, sampled: `districtAr || row.district_label_ar || dist` in `market/verdict.ts:157`, `ar ? (r.district_label_ar || r.district_label) : r.district_label` in `rent-index/page.tsx:139`, `String(req.district_label_ar ?? "").trim() || label_en` in `matching.ts:251`. Files: `[locale]/page.tsx`, `building/[id]/page.tsx` (4), `market/page.tsx`, `map/page.tsx`, `listings/page.tsx`, `advisor/page.tsx`, `rent-index/page.tsx`, `compare/page.tsx`, `listings/[id]/flyer/page.tsx`, `api/advisor/route.ts`, `api/advisor/shortlist/route.ts`, `api/index/segments/route.ts`, `components/WatchBanner.tsx`, `lib/useAdvisorChat.ts`, `lib/matching.ts`, `lib/market/verdict.ts`, `lib/market/valueEvidence.ts`, `lib/market/underwrite.ts`. The correct fix is upstream: hold the label in both languages at ingestion, from the source's own geography, or state in the reader's language that the label is the source's and reproduce it as written. That is a data and rights question, not a rendering one, so it is deliberately not attempted here and is named in both `displayName.ts` and the guard comment as out of scope. |
| 93 | P2 | A lister's free-prose `about` paragraph falls back to the other language, so an Arabic reader of a lister profile can be handed an English paragraph presented as that lister's own words | Confirmed open | Found in the same sweep as finding 66's second half and separated from it because prose is neither a description we own nor an identifier. `src/app/[locale]/lister/[id]/page.tsx:91` reads `const about = (ar ? p.about_ar : p.about_en) || p.about_en || p.about_ar || ""`. `entityName`'s reasoning does not transfer: a registered name in the wrong script is still recognisable and still short, whereas a paragraph in the wrong language is unreadable to the reader it is shown to and, unlike a title, has no generic honest substitute composed from fields we hold. The three candidate answers, none of which is obviously right, are to show nothing, to show the other language's paragraph under an explicit label naming the language it is in, or to prompt the lister the way PKG-NM1 prompts them about a missing title. Deferred to whichever package next touches the lister profile surface. |
| 94 | P2 | `ar_translation_status` is one value for a whole listing, so it cannot say who wrote the Arabic title as distinct from who wrote the Arabic description | Confirmed open | Raised by PKG-LS1 while giving the lister's edit form its Arabic title and description fields. The column is `NOT NULL` with `CHECK (ar_translation_status = ANY (ARRAY['pending','machine','in_review','approved']))` and is written only by `/api/listings/[id]/translate`, as `machine`. A listing drafted in the Studio, machine-translated, then given a lister-written Arabic description from the dashboard now holds lister Arabic on a row that still says `machine`, and the opposite case is equally reachable. The hash cannot separate them either: `PATCH /api/listings/[id]` stamps `title_ar_src_hash` for lister-written Arabic exactly as the translator would, and correctly so, because the stamp exists to stop a later translate run overwriting Arabic that answers the current English. PKG-LS1 therefore asserts nothing about authorship anywhere: `arabicState` in `src/lib/listingArabic.ts` answers only whether the Arabic is behind the English, which is true regardless of who typed either sentence. Nothing in the platform renders `ar_translation_status` today, so this is a record defect rather than a display defect, and no reader is currently told anything false. It becomes a display defect the moment any surface prints a translation marker. The fix is per-field provenance, which is a schema change: one authorship value per translatable field rather than one per row. Measured against the live corpus at closure, on `dpl_ArzX4TDegaDXKPhSQyEYrm7Hrq8T`, this is not hypothetical: of the fifty published rows returned by `/api/listings`, seventeen carry a `title_ar_src_hash` and an Arabic title while `ar_translation_status` still reads `pending` and `ar_translation_model` is null. Whatever stamped the hash on those rows did not write the status, so the column already fails to describe thirty-four per cent of the published corpus. The remaining rows split twenty-three `machine` with a model recorded, nine `pending` with Arabic and no hash, and one `pending` with no Arabic. |
| 95 | P1 | The lister's edit form labelled its two fields in the reader's interface language while both wrote the English columns, so an Arabic-reading lister was invited to type Arabic into `title_en` | Closed in PKG-LS1 | `src/components/EditListingForm.tsx` had `title: "العنوان"` over an input bound to `f.title_en`, and the same for the description. The form had no Arabic field at all, while `PATCH /api/listings/[id]` had accepted `title_ar` and `description_ar` behind the same `mayEdit` check since it was written, and `ALWAYS_EDITABLE` already listed both. The screen therefore carried the PKG-NM1 notice telling a lister that a reader in the other language is shown a generic description of their space, on the one page whose form could not answer it, and a Studio-created listing could hold Arabic its owner could neither read nor change. Closed by giving the form all four fields under labels that name their language in the Studio's exact wording, with `dir` and `lang` on each input set from the text it holds rather than from the interface around it. The guard in `src/lib/listingArabic.test.ts` scans every file that calls the listings API and fails any one that names a listing title in one language only, so the class cannot return in a new writer. |
| 96 | P2 | Not one published listing has an Arabic description, so an Arabic reader is given a title and then nothing, on every space in the exchange | Confirmed open | Measured, not inferred. `/api/listings` on `dpl_ArzX4TDegaDXKPhSQyEYrm7Hrq8T` returns fifty published rows and `description_ar` is empty on all fifty, while forty-nine of the fifty do have an Arabic title. The Arabic reader therefore reaches a listing page, reads its name, and finds the paragraph that carries access, condition and terms either missing or in English. The three write paths all exist: `ListingStudio` has had the field since it was built, `PATCH /api/listings/[id]` has accepted `description_ar` behind `mayEdit` throughout, and PKG-LS1 has now put it on the lister's edit form as well. So this is a corpus fact rather than a missing capability, and it is not closed by more engineering on the form. Closing it means either listers writing the paragraph, which the edit form now makes possible for the first time, or a translate run over the description with the result marked as a translation. Recorded here because the count is the honest measure of what the Arabic half of the market currently reads, and because a package that shipped the field should not leave the impression the content followed it. |
| 97 | P2 | `PATCH /api/listings/[id]` re-stamps the Arabic source hash whenever the body carries the Arabic, so any client that posts every field on every save silently declares the Arabic current on English the lister just rewrote | Confirmed open | Found while closing PKG-LS1, by asking what the new fields would do rather than by testing that they appeared. The route sets `patch.title_ar_src_hash = ta && en ? hashSource(en) : null` inside `if (can("title_ar") && typeof body.title_ar === "string")`, so the stamp is a function of the request shape, not of whether the Arabic changed. The stamp means "this Arabic was written against this English", and it is read by `/api/listings/[id]/translate` to decide what to refresh and by `arabicState` to decide what the lister is told. A form posting all fields unconditionally would therefore have made `stale` unreachable from the very screen PKG-LS1 built it for, permanently exempted the row from any later translate run, and shown the lister `current` on Arabic describing a space in terms their own English no longer uses. PKG-LS1 answers it client-side with `changedArabic`, which sends an Arabic field only when its trimmed value differs from what was loaded, with four tests including the clearing case. That is a real fix for the two writers that exist, and it is discipline rather than a boundary: the next writer that posts a whole form gets the old behaviour back with nothing to stop it. The durable fix belongs in the route, which has the stored row in hand at that point and can compare the incoming Arabic against it before stamping. Left open deliberately rather than closed as fixed. |
| 98 | P1 | A CSS custom property that nothing declares fails silently, so a rule referencing one is dropped whole and the element renders with no border, no colour and no error | Closed in PKG-SUP1 | Found by looking, which is the finding's point: nothing in this repository could have caught it. The new `/list` used `var(--line)` for its stage separators; a grep of `src/styles` for `--line` returned nothing, and the same sweep found the property already shipped on the owner-documents card of `src/app/[locale]/listings/[id]/page.tsx:554`, where owners had been reading a card whose border never drew. The cascade treats a declaration containing an unresolvable `var()` as invalid at computed-value time and discards the entire declaration, so the failure is invisible to typecheck, to the test suite, to the build and to the console. Both sites now use `var(--silver)`, the divider colour the rest of the platform uses. The durable part is `src/lib/cssVars.test.ts`, which scans every non-test `.ts`, `.tsx` and `.css` file under `src` and fails any reference with no fallback to a property nothing declares. It reads the declaration set rather than holding a list: the platform tokens from `src/styles/*.css`, and the four `next/font` families from the `variable:` options in `src/app/layout.tsx`, which are injected onto the `html` element at runtime and appear in no stylesheet. `var(--x, fallback)` is deliberately not flagged, because the fallback keeps the declaration valid, which is why `AdPermit.tsx:73` is correct. The guard has its own sensitivity test over all four judgements. |
| 99 | P2 | The active locale's entire dictionary is serialised into every page's payload, so every route ships the copy deck of every other route | Confirmed open | Measured on `dpl_2mGUZD7s8ZdTBnMa2ZGQNkmiCfj7` while verifying `/list`. `/en/list` returns 165,422 characters, of which 141,202 are script and 2,906 are visible text. The English dictionary file is 93,111 bytes and the Arabic 125,794, and the payload carries the whole block for the active locale, including the `invest`, `hbu`, `area`, `messages` and `advisor` sample strings, on a page that renders none of them. It is a payload and coupling fact rather than a truth defect: only the active locale ships, zero Arabic characters appear in the English payload, and the sample values carry their own sample labelling on the routes that render them. It is registered rather than absorbed into PKG-SUP1 because narrowing it means changing how every page obtains its dictionary, which is a package with its own regression surface across every route, not a line in this one. |
| 100 | P1 | The public demand form ships pre-selected on a value its own API rejects, so an English visitor who does not touch the move-in control is refused and an Arabic visitor is refused whatever they choose | Closed in PKG-DEM1 | Measured live on `dpl_2mGUZD7s8ZdTBnMa2ZGQNkmiCfj7`, not inferred from the source. The served HTML of `/en/post-requirement` carries `checked=""` on `value="1-3 months"` with the en dash, and `/ar/post-requirement` on `value="1-3 اشهر"`. `POST /api/requirements` validates `timeline` against `["ASAP","Q1","Q2","Q3","Q4","Flexible","Immediate"]` and returns 400 `Choose a valid timeline.` for anything else, so two of the four English options and four of the four Arabic options are refused, and the refused set contains the default. The live board corroborates it: `GET /api/requirements` returns six rows with `sample: false` whose timelines are null, Q3, ASAP, Q4, Q3 and Q4, and not one is a value this form can produce. The failure runs one layer deeper than the 400. `matchListing` treats a timeline as urgent only when it folds to `asap` or `immediate`, which is the branch that makes a listing's dated availability affirmation part of the match, so the Arabic word for immediate is dropped by the route and would be dropped by the matcher too. Nothing failed because no test submits this form: the form's vocabulary and the route's accepted set are two independent literals in two files. Scoped into PKG-DEM1 with a shared vocabulary both sides import, rather than by widening the route's validation, which would fix the wrong side and admit a token the matcher cannot read. Closed there. `src/lib/requirementIntake.ts` holds the token, both labels and the urgency flag; the form renders from it, the route validates against it and `matching.ts` derives its urgent set from it. Nothing is pre-selected, because the column is nullable and an unstated move-in date is a real answer rather than missing data. `src/lib/requirementIntake.test.tsx` reads the `value` attributes the rendered form emits, in both languages, and tests each against the validator's own predicate, so the two lists are now put side by side on every commit; it also carries the sensitivity case, which asserts that the eight literals the shipped form held still fail that predicate. |
| 101 | P1 | The demand form held five district ids as literals, all of them in Riyadh, and sent `city: "Riyadh"` whatever was chosen, so a tenant outside Riyadh could not post a requirement for their own city | Closed in PKG-DEM1 | Found while scoping PKG-DEM1 and confirmed against the deployment: the districts source holds 77 locations across 21 cities, and the form offered five, hardcoded with their UUIDs, with names that had drifted from the source. It said "كافد" where the board says "واجهة الرياض المالية". The `city` field in the submitted body was the literal "Riyadh" regardless of the choice, so a Jeddah brief was stored against a Riyadh district and a Riyadh city, and both facts were invented by the form. `/post-requirement` is now a server page that reads `districts` and passes the rows to the client form, which groups them by the canonical city key through `cityLabel`, the same function the locations directory uses. The city is no longer sent as a fact at all: the write path derives it from the district row, which is the only place it is known. |
| 102 | P2 | A requirement cannot represent more than one location, and the form that offered multi-select sent only the first to a structured field | Confirmed open | The record holds one `district_id`. The form that shipped let a visitor select several locations, sent the first as `district_id` and appended the rest to `notes` as an English prose sentence no matcher reads, while the match count on the success card was computed for the first location alone. So a visitor who named three districts was shown a count for one of them and told the market had been informed of all three. PKG-DEM1 stopped the misdescription by offering one location and saying so in the control's own note, which is the honest shape of what the storage can hold, rather than half building the fix. What a real fix needs is recorded here so it is scoped rather than rediscovered: a `requirement_districts` join table, a change to the `create_requirement` RPC so the brief and its locations are inserted in one transaction, a matcher that scores a listing against the set rather than the column, and a board and detail page that name several locations without implying a ranking between them. That is a supervised schema change and is not attempted from a form package. |
| 103 | P2 | A quarter timeline carries no year, so `Q1` submitted in the second half of a year is ambiguous by one year and the column holds nothing to disambiguate it | Confirmed open | Surfaced by writing the vocabulary down. `Q1` through `Q4` are stored tokens and the `timeline` column is a text column with no companion year, so a requirement posted in July 2026 asking for Q1 may mean January 2026, which is past, or January 2027. The matcher does not read the quarters at all, since only `Immediate` and `ASAP` make availability part of the question, so nothing is currently scored wrongly; what is wrong is what a lister reads on the board. Left open rather than papered over, because the two available fixes both cost something real: inventing a year the storage cannot hold would be a fabricated figure, and replacing the quarters with a date column is a schema change plus a migration of the rows that already carry them. Recorded with the vocabulary so the next person meets the constraint before designing around it. |
| 104 | P1 | The Arabic city vocabulary named six of the twenty one cities the districts source holds, so fifteen cities were shown to an Arabic reader in Latin script | Closed in PKG-DEM1 | Found by enumerating the deployment's own locations rather than by reading the table: the `/locations` page embeds a JsonLd `ItemList` whose entries name each place and its city, and grouping them yielded 21 distinct cities against a `CITY` map that held six. `cityLabel` falls back to `prettifyKey`, which title-cases the token and leaves Latin script intact, so an Arabic reader of a Dammam or Tabuk location was shown "Dammam" inside an Arabic sentence. The map now holds all 21 with Arabic names, and `CITY_ALIAS` gained twelve entries so a second spelling of one city cannot become a second group. `labels.test.ts` asserts that every city the source holds resolves in both languages, and carries the sensitivity case for the guard itself. |
| 105 | P2 | `POST /api/requirements` validated the district id by shape, so a well formed UUID that names no district was stored and the board then fell back to the free text city | Closed in PKG-DEM1 | The check was `/^[0-9a-f-]{36}$/i`, which is a claim about the string and not about the district. A brief stored against a district that does not exist reads as located on the board through the city fallback, and is invisible to district matching forever, so the failure is silent on both sides. The route now looks the district up and returns 400 for one it cannot find, which is also what makes the city derivation of finding 101 safe: the city comes from the row, and a request with neither a real district nor a recognised city is refused rather than filed in a city it never named. |
| 106 | P2 | The segmented control had no rule for the element it is actually built from, so the demand form's transaction and move-in controls carried the selected state in the DOM and never showed it on screen | Closed in PKG-DEM1 | `sat-platform.css` styled `.seg span` and `.seg button`, and the one place the control wraps real radios uses `<label>`, because that is what a radio's own text has to be. So `class="on"` was present on the chosen option and looked exactly like the unchosen ones, in both languages at every width, and the focus ring was drawn on a visually hidden radio, which is not a focus ring. Added `.seg label`, `.seg label.on` and `.seg label:has(input:focus-visible)`, deliberately identical to the `span` rule so a reader who has met one segmented control has met all three, plus the label in the coarse pointer touch target rule. This is the class of defect finding 98 was about: nothing in the repository could have caught it, because a missing rule is not an error. |
| 107 | P2 | The requirements board named asset types from a private map holding seven Arabic entries and zero English ones, so an English reader was shown the title cased token and an Arabic reader saw the raw token on any type the map had missed | Closed in PKG-DEM1 | `ASSET_L` was declared inside the board component as `ar ? {...} : {}`, so the English branch was an empty object and every English card fell through to the raw `asset_type`. That is why the board said "Retail" where every other surface says "Retail & F&B". The map has been deleted and the board calls `assetLabel`, which is where the platform names an asset type. The same call now renders the form's asset chips, so the demand side names a type once. |
| 108 | P2 | The requirements board and the requirement detail page printed the stored timeline and must-have tokens directly, so an Arabic reader was shown English tokens on every card | Closed in PKG-DEM1 | The read side of finding 100. `Q3` is readable enough in English to have gone unnoticed for as long as it did; `fitted` beside `مجهّز` on an Arabic card is not, and neither is the raw token an Arabic reader sees for a brief posted in English. Both fields now go through `timelineLabel` and `mustHaveLabel`, the same vocabulary the form renders from and the write path validates against. Both fall back to the stored value rather than to a placeholder, because a row written before this vocabulary existed carries its own words and those are worth more than "unknown". AMENDED BY FINDING 113: that fallback was measured against the live corpus after the ship and it was carrying almost all of it, because every must-have on every real row is a display phrase rather than a token. The closure above is true of the timeline field and of every row the new form writes; it was not true of the must-have chips on the rows that are on the board today until finding 113 taught the lookup to recognise a stored value by its own label. |
| 109 | P3 | The requirements board rendered a header and then nothing when no requirement was open, which reads as a page that failed rather than as a market with no open demand | Closed in PKG-DEM1 | The board had a loading branch and a grid branch and no third one, so the zero case was an empty grid. An empty state is a claim worth making explicitly: the board now says no requirements are open and offers the one action that changes that, in both languages. |
| 110 | P2 | The requirements board's page description claimed a Riyadh only scope that the form now contradicts | Closed in PKG-DEM1 | `req.metaDesc` said the board was for Riyadh commercial space. That was narrow before PKG-DEM1 and false after it, since the form offers 77 locations across 21 cities, and a page description is what a shared link and a search result show. Rewritten in both languages to describe the board by what it does rather than by a city. |
| 111 | P2 | The preview sample requirements carried English must-have phrases, one of them a condition the form has never offered | Closed in PKG-DEM1 | The sample rows served when the database is unconfigured showed "Fitted" and "Dock doors" to an Arabic reader, and one row carried "Heavy power", which is not in the vocabulary and never has been. Sample data that cannot be produced by the form misdescribes the product as surely as a wrong figure does. The rows now carry the tokens the form writes, and `requirementIntake.test.tsx` reads the sample rows out of the route's source and asserts every timeline, must-have and asset type in them is a value the form could actually have produced. |
| 112 | P3 | The requirement detail page showed "n/a" where a visitor had deliberately left the move-in date unstated | Closed in PKG-DEM1 | The column is nullable, the route accepts an empty timeline and the form pre-selects nothing, so an absent timeline is an answer rather than a gap. "n/a" in a stat card reads as a figure that was meant to be collected and was not. The card now says the date was not given, in both languages. |
| 113 | P2 | The must-have vocabulary resolved stored tokens only, and every must-have on every real requirement in the live corpus is a display phrase rather than a token, so the read side repair reached almost none of the rows that are on the board today | Closed in PKG-DEM1 for the reading, open for the data | Found by the live sweep after the ship, which is the point of running one. `GET /api/requirements` on `dpl_5p9z42CxnpbVY4PHQFjtSgUvjJza` returns six rows with `sample: false`, and their must-haves are "Fitted", "Parking", "Metro nearby", "24/7 access", "Raised floor", "Dock doors", "Street-front", "Heavy power" and "High footfall": the English display phrases the old form stored, in whichever language the visitor happened to be reading. `mustHaveLabel` looked a value up by lowercasing it and comparing it to the token, so only the single word conditions resolved, and five of the six rows still showed an Arabic reader Latin script on the chips. Finding 108 was closed against the tokens the new form writes and was therefore true of every future row and almost no present one, which is a closure worth correcting rather than leaving to be discovered by a reader. A stored value is now recognised by its own label in either language as well as by its token, ignoring case and treating the space and the underscore as one character, because "Metro nearby", "metro nearby" and `metro_nearby` are one condition written three ways. That is a reading of what the row already says and not a new claim about it. Two phrases stay outside the vocabulary on purpose: "Heavy power" and "High footfall" were never offered by any form, so there is no token they belong to, and filing them under one would name a condition the visitor never chose; they fall back to their own words. The open half is the data: a supervised one time migration rewriting the stored phrases as tokens, which needs a database write channel this environment does not have, and until it runs the alias lookup is what every reader of the board depends on. Guarded by two tests in `requirementIntake.test.tsx`, one asserting that each token and each of its two labels reach the reader's own language and that the seven live phrases in the vocabulary leave no Latin script for an Arabic reader, one asserting that a phrase outside the vocabulary keeps its own words and that a phrase merely containing a label is not that label. The first was run against the pre-fix lookup and fails there. |
| 114 | P0 | A requirement's public card printed a budget of zero for an occupier who stated no budget, and the word `null` for a size they did not state | Fixed, verified on the deployment to the limit the corpus allows | Found by PKG-DEM2 while reading the surfaces PKG-DEM1 had just made reachable. `size_min_sqm`, `size_max_sqm` and `budget_sqm_max` are nullable in the column and optional on the form; `RequirementForm` sends `Number(sizeMin) || null`, so a blank field stores a null, and PKG-DEM1 is what made that path reachable at all, because before it the form could not submit. Both public surfaces then interpolated the raw values. The board rendered `{r.sizeMin} to {r.sizeMax} m²`, printing `null to null m²`, and rendered `Number(r.budget).toLocaleString("en-US")`, which for a null budget is neither blank nor an error: it is the string `0`. A visitor would have been shown a requirement whose occupier stated no budget as a requirement with a budget of zero. That is an invented figure on a public surface, the one thing this platform's standing law does not allow, and it was invented by arithmetic rather than by anyone's judgement, which is why no review caught it. The detail card carried the same two defects under labelled rows, where a bare `0` beside the word `Budget` reads as the occupier's own stated number. Fixed in `src/lib/requirementFigures.ts`: each function returns a finished string or `null`, and `null` means the occupier did not state it. Nineteen tests in `src/lib/requirementFigures.test.ts`, including a sensitivity case that reproduces both shipped expressions and asserts `Number(null).toLocaleString("en-US") === "0"`. Verified on `dpl_BJTZoWcdPKkpSFtZsR9sHGkozieR` in both languages: the served HTML of the board and of a requirement detail page contains no `null`, `NaN`, `undefined` or `Infinity` in either locale, and the deployed dictionary carries `reqDetail.figureUnstated` while `req.upTo`, `req.rangeTo`, `reqDetail.rangeTo` and `reqDetail.reply` are gone from it. The limit is stated rather than glossed: `GET /api/requirements` returns six rows and every one carries a non-null `sizeMin`, `sizeMax` and `budget`, so no live row exercises the null path today. The defect was reachable and not yet realised, and the fix is verified against the module under test rather than against a live null-figure row. |
| 115 | P2 | One stored size had four renderings across three surfaces, none of them through `src/lib/format.ts` | Fixed, verified on the deployment | The board and the detail card printed `null to null m²`; the lister dashboard read the nulls honestly but printed `200 to ? m²` for a half-open range and spelled its unit `sqm` in English and متر مربع in Arabic, so the same stored fact carried four unit spellings and only one surface told the truth about an absent bound. None went through `format.ts`, the module built precisely so a figure has exactly one rendering, with grouped numerals, one unit spelling per locale, and a first-strong isolate so the digits and unit do not swap places in an Arabic paragraph. All three surfaces now call `sizeRange` and `budgetCeiling`. A half-open range states the bound that exists, `from 500 m²` or `up to 1,200 m²` and `من` or `حتى`, rather than inviting the reader to supply the missing half; equal bounds collapse to the single figure. The budget states itself as a ceiling on every surface, because the column is `budget_sqm_max` and the board said so while the detail card printed it bare under the label `Budget`. `scripts/ar-lint.mjs` reads the new module, so its Arabic is inside the same banned-term gate as `format.ts`. |
| 116 | P2 | Every response to a requirement carried a Reply control that could not act, and the platform holds no channel behind it | Partially addressed | The control was a `span` with no handler, no role, no keyboard path and nothing to open. `GET /api/requirements/[id]` returns a respondent's name, organisation and message and deliberately returns no email and no phone, so the platform holds no channel from the occupier back to the person who answered, and the occupier who posted the requirement was the only person who ever saw the control. A control that cannot act is a promise the product does not keep. It is removed, and the responses list now carries one honest sentence saying replying inside SAT Markets is not built, no contact details are shown, and what each party wrote is the whole of their response. The open half is the reply loop itself: a messages table keyed to the requirement and the respondent, a write route under the same identity derivation the interest route already uses, and a disclosure position on what each side sees of the other. That is a supervised schema change and there is no database write channel from this environment, so it is recorded rather than half built. |
| 117 | P1 | The `create_requirement` RPC still files a requirement in Riyadh when the payload names no city | Migration authored, awaiting owner application | RC14. Read out of `supabase/migrations/20260714_public_write_paths_restored.sql` while verifying PKG-DEM1's city fix. The route was corrected in PKG-DEM1 to derive the city from the district row and refuse a request naming neither a real district nor a recognised city, but the RPC body still holds `coalesce(nullif(payload->>'city',''), 'Riyadh')`, so the fabrication survives one layer below the fix. `create_requirement` is SECURITY DEFINER and executable by `anon` and `authenticated`, so the route is one caller of it and not a gate in front of it: a direct PostgREST call, a future server action, an admin script or a restored older client all still reach the default. User consequence: a requirement carrying no city is stored as a requirement in Riyadh, the board renders `city` as fact, matching filters on it, and the poster is never shown the value chosen for them, so a brief meant for Jeddah is shown to Riyadh landlords with nothing in the row saying the city was assumed. Fix authored as `supabase/migrations/20260801_requirement_city_is_never_assumed.sql`: the coalesce becomes a `raise exception` with sqlstate `check_violation`, before the insert, and the rest of the function is restated verbatim so a reader can see that one line differs. Not applied. Every Supabase write tool is permission denied from this environment (`apply_migration` and `execute_sql` both), so this row stays open until the owner runs the migration; the register does not record a database change that has not happened. `src/lib/requirementCity.test.ts` holds four guards on the repository state: that the LAST definition of the function in migration order defaults no city and names no city literal, that it raises before the insert with a distinguishable sqlstate, that the restatement did not drop the hardcoded `open` status, the notification rows or the pinned `search_path`, and that the route's own refusal stays upstream of the RPC call. Evidence: automated source scan and guard tests, repository state only. The deployed function is unverified from here and still defaults until the migration is applied. |
| 118 | P2 | Registering interest in a requirement notifies nobody, so the occupier is never told a response arrived | Confirmed open | `POST /api/requirements/[id]/interest` inserts into `requirement_interests` and inserts no notification row, while `create_requirement` does insert `requirement_notifications` for the broker, landlord and SAT audiences. So the platform tells owners and brokers that demand exists and never tells the occupier that supply answered. The occupier finds out by returning to the page. This is the demand side of the same absence as finding 116: the notification schema exists and this write path does not use it. Writing to it needs the same supervised database access this environment does not have. O12 also keeps external channels off until consent is designed and recorded, so the in-platform row is the whole of what is available even once the write channel exists. |
| 119 | P3 | Two Arabic progress strings on the requirement surfaces were written جاري where the rest of the platform writes جارٍ | Closed in PKG-DEM2 | Found in the live evidence for this package rather than in the source: the deployed Arabic requirements board serves `جارٍ تحميل الطلبات…` and the deployed Arabic requirement detail page serves `جاري تحميل الطلب`, so a reader moving from one to the other reads the same word spelled two ways one click apart. `جارٍ` is the indefinite nominative and is what the other eight progress strings in the dictionary use, including `reqDetail.matchesLoading` in the same object. `reqDetail.loading` and `reqDetail.registering` are corrected, which makes all ten agree. This is a two-value change with no key change, so dictionary parity is untouched. Not swept further: the same ten strings carry three ellipsis styles, an ASCII `...` in `ui.searching` and `enquiry.sending`, a `…` in three others and none in the rest, with some callers appending their own in JSX. That is a real inconsistency and it is recorded here rather than fixed, because deciding it properly means deciding where the ellipsis belongs, in the value or at the call site, and a blind sweep would settle it by accident. Verified on `dpl_B3zv6rnM1qSmtpJjux32vrBPJdJA`: the Arabic requirement detail page serves `جارٍ تحميل الطلب`, and `جاري تحميل الطلب` and `جاري التسجيل` are absent from the payload. |
| 120 | P1 | The lister's own surfaces render an annual rent as if it were not annual, so the one reader who has to check the price is shown it wrong | Closed in PKG-SUP2 | `listings.asking_rent_sqm` is collected by a form whose own label reads "Asking rent (SAR per sqm per year)" and, in Arabic, `الإيجار المطلوب (ريال للمتر المربع سنوياً)` (`ListingStudio.tsx:459`). That is record-level evidence of what the column means, not an inference from the column name. The public listing page, the compare table, the map pin and the broker profile all rendered it per year. `/dashboard`, `/dashboard/listings` and `/find` rendered it as `SAR/m²`, with the year taken off. So a lister who set an annual rate saw it stated as something else on their own management screen, which is the one screen whose entire purpose is letting them confirm that the price they entered is the price the market is being shown. The unit is now decided once, from the deal type, in `src/lib/listingFigures.ts`, and no surface spells it. Verified on `dpl_9mYWf9XgXdTTnzCGrURi5vtwibjx` in both languages: `/listings`, `/compare`, `/brokers`, a listing detail page and a printable flyer all serve the annual unit from one source, `SAR/m²/yr` and `ريال/م²·سنة`. `/dashboard`, `/dashboard/listings` and `/find` sit behind a session or a POST and cannot be reached through the GET-only channel available here, so for those the evidence is the module tests, the typecheck and the production build. |
| 121 | P1 | The lister dashboard advertised every open brief as `? to ? m²`, turning an occupier's deliberate silence into a printed question mark | Closed in PKG-SUP2 | The brief-match panel rendered `(b.size_min_sqm || "?")` and `(b.size_max_sqm || "?")`, so a requirement whose author deliberately left the size open was advertised to every lister as a brief with two unknowns rather than a brief with an open range. `||` also swallows a stated zero. It now goes through `sizeRange` from `requirementFigures.ts`, which states an open range as an open range and draws nothing where the record states nothing. Closing this required a second fix: the file referenced `loc` in three places and never declared it, so the tree did not typecheck until `const loc: Loc = ar ? "ar" : "en";` was added. Not live verified: the brief-match panel is on the lister dashboard, behind a session the GET-only evidence channel cannot open. Verified by `requirementFigures.test.ts` and by the typecheck that the missing `loc` broke. |
| 122 | P2 | An unstated area was printed as the word `null`, including on a document a broker prints and hands to a client | Closed in PKG-SUP2 | `area_sqm` is nullable and seven surfaces interpolated it unguarded. The printable flyer produced a tile reading `null m²` on paper, where nothing can be corrected afterwards. The public explore card, `/me` in three places, the enquiry panel and the Advisor widget did the same on screen. `/advisor` ran it through `Number(l.area_sqm)`, so an unstated area was shown to the occupier as `0 m²`, which is worse than nothing because it is a figure nobody wrote down. `/saved` drew the unit outside its own guard, producing `na m²`. Every figure function in `listingFigures.ts` returns `null` for a figure the record does not state, and null instructs the caller to draw no line at all. The flyer's tile grid moved from `repeat(4, 1fr)` to `repeat(auto-fit, minmax(128px, 1fr))` so a dropped tile leaves no hole. Verified on `dpl_9mYWf9XgXdTTnzCGrURi5vtwibjx`: zero occurrences of `null`, `NaN` or `undefined`, and zero standalone `0 m²` or `0 م²` under a boundary-aware match, across six payloads in both languages. The live corpus does not exercise the withheld path, since all 88 listings state a price and the rendered payload carries no raw column names, so the null branch is verified against the module rather than against a live row. |
| 123 | P3 | One unit was spelled six ways, and two client components resolved their digits from the reader's device, emitting Arabic-Indic numerals on the most viewed public page | Closed in PKG-SUP2 | `Number(price).toLocaleString()` with no locale argument resolves the runtime default rather than the page's locale. `ListingCard.tsx` and `saved/page.tsx` are CLIENT components, so on a phone set to Arabic the public explore grid and the saved shortlist rendered their prices in Arabic-Indic digits, a live breach of the standing law that Western numerals are used in both languages. The same bare call sat server-side on `/compare`, `listings/[id]` and `building/[id]`, one server-environment setting away from the same result. Alongside it the same unit was spelled six different ways across the tree, including `SAR/m2 yr` on `/saved` against a different Arabic separator from every other surface. All of it now comes from `format.ts` through `listingFigures.ts`. Fifteen orphaned unit strings were deleted from each dictionary after checking every one for readers; `common.sqm`, `building.perYear` and `locations.officeMedianSuf` were kept because they still have them. Verified on `dpl_9mYWf9XgXdTTnzCGrURi5vtwibjx`: zero Arabic-Indic digits in the Arabic body of every payload fetched. The fifteen deleted keys were checked against the unstripped payload, where the dictionary is serialised, alongside control keys kept on purpose that still return their expected hits, so the zero means the key is gone rather than that the dictionary was absent. |
| 124 | P2 | A sale listing stated no price at all in the Advisor and on `/find`, because the shortlist payload carried only the lease column | Closed in PKG-SUP2 | `/api/advisor/shortlist` selected both price columns and forwarded only `asking_rent_sqm`, which is null on every sale row, so a shortlist run with `dealType` `sale` returned rows whose price line was simply absent: the occupier asked to buy and was shown inventory with no prices. `AdvisorWidget` separately printed a bare number with no unit beside it, so a rent and a sale price appeared in the same column with nothing to tell them apart, and `/advisor` chose the unit by asking whether `asking_rent_sqm` was populated rather than by asking what the deal type was, which is the same class of error as finding 120 reached from the other end. The route now forwards `deal_type` and `sale_price`, the `Row` and `R` types declare them, and every price on both surfaces goes through `priceUnitKey`. Found while rewiring the call sites for finding 120, not by a separate audit. Verified on `dpl_9mYWf9XgXdTTnzCGrURi5vtwibjx`: the English explore grid serves 88 listings, 70 with the lease unit `SAR/m²/yr` and 18 with a bare `SAR`, and none falling back to "on request". The route half is a POST and cannot be exercised through the GET-only evidence channel available here. |
| 125 | P0 | The published Rent Index band was rendered on the front door and the explore map by client components that resolved their digits from the reader's device | Closed in PKG-FIG1 | `MarketingHome.tsx` and `MapExplorer.tsx` both begin `"use client"`, and both called `Number(x).toLocaleString()` with no locale argument on the REGA-attributed published band. `toLocaleString()` with no argument resolves the device locale, so on a phone set to Arabic the first screen of the site and the explore map rendered the attributed figure in Arabic-Indic digits. This is the same defect class as finding 123 and it is graded higher because of where it sat: the front door, on the one figure the platform carries an external attribution for. Both now route through `formatInteger` and `formatRange`. Two invented bounds were closed with it: `sel.bandHigh ?? 0` on the map and a truthiness test on `band_low` in `building/[id]` each let one stated end print beside an absent end, so a band with no ceiling read as ending at zero. The shipped guard is a null check on both ends, because a range with one end missing states neither. Guarded by three tests in `src/lib/figureGrammar.test.ts`, one of which is a tree-wide scan for a bare `toLocaleString()` in any shipped source, and one of which reproduces the device-dependent call and asserts it differs from the formatter's output. The limit, stated rather than glossed: this defect only manifests on an Arabic-locale DEVICE, and the only live evidence channel available here is a server-rendered GET that carries the server's locale, so the breach itself cannot be reproduced live. The live payloads establish that the corrected code is served and that the Arabic bodies carry no Arabic-Indic digits; they cannot establish what an Arabic-locale phone would have rendered from the previous code. |
| 126 | P2 | The Listing Studio preview is captioned "As a visitor will read it" and rendered a unit no visitor surface renders | Closed in PKG-FIG1 | The preview block spelled its own price as `formatMoney(price) + " per sqm per year"`, in Arabic ` للمتر المربع سنوياً`, and decided the unit from `f.deal_type === "sale"` inline. That is a seventh spelling of the unit finding 120 consolidated, and it appeared under a caption that is a claim about fidelity, on the one screen built to let a lister check that the price they typed is the price the market will be shown. The caption was false in the exact way the screen exists to prevent. Both figures now come from `listingFigures.ts`, the module every visitor surface reads, so the caption is true by construction rather than by two authors agreeing. The intake label that reads "Asking rent (SAR per sqm per year)" is deliberately KEPT and asserted present by the guard: it is the record-level evidence finding 120 turned on, the sentence the lister reads while typing the number, and the only statement in the tree of what `listings.asking_rent_sqm` holds. The first version of the guard would have deleted it, which is recorded here because a guard that removes the evidence for its own rule is worse than no guard. Not live verified: the Studio sits behind a session and the only channel available here is an unauthenticated GET. |
| 127 | P2 | One published range had four spellings, one of them an en dash inside Arabic copy that no gate could see | Closed in PKG-FIG1 | `src/lib/market/verdict.ts` built the Arabic verdict sentence `النطاق 1,800-2,900` in source. The dictionary walk in `scripts/ar-lint.mjs` bans the en dash inside `src/i18n` only, and the tree-wide sweep beside it deliberately bans the em dash alone, because in English the en dash is the correct numeric range separator. Both rules assumed Arabic copy lives only in the dictionaries. It does not, so the one construction the gate exists to prevent shipped in the one place the gate did not look. Alongside it, four surfaces spelled the separator themselves and a fifth branched inline on `ar ? " إلى " : "-"`. Eight files now route through `formatRange`, and `ar-lint` gained a rule scoped to the LANGUAGE rather than to a directory: a quoted string or template chunk carrying Arabic script may not also carry an en dash, wherever it is written. That rule was proved to fire against a deliberate probe file before the probe was removed. Two limits of the new rule are stated in the script itself: a literal that renders into Arabic without containing Arabic, because its locale arrives as a parameter, is invisible to it, which is exactly what `rentIndexEvidence.ts` was and why the call sites were rewired rather than left for the gate; and JSX text content is not a string literal, so a dash typed between two elements is not seen either. Deliberately not swept, and recorded rather than silently left: `WatchBanner.tsx`, `market/analyser.ts`, `market/valueEvidence.ts` and `agents/tools.ts` compose a range inside a larger sentence that carries its own preposition, all locale-pinned, none using a dash. The Unicode Bidi Algorithm was derived by hand for the Arabic case before this exclusion was taken, because the suspicion was that they render backwards; they do not. A bare range inside an already-RTL paragraph reads low then high correctly, and isolation matters for a composite placed beside a differently-directioned neighbour rather than for this. No defect was invented to justify the sweep. |
| 128 | P1 | The Advisor and the match explainer handed a reader figures spelled in the storage layer's grammar, including a snake_case column key printed as a unit in an Arabic answer | Closed in PKG-FIG1 | Found by running `figureGrammar.test.ts` for the first time and treating its two failures as a discovery instrument rather than as noise to be silenced. Six sub-defects, each with record-level evidence. One, `/api/advisor` interpolated `band_low`, `band_high` and `median` raw from PostgREST, which hands `numeric` back as a string, so the figures were ungrouped, and interpolated `band.unit` raw, which is the STORED key `sar_sqm_yr` written by `src/lib/ingest/rentBasePipeline.ts` and aliased in `format.ts`. So an Arabic answer quoting the published, REGA-attributed, licence-gated, passport-carrying band ended in a Latin snake_case identifier. The same file forbids the raw `2026-Q2` period form six lines above under Codex item 5, which is the same rule about the same class of value. Two, `/api/advisor/shortlist` spelled the lease unit `SAR/m²·yr` with a middle dot against the canonical `SAR/m²/yr`, an eighth spelling of the unit finding 120 consolidated once. Three, the same route could print `Asking runs NaN to NaN`, because `.map(Number)` yields NaN for an unparseable stored price, `Math.min` of NaN is NaN and the string `"NaN"` is truthy; unparseable prices are now filtered and an unstateable range is withheld entirely. Four, `matching.ts` printed `1200 sqm` and `1200 متر مربع`, ungrouped because its local formatter was `String(n)`, in a unit spelling no visitor surface renders, beside a card reading `1,200 m²`; it now calls `sizeRange`, which already rendered that exact fact with the half-open case handled. Five, the same file printed a budget as `2000 per sqm` and `2000 للمتر المربع`, naming an area with NO CURRENCY AND NO PERIOD, for a column the form collects under the label "Budget ceiling (SAR/m²/yr)". A rate with the currency taken off is not a smaller version of the figure, it is a different figure, and the reader has no way to tell which one they are looking at. Six, `requirementFigures.budgetCeiling` branched on the deal type and rendered a non-lease budget as a bare `SAR` total. `RequirementForm` has ONE budget input, not conditional on the deal type, under one label, and `matching.ts` compares the column it fills against `ratePerSqm(listing)`, which for a sale divides the sale price by the area. So the form and the matcher agreed the column is a rate and only the renderer disagreed, and one stored column meant two different things depending on which screen read it. The existing test asserting the opposite property was rewritten rather than deleted, with the record it was wrong about quoted in it. `en.json`'s `postReq.budgetCeiling` was corrected from `SAR/m²·yr` to `SAR/m²/yr` to match the canon `format.ts` renders; the Arabic value already matched and was left alone, so dictionary key parity is untouched. Guarded by four new tests in `figureGrammar.test.ts`, three of which reproduce a shipped expression and watch it fail. The limit, stated rather than glossed: both Advisor routes are POST-only and the only live channel available here is an unauthenticated GET, so neither can be live verified at all. For those two the evidence is the source, the typecheck and the suite, and nothing more is claimed. |
| 129 | P1 | The platform carried four parallel unit tables and nineteen distinct spellings of units `format.ts` already owns, one of them live on the English front door, and sixteen registry fields printed a Latin `m` on Arabic pages | Closed in PKG-FIG2 | Findings 120, 124, 128 and 129 were the same defect arriving four times: a surface spelling a unit itself. Each was fixed by rewiring call sites, and each time a new spelling appeared where the previous sweep had not looked, because nothing stopped one. A purpose-written scan of every `.ts`, `.tsx` and `.json` file under `src` found the reason was structural: there were FOUR unit tables and only the first was typed. `src/lib/format.ts` `UNITS`, the canon. `src/lib/labels.ts`, a two-entry map exported as `unitLabel` with spaced spellings nothing renders and zero callers anywhere in the tree. `src/lib/attributeDisplay.ts`, a six-entry EN to AR map keyed on the exact strings in `assetFields.ts`. `src/lib/market/valueEvidence.ts`, two regexes plus a private duplicate of `format.ts`'s `joinUnit`, neither regex matching `sar_sqm_yr`, which is the string `ingest/rentBasePipeline.ts` actually writes. Nineteen spellings across eighty-nine occurrences. The live one: `MarketingHome.tsx` held the lease unit twice in its own copy objects, once per language, and the English literal ` SAR/m²·yr` did not match the table, so the first screen of the site carried the wrong spelling of the platform's most-used unit six times, on the band panel, the band caption and three featured price cards. The largest one was invisible to every previous sweep: `resolveUnitKey` had no alias for `m`, the spelling the asset field registry stores in sixteen fields, more than any other unit, so every one of them rendered the Latin `m` on an Arabic page. `formatUnit` passes an unresolved unit through verbatim, which is deliberate and correct and is also why no error was ever raised. The same passthrough is how `kN/m²` and `L` reached Arabic attribute rows in Latin script through `attributeDisplay.ts`'s private map. Fixed by adding `t_sqm`, `kn_sqm` and `litre` to `UNITS` with fourteen aliases including `m`, deleting the dead `labels.ts` table outright, rewiring `attributeDisplay.ts`, `valueEvidence.ts`, `assetFields.ts` (six rows), `MarketingHome.tsx`, `MapExplorer.tsx`, `FilterBar.tsx`, `EditListingForm.tsx`, `proto/page.tsx`, `searchNote.ts` and `building/[id]/page.tsx` through `formatUnit` and `formatWithUnit`, correcting eight `en.json` values, deleting the dead dictionary keys `perSqm`, `unitDeskMo` and `perYear` from both locales, and adding an `ar-lint` rule that READS the canonical set out of `format.ts` rather than restating it, because a hardcoded list in the gate would have been a fifth table. Two sub-defects were also closed: `attributeDisplay.ts` and `LocationFacts.tsx` each pinned half a number format, fixing the numbering system to Latin and leaving the rest to whichever CLDR data the runtime carries, which on a client component means the visitor's browser. That was first written up as a grouping defect and the claim was checked before it was believed: on the runtime this ships on, `ar-SA` with Latin numerals groups with the same comma `en-US` does, so no grouping difference exists and none is claimed. The difference that does exist is a `U+200E` LEFT-TO-RIGHT MARK emitted before the minus sign of a negative value. Guards: five new tests in `figureGrammar.test.ts` and one in `attributeDisplay.test.ts`, each proved to fail against `git show HEAD` of the file it guards. |
| 130 | P1 | The front door captioned the published Rent Index band "Average" over a row whose statistic it never read | Closed in PKG-FIG2 | The hero band panel on `/` printed `home.medianUnit`, whose value was `Average {unit}` in English and `المتوسط {unit}` in Arabic. The word was typed once, in the dictionary, and rendered over whatever row the query returned, so a median cell and an average cell were captioned identically and the key name said `median` while the value said `Average`. The row itself carries `stat_kind` and the front door select did not even fetch it. Three things were wrong at once: the statistic was asserted rather than read, the key and its value disagreed about which statistic was being asserted, and the two languages could drift apart because each held its own copy of the assertion. Fixed by making `stat` a REQUIRED field on `HeroBand`, so the compiler rather than a reviewer is what stops a band travelling without one; by adding `stat_kind` to the front door select and resolving the word through `statisticLabel(normalizeStatisticKind(r.stat_kind), ar)`, which routes an unrecognised or absent value to `Unlabelled` and `غير مُعرّف` rather than to a named statistic; and by replacing `medianUnit` with a pattern holding a slot for the word instead of the word itself. `normalizeStatisticKind` is case sensitive on purpose and the guard asserts it: `Average` with a capital letter is a near miss, not a match, and a near miss must cost the caption its name. Guarded by three tests in `src/lib/figureGrammar.test.ts`, one behavioural on the label resolver, one on the required field, one scanning the front door source for the resolving call and the select column. The tail worth recording: nothing anywhere read `PublishedKpis.stat`, so the field was both dead and dishonest, which is how a wrong value survives for as long as this one did. |
| 131 | P1 | Not how a unit is spelled but who spelled it: forty six unit shaped strings per dictionary, seven of them putting Latin script into Arabic copy | Closed in PKG-FIG2 | Finding 129 built one unit table and a gate that reads the canonical spellings out of it. That gate checks SPELLING. It cannot see AUTHORSHIP, so a dictionary string spelling a unit correctly passes it, and forty six strings per locale did. Seven of them were worse than merely duplicated: the Arabic values of `invest.csvAcqPrice`, `invest.csvIndValue`, `invest.csvNoi`, `invest.csvPotentialNoi`, `invest.potentialNoi`, `invest.kNoi` and `hbu.kNoi` carried the unit in LATIN SCRIPT inside Arabic copy, which is a language defect the spelling gate is structurally unable to detect because the string it is looking at is spelled exactly right. Fixed by giving each of those strings a `{unit}` slot and filling it from `unitText` at the call site, in the reader's language, and by adding a fifth pass to `scripts/ar-lint.mjs`: an AUTHORSHIP rule that fires when an English unit spelling appears inside a literal that also carries Arabic script. The rule was proved to fail against HEAD before it was believed, seven hits, exit 1, and clean against the corrected tree. `unitText` was added beside `formatUnit` in the same operation and is deliberately not a second table: `formatUnit` is now defined in terms of it. The difference is the word joiner. `formatUnit` inserts `U+2060` around the separators so a unit cannot break across two lines, which is right on a page and wrong in a CSV header, where an invisible control character survives a copy, defeats an exact match comparison in someone else's spreadsheet, and cannot be seen by the person trying to work out why their lookup fails. `map/page.tsx` was rewired off `common.sqm`, which was a square metre spelled in the dictionary rather than read from the table, and that key was deleted. Guarded by three tests in `figureGrammar.test.ts`, including a tree wide scan of both dictionaries for an English unit spelling inside an Arabic value. Recorded and deliberately NOT fixed here, so the remaining surface is stated rather than implied: forty six unit shaped strings per locale remain, most of them legitimate sentences that name a unit as part of a label; `/invest`'s `fmtM`, `fmtPct` and `fmtN` bypass `formatNumber` entirely and `fmtM` appends a Latin `M` on Arabic pages; the investment CSV writes Latin `Y1` to `Y7` year heads in both languages; the `unit-law` exemption marker documented in `ar-lint` was unusable in any `.ts` or `.tsx` file until this package fixed the comment stripper's line counting, which means the documented escape hatch had never once worked. The provenance of the English dictionary edits in this finding is stated here because it matters: an accidental `git checkout --` during the package destroyed the uncommitted work and `en.json`'s seven corrected values were hand reconstructed from HEAD, cross checked against the Arabic post fix values and against the assertions in `figureGrammar.test.ts`. They are value identical to what the tests assert and parity clean against `ar.json`; they are not the original bytes. |
| 132 | P0 | Two published tables printed the header "Average SAR/m²" over rows storing `SAR/m2/yr`, so both halves of the heading were wrong and a reader could not tell | Closed in PKG-FIG2 | The listings index cut and the Rent Index district table each carried a column heading that asserted a statistic the page never read and spelled a unit that dropped the period the stored unit carries. `1,650 SAR/m²` and `1,650 SAR/m²/yr` are not the same claim. A reader who assumes a month is out by a factor of twelve, and there is nothing on the page that would tell them they should not. Graded P0 for that reason: it is a wrong figure, not a wrong word, and it was live on two public surfaces in both languages. Five dictionary families were involved. `listings.colMedian` and `rentIndex.thMedian` were both keyed `median` and valued `Average`. `rentIndex.thBand` read `Band (SAR/m²)`. `rentIndex.heatSubReal` spelled the unit inside a sentence. `marketPage.kpiMedianOffice` read `average office rent SAR/m²/yr` over a figure that is the middle of the sorted office cells, so the key name was right and only the value disagreed. `flyer.officeAvg` read `Office average {rate} SAR/m²/yr`. Two more instances were found while scoping and folded in: `rentIndex.kpiOffice` and `kpiRetail` were asymmetric between the languages, English naming no statistic and Arabic asserting `متوسط`, and `published.ts` set `stat` from `rows[0].stat_kind`, the first row's word presented as the whole set's. Fixed by adding `src/lib/market/columnHeading.ts`, which resolves a heading from the cells under it on one rule: name a thing only if every record agrees, and say nothing rather than guess. A column of mixed statistics gets the neutral quantity word, `Rent` and `الإيجار`. A column whose units do not all resolve to the same key prints no unit at all, and a single row arriving without a unit denies the whole column, which is what makes the Rent Index page's narrow fallback select safe: it does not fetch `unit`, so a schema surprise costs the heading its unit rather than buying it a guess. `PublishedKpis` gained a resolved `unit` beside a now honest `stat`, and the two rent tiles on the Rent Index, the market page KPI and the flyer line all take their unit from the same object their figure came from. A unit and a statistic name are not market figures, so resolving a heading over rows whose figures the licence withheld publishes nothing Codex item 2 fences: the heading describes the column, and each cell still decides on its own whether it has anything to put in it. Two adjacent defects were closed with it: the index cut's figure cell called `toLocaleString("en-US")` directly and now calls `formatInteger`, and `MarketingHome`'s `kpis` prop restated the `PublishedKpis` shape by hand, declaring `stat` as `"average" | "median" | null` over a producer that can also answer `single`, `count`, `range`, `rate` or `index`; it now imports the type. Guarded by three tests in `figureGrammar.test.ts`, two behavioural over `columnHeading.ts` and one scanning the callers and both dictionaries, proved to fail against HEAD callers in a falsification tree. Because `columnHeading.ts` is new, reverting the callers does not exercise it, so the rule it replaced is written out inside the behavioural test and asserted to disagree with the shipped one. Recorded and not fixed: when units do not agree the heading names none and the cells render bare numbers, so a mixed unit column would show figures with no unit anywhere; that case does not exist today, since all seven published segments store `SAR/m2/yr`, and it is the next thing to close when a second unit enters the index. `market/page.tsx` still formats through a local `nf = (n) => n.toLocaleString("en-US")` rather than `formatNumber`; the output is identical because `formatNumber` pins Latin numerals for both locales, so this is an authorship defect and not a display one. `rentIndex.kpiContracts` was left asserting a statistic in copy on purpose: it describes the SOURCE's methodology, REGA publishing averages of registered contracts, not our rows, and building the Arabic from `statisticLabel` would produce ungrammatical Arabic because the label carries the definite article. |
| 133 | P1 | Three surfaces built a form label by concatenating the raw registry `field.unit` string, so an Arabic lister was asked for `(m)`, `(m²)`, `(kVA)`, `(t/m²)`, `(kN/m²)`, `(L)` and the English words `(months)` and `(years)` | Closed in PKG-LS2 | Found by reading `ListingStudio.tsx` rather than by trusting the previous handback's prediction, which named only the two hand spelled labels above it. `renderField` in the Studio and in `EditListingForm`, and the numeric facet placeholder on `/listings`, each appended `field.unit` verbatim in both languages. Seventy registry fields declare a unit and the canon in `format.ts` holds an Arabic spelling for every one of them. `attributeDisplay.ts` already routed the VALUE through that table and already treated `months` and `years` as counted nouns, so the number a reader saw carried the right unit while the box the lister typed it into carried the wrong one. Closed by `src/lib/fieldLabel.ts`, one module that resolves a registry unit through the one table and takes `months` and `years` from the exported `COUNTED` forms, since a label carries no count. `fieldLabel.test.ts` asserts every unit class in Arabic, asserts that no declared unit can fall through verbatim, and bans the concatenation in source; the ban fails against `95f162b` naming all three files. |
| 134 | P1 | The Listing Studio asked for `Area (sqm)` and `Asking rent (SAR per sqm per year)` while the edit screen for the same two stored numbers said `Size (m²)` and `Asking rent (SAR/m²/yr)`, so one number had two units depending on the screen | Closed in PKG-LS2 | None of the Studio's four unit spellings, English or Arabic, is a spelling `format.ts` knows, and `sqm` is not a unit this platform shows a reader anywhere. Both screens also decided the deal to unit question with an inline `deal_type` comparison, although `listingFigures.ts` has owned that decision for every rendering surface since PKG-SUP2. This is the point where a figure enters the platform, and the stored value is a bare number, so a label naming the wrong unit is undetectable downstream. Closed by `areaFieldLabel` and `priceFieldLabel`, which read `priceUnitKey` and the one table; `Area` was chosen over `Size` because the public dictionaries and `netArea` already say `Area`. The finding 126 guard, which asserted the old literals were still present as finding 120's evidence, was superseded rather than deleted: it now asserts the label is built by `priceFieldLabel`, which cannot drift from the surfaces that render the price. Live verification is owed and recorded as owed, because both intake screens are session gated and `web_fetch_vercel_url` is GET only. |
| 135 | P1 | Coordinates on a published listing were unreachable through self serve at any stage, so an essential completeness check named a gap the lister structurally could not close, on 50 of 50 published rows | Closed in PKG-LS3 | Found by measuring the live corpus rather than by reading the intake form. `GET /api/listings` on `3f13eb6` returned 50 published rows, HTTP 200, and `lat` and `lng` were null on every one of them. Running the repository's own `assessListing` over those rows put all 50 in the `incomplete` band with `coordinates` among the missing essentials. The reason was not that the field was unbuilt: `lat` and `lng` sit in `DRAFT_ONLY_EDITABLE`, so `PATCH /api/listings/[id]` answered every attempt with 403 and the text `The location of a published listing is changed by SAT.` The draft only rule is written against substitution, because a reader relied on the pin and moving it changes what was advertised. That reasoning does not reach a listing that never had a pin: nothing was relied on, the space is on no map, and it answers no radius search. Closed by a third editability category, `FILLABLE_WHEN_ABSENT` and `mayFillAbsent` in `listingEdit.ts`, which widens `mayEdit` and never narrows it and opens exactly one case, the first write to an empty fillable field on a live listing. `mayEdit` is byte identical, so every existing caller and test is untouched. The route tests the value on the row rather than what the client asserts, and the screen is gated on the same predicate computed on the server, so the form cannot offer a write the route refuses. |
| 136 | P2 | The floor plan check in `listingQuality.ts` read only the `floorplan_url` column and could not see a plan uploaded as a `listing_media` row, so a lister who had supplied one was told it was missing while the page above them displayed it | Closed in PKG-LS3 | A floor plan arrives two ways. A link goes into `listings.floorplan_url`; an upload through the docs panel writes a `listing_media` row of kind `floorplan` and never touches the column. The check read the column alone, so the second path was invisible to it. The Listing Studio papered over this at creation time with its own sentinel, `hasFloorplanFile: floorFiles.length > 0 || stored.floorplans > 0`, which is why the defect never surfaced during intake and only bit a lister returning to a listing that already exists. Closed by an optional `floorplan_count` on `ListingFacts` and a check that reads `isFilled(facts.floorplan_url) || plans > 0`. It is optional rather than required because a caller that cannot supply the count must fall back to the column rather than assume either way; both dashboard callers supply it from the same media query that renders the panel above. Two tests assert the media path reads present, that neither path still reads missing, and that an undefined count changes nothing. |
| 137 | P2 | A first pin whose nearest district disagrees with the listing's recorded `district_id` is a real disagreement that nothing currently reads | Closed in PKG-ELITE-E1 | Raised deliberately in PKG-LS3 rather than resolved inside it. `LocationPicker` derives a district from the pin through `nearestDistrict`, and every published listing already carries a `district_id`, which is the field that places it in search today. Letting a first pin overwrite that district would be a substitution wearing an addition's clothes, so `district_id` is deliberately excluded from `FILLABLE_WHEN_ABSENT` and the edit form never sends it, on either stage. That is the safe choice and it is asserted by test, but it leaves the disagreement itself unhandled: a listing can now carry a pin in one district and a recorded district name in another, and no surface compares them. Resolving it needs a reconciliation rule and an owner decision about which side wins, neither of which belongs in a package whose subject is telling a lister what is missing. Recorded here so the comment in `listingEdit.ts` that cites this number has something to cite. CLOSED IN PKG-ELITE-E1 SLICE C. The evidence source is stated first because Codex required it: SAT holds no district boundary geometry of any kind. The deployed production preview is the evidence, and it is conclusive rather than indicative, because the listings page ships the whole location set to the browser and every row carries exactly an id, an English name, an Arabic name, a latitude, a longitude and a kind. There is no polygon, no radius, no bounding box and no area anywhere in the record. No dataset was purchased, licensed or scraped, and Codex forbade doing so in this package. That fact decides the shape of the answer rather than merely constraining it: a pin cannot be tested for containment inside a district when no district has an inside, so `src/lib/locationConsistency.ts` has no `verified` verdict and cannot grow one while the data is a single point per row. Its five verdicts are `no_pin`, `no_location_recorded`, `unverifiable`, `consistent_unverified` and `contradicted`, and the fourth is named for what it is: nothing contradicts this, and nothing confirms it either. `CONTRADICTION_KM` is 25, set far above ordinary intra-city centroid spread because the costs are asymmetric. A false accusation costs a lister their trust in every other thing the screen tells them; a missed contradiction costs one review. A differing city is not on its own an accusation, because Dammam, Dhahran and Al Khobar sit within a few kilometres of one another. Four surfaces read the verdict. `LocationPicker` no longer rewrites a recorded location from a later pin at all: only an empty location is answered from the pin, which is a first answer rather than a rewrite, and a pin that is closest to some other row produces an offer with a button the lister presses. `PATCH /api/listings/[id]` refuses a contradicting pin with HTTP 409 and `code: location_contradiction`, carrying the statement in both languages. `launchGate.ts` gained a fifth fact and two blockers, `location_contradicts_pin` and `location_consistency_not_checked`; `unverifiable` maps to not checked on purpose, because that is the state where SAT holds no point for the recorded location, so the comparison never happened and calling it a pass would let an unanswered question count as an answer. `listingQuality.ts` relays the contradiction to the lister as a `pin_disagrees_with_recorded_location` contradiction, and relays rather than restates: the statement is the one `locationConsistency.ts` wrote, so the quality module holds no geometry and cannot become a second place in the codebase able to decide what a district is. Law 7 is kept at the level of the name as well as the behaviour: `nearestDistrict.ts` is now `nearestLocation.ts`, because the old name asserted something the function cannot know, and `kind` now travels with every row so the label comes from `locationKind.ts`. KAFD reads Development, never District. The lister-facing wording names no column, no table and no distance model, in either language, per Codex. Existing published preview records are unchanged and remain test data under the existing sample and noindex controls. Guarded by 9 tests in `locationConsistency.test.ts`, 2 added to `nearestLocation.test.ts`, 1 added to `launchGate.test.ts` with two more one-at-a-time blocker cases, and 2 in `listingQuality.test.ts`. |
| 138 | P2 | Two dashboard selects asked `districts_geo` for a `city` column and ordered by it, and whether that view carries one cannot be proved from this environment | Open | Found while repairing the two location-picker selects in PKG-ELITE-E1 slice C. Both `dashboard/new/page.tsx` and `dashboard/listings/[id]/page.tsx` ran `.select("id, name_en, name_ar, city, lat, lng").order("city")` against `districts_geo`, while the public listings page has read the city from `districts` and joined it since PKG-NM1, with a comment saying in as many words that `districts_geo` carries no city. Both cannot be right. The honest statement of what is known: it is UNPROVEN, not proven either way. Every Supabase read available here is permission denied, `execute_sql`, `list_tables`, `get_advisors` and `generate_typescript_types` alike, and both dashboard routes are session gated while `web_fetch_vercel_url` is GET only and unauthenticated, so neither the schema nor the running behaviour can be observed from this environment. It is recorded rather than asserted for that reason, and graded P2 rather than higher because the repair does not depend on the answer. If the column does not exist PostgREST returns an error for the whole select and the picker silently receives an empty list, which is a lister staring at a map that can offer no location; if it does exist the old select was merely duplicating a column. Both selects now read `districts_geo` for the point and the kind only and join the city from `districts`, which is correct under either answer and matches the pattern the public map already uses. What would close this: one authenticated load of either dashboard route with the network response inspected, or any working schema read. Recorded here so the repair is not mistaken for a proof. |
| 139 | P1 | The language segment control in the shared header sits far below the 44px SAT touch floor | Closed in PKG-A11Y-1 | The reported control was real and the measured height corroborates the arithmetic in the original note exactly: with touch emulated, `.lang-seg` rendered at 26px. But the language segment was never the cause. The cause is that the coarse-pointer floor at `src/styles/sat-platform.css` is an enumeration of selectors, and an enumeration is a list of the controls somebody remembered; every defect ever reported against it has been the same defect, a control that was not on the list. RC5 therefore rewrote the enumeration rather than adding one selector, and four distinct gaps were measured and closed. ONE. `.btn` matched the base class only, while `.btn-gold`, `.btn-ink` and `.btn-ghost` are used alone at several call sites (the header's list-your-space link, the 404 return, the auth-callback return), so a control that looks exactly like a button was never seen by the floor. TWO. `min-height` has no effect on a non-replaced inline box, and two of the three bare `.btn-gold` anchors are inline, as is every anchor in a footer column; the floor would have been declared and done nothing, which reads as done and is worse than absent. Those families now also receive `display:inline-flex` with centred alignment, which is what stops a 44px box rendering its label jammed against the top edge. THREE. The icon group set `min-width` without `min-height`, so a 34px square icon link became 44 wide and still 34 tall. FOUR. The site header's own controls are Tailwind-only and carry no floor-bearing class at all, so its nav links and account-menu rows resolved to about 36 and 37px while the menu trigger beside them, being a `button`, was already 44; `.site-header a` is the structural cover, and the header is uniform on touch after this change where it was inconsistent before. The footer link-column floor is declared in `src/styles/footer.css` rather than in the platform stylesheet, because `.foot .foot-col a` is (0,2,1) and `footer.css` is imported last in `src/app/layout.tsx`; an equally specific rule written earlier loses the `display` declaration on source order and the floor would be inert. Finding 86 is the recorded precedent for that exact mistake. The contrast half of the control was fixed in the same pass: the inactive segment was `rgba(28,33,38,0.55)`, which composites over the `#EEF1F5` pill to 3.57:1 and fails SC 1.4.3 at 12px 600, where the large-text allowance does not apply. It is now `/65`, the same muted-text floor RC2 settled on, which composites to 4.83:1, so no new grey enters the system. The active state, white on Harbor `#3A6EA5`, is 5.31:1. MEASURED, `scripts/touch-probe.mjs`, 224 control measurements, EN and AR, at 320, 360, 390, 430, 768 and 1280px, with `hasTouch` on and then off. Coarse pointer after RC5: every control 44 at every width in both locales, document overflow 0 everywhere. Fine pointer, unchanged by RC5 and therefore both the before values and the proof the desktop rendering did not move: `logo 34, nav 36.3, lang 26, btnink 37.5, trigger 36, menurow 37, btngold 35, btngoldib 40, footcol 29.5, soc 34`. So `lang 26 to 44, nav 36.3 to 44, btnink 37.5 to 44, btngold 35 to 44, footcol 29.5 to 44, menurow 37 to 44, logo 34 to 44`, and `soc 34 to 44 by 44` on both axes. GUARDED by `src/lib/touchTarget.test.ts`, five tests, which check the properties an enumeration must hold rather than the one control: every declared `.btn-*` variant is on the floor, every family that can render inline also receives a display, the icon group claims both axes, the header and footer columns are covered structurally, and the segment contrast is computed by compositing the declared rgba over the declared pill background rather than asserted. The guard was negatively controlled: removing `.btn-ghost` from the enumeration made the suite fail with the intended message, and the file was then restored and byte-compared. EVIDENCE CLASS: automated scan plus browser-emulated touch measurement. NOT tested on a physical device and NOT independently audited. `docs/a11y-p1-triage.md` records why that distinction matters here: a CSS `min-height` is evidence that a rule exists and not evidence that a thumb reaches it. Physical-device verification of 139 and 26 remains outstanding. |
| 140 | P1 | The `text-charcoal/35`, `/40`, `/45` and `/55` opacity series renders body text between roughly 2.22 and 3.96 to 1 on white | Closed in PKG-A11Y-1 | `charcoal` is declared once, as `#14181B`, at `tailwind.config.ts:20`. The four alpha steps are in live use across journey 1 and journey 2: `/35` at `src/app/[locale]/login/page.tsx:102`, `/40` at `src/app/[locale]/login/page.tsx:116` and `src/components/ListingCard.tsx:75`, `/45` at `src/components/ListingStudio.tsx:69` and `src/components/LocationPicker.tsx:154`, `/55` at `src/components/ListingStudio.tsx:552` and `src/components/Header.tsx:121`. The figures below are arithmetic on those declared token values and are not a measurement of rendered paint: each alpha was composited over `#FFFFFF` by the usual source-over blend, then the WCAG relative luminance and the `(L1+0.05)/(L2+0.05)` ratio were computed. `/35` composites to `#ADAEAF` and gives 2.22 to 1; `/40` to `#A1A3A4` and gives 2.53 to 1; `/45` to `#959798` and gives 2.93 to 1; `/55` to `#7E8082` and gives 3.96 to 1. Those agree with the recorded series of 2.24, 2.53, 2.97 and 4.0 to within compositing rounding. Every one of the four is text below 18.66px, so none of them is large text. Rule engaged: SC 1.4.3 contrast, minimum. Not fixed in this slice because it is a design token change that reaches surfaces this pass did not read, and it belongs to the parked visual package; the ELITE-4 record states the same reason in its own words at `docs/accessibility-elite-4.md`, "no new tokens and no colour sweep to chase contrast". **Corrected in RC15.** Closed by `beef75a`, which this row was never updated to record. None of `/35`, `/40`, `/45` or `/55` appears anywhere under `src` at `7aaab03`. The lowest charcoal step still in use is `/65`, which composites over `#FFFFFF` to `#66696B` and gives 5.53 to 1; `/70` composites to `#5B5D5F` and gives 6.61 to 1. Both clear SC 1.4.3 at any size. The fifth test in `src/lib/palette.test.ts`, "every charcoal opacity step used for text passes 4.5 to 1 on every surface it can sit on", recomputes this rather than trusting the sweep. Evidence class: automated. The ratios are arithmetic on the declared token values in the source at `beef75a`, recomputed at `7aaab03`, not a measurement of rendered paint, and `src/lib/palette.test.ts` recomputes them on every run so a future edit that reintroduces the step fails the suite. Not tested on a physical device, not tested with an actual screen reader and not independently audited. |
| 141 | P1 | The authentication split pane pins itself to `100vh` and nests a scroll region inside the page scroll at 400 percent zoom | Closed in PKG-A11Y-1 | `.auth-split{display:flex;min-height:100vh;}` at `src/styles/sat-platform.css:449`, with `.auth-form{flex:1;...;overflow-y:auto;...}` at `src/styles/sat-platform.css:451`. The pane is the whole of the signup surface, `src/app/[locale]/signup/page.tsx:17`. The `.auth-brand` half is dropped below 820px at `sat-platform.css:452`, but the `100vh` floor and the inner `overflow-y:auto` survive at every width, so at 400 percent zoom the form column becomes a second scroll container inside a viewport that is already only a few hundred CSS pixels tall, and the visitor scrolls one region to reach content while the document itself does not move. Rules engaged: SC 1.4.10 reflow and SC 1.4.4 resize text. Not fixed in this slice because removing the viewport floor and the nested scroller means replacing the split pane with a layout that reflows, which is a structural rewrite larger than a bug fix; the same `.auth-*` block is the shell for the login surface as well, so the change cannot be scoped to the one route that was audited. Closed in PKG-A11Y-1 (RC7 slice F). `.auth-form` no longer declares `overflow-y:auto`, so the nested scroll container is gone: the pane is a minimum and content taller than it grows the document, which is the thing that scrolls. `min-width:0` is added so the flex child can shrink below its content, and the floor is now written twice, `min-height:100vh` then `min-height:100svh`, so a browser that understands the small-viewport unit cannot make the pane taller than the visible screen while an older one keeps the previous behaviour. The centring is safe against a minimum rather than a fixed height, so the tall case grows downward instead of centring content off the top edge. Correction to this finding as filed: it states that the same `.auth-*` block is the shell for the login surface as well, and that is not true. `grep -rn "auth-split" src` returns exactly one consumer, `src/app/[locale]/signup/page.tsx:17`; login renders `<section className="mx-auto max-w-md py-14">` around a `.card p-7` and never touches these rules. The blast radius was one route, not two, and the scoping objection recorded as the reason for deferral therefore did not apply. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 142 | P2 | The fixed bottom tab bar permanently consumes roughly 61px of viewport at high zoom; its items are not below the touch floor | Open | Both halves of the recorded description were checked and only one holds. `.tabbar` at `src/styles/globals.css:233` declares `position:fixed;bottom:0;left:0;right:0` with `padding:5px 6px calc(5px + env(safe-area-inset-bottom))` and a 1px `border-top`; `.tabbar a` at `src/styles/globals.css:234` declares `min-height:50px`, and the icon slot `.tabbar .tb-ic` at `src/styles/globals.css:238` is `width:44px;height:28px`. 50px is above the 44px SAT floor, so the claim that the items are below the touch floor is NOT true and is not asserted here. What is true is the second half: the computed bar is 5px plus 50px plus 5px plus a 1px border, roughly 61px, which is what `main.has-tabbar` reserves as 62px at `src/styles/globals.css:241`, and the bar is suppressed only by `@media(min-width:1024px)` at `src/styles/globals.css:240`. At 400 percent zoom on a 1280px display the CSS viewport is 320px wide and roughly 200px tall, the query does not match, and a fixed 61px bar occupies close to a third of the remaining height on every page that renders `src/components/TabBar.tsx:36`. Rule engaged: SC 1.4.10 reflow. Not fixed in this slice because the correct remedy depends on evidence not available here: no development server and no browser were reachable in this container, so the actual reflow at 200 and 400 percent was never rendered, and whether the bar should collapse, shrink or unpin is a navigation decision that the design-partner round is meant to answer. |
| 143 | P1 | The dashboard rail is an `<aside>` with no `<nav>` landmark and no accessible name | Closed in PKG-A11Y-1 | `src/app/[locale]/dashboard/layout.tsx:83` opens `<aside className="dside">`, and the only navigation inside it, `DashNav`, renders `<div className="dnav">` at `src/components/DashNav.tsx:24`. There is no `<nav>` and no `role="navigation"` anywhere between them, and the `<aside>` carries no `aria-label`, so it exposes a complementary landmark rather than a navigation one. The eight primary destinations for a signed-in lister are passed in at `dashboard/layout.tsx:87` to `96` and are the only way to reach listings, enquiries, viewings, requirement matches and the profile, so a screen-reader user jumping by landmark finds no navigation region on any dashboard page. The individual links are correct otherwise: `DashNav.tsx:29` already sets `aria-current="page"`. Rule engaged: SC 1.3.1 info and relationships, with SC 2.4.1 bypass blocks as the practical consequence. Not fixed in this slice because the rail is the shared chrome for every page under `/dashboard`, and changing the landmark structure of that shell reaches the listings, enquiries, viewings, requirements and profile surfaces at once, which is a structural change larger than a bug fix and wider than the two journeys audited. Closed in PKG-A11Y-1 RC6. The rail is now `<nav className="dside" aria-label={db.navRailLabel}>` at `src/app/[locale]/dashboard/layout.tsx:91`, with a new `dashboard.navRailLabel` string in both dictionaries ("Dashboard" and "لوحة التحكم"), so the region announces as a dashboard navigation beside the site header's existing "Primary". The landmark was placed on the rail itself rather than wrapped around `DashNav` alone, and that was a decision rather than convenience: the rail also holds the brand link at `layout.tsx:92`, the account and billing link at `107` and the sign-out control at `112`, so wrapping only the item list would have left those three outside every region on every dashboard page while fixing the eight destinations that were already easiest to find. `DashNav` therefore declares no `<nav>` of its own and the guard asserts that it does not: a `<nav>` inside a `<nav>` is two navigation landmarks over one set of destinations, and the inner one would be unnamed, which is worse than none because it is announced and says nothing. The item list is also deliberately not a `<ul>`. Below 900px `.dnav` becomes a horizontal scroller, `flex-direction:row` with `flex:none` on each link at `src/styles/sat-platform.css:414` to `424`, and a list wrapper only preserves that layout through `display:contents`, which is the pattern known to strip list semantics in some assistive technology; the trade buys an item count and risks the semantics it was added for. Every destination is already a link inside a named region, and the current one carries `aria-current="page"` at `src/components/DashNav.tsx:43`. VISUALLY INERT, MEASURED RATHER THAN ASSERTED: a before and after harness rendered the old shell, markup and CSS taken from `git show HEAD`, and the new one in the same browser context at 320, 390, 430, 768 and 1280px in `ltr`/`en` and `rtl`/`ar`, injecting the next/font custom properties first so computed families resolve to named tokens instead of collapsing to an initial value. The only difference reported on the rail at any width in either locale is the tag name, `aside` to `nav`. `display:flex`, `flex-direction` (row below 900px, column at 1280), width (full width on mobile, 240px at 1280), `background rgb(20, 24, 27)` and padding (`8px 12px` on mobile, `22px 16px` on desktop) are identical, and document overflow is 0 at every width. GUARD: `src/lib/dashboardHeadings.test.ts`, four tests, registered in `package.json`. It asserts exactly one `<nav>` in the layout, that it carries `className="dside"` and an `aria-label`, that `DashNav` declares none, and that the label exists in both dictionaries and is written in Arabic script in `ar.json`. It reads the sources with comments stripped, because these files explain their own structure and the explanations quote the markup they are about, so a raw scan fails the file for documenting itself accurately. NEGATIVE CONTROL: reverting `<nav>` to `<aside>` fails the first test; the file was restored from backup and the restore verified. EVIDENCE CLASS: automated scan plus browser emulated measurement, EN and AR, five widths. NOT tested with an actual screen reader and NOT independently audited; how the region is announced remains outstanding and is recorded as such in `docs/a11y-p1-triage.md`. |
| 144 | P2 | Two competing `<h1>` elements render together on the onboarding surfaces | Open | `src/app/[locale]/dashboard/layout.tsx:110` renders `<h1>{acctName}</h1>` in the `.dtopbar` of the shared shell, and the profile surface that completes organization onboarding renders its own `<h1>` at `src/app/[locale]/dashboard/profile/page.tsx:54`. Both are in the document at the same time, so the surface announces two top-level headings and neither one is the page title in the sense a heading-navigation user expects: the shell heading names the account, the page heading names the task. `ProfileForm.tsx` itself is clean and adds no third heading. Rule engaged: SC 1.3.1 info and relationships, with SC 2.4.6 headings and labels as the practical consequence; no conformance is asserted either way, since a second `<h1>` is not by itself a stated failure of any single criterion. Not fixed in this slice because it is a heading-hierarchy change that alters shared chrome: the offending `<h1>` lives in the dashboard layout, so demoting it changes the heading outline of every page under `/dashboard`, including the six this pass did not read. |
| 145 | P1 | The three-step signup progress indicator states the current step by colour alone | Closed in PKG-A11Y-1 | `src/components/SignupFlow.tsx:120` renders the indicator as three bare `<span>` elements, `{[0, 1, 2].map((i) => <span key={i} style={{ height: 4, borderRadius: 2, flex: 1, background: i <= step ? "var(--harbor)" : "var(--silver)" }} />)}`. The only difference between a completed step and a pending one is the fill, `--harbor` against `--silver`; there is no text, no `aria-*`, no `role="progressbar"` and no `aria-current`, and the spans are 4px tall with no content, so a screen reader has nothing to read and a user who cannot separate the two fills has nothing to see. The step number does exist in state at `SignupFlow.tsx:19` and drives the panels at `SignupFlow.tsx:123`, `135` and `163`, but it is never written out. Rule engaged: SC 1.4.1 use of colour, with SC 1.3.1 for the missing programmatic state. Not fixed in this slice because the correct remedy depends on evidence not available here: whether the flow should gain a written "Step 2 of 3" line, a `progressbar` with `aria-valuenow`, or a labelled `ol` of steps is a choice about the shape of the onboarding flow rather than a defect with one obvious repair, and no participant evidence about how the three steps are actually read was available. Note that the Listing Studio solved the same problem differently at `src/components/ListingStudio.tsx:937` and `942`, so the two onboarding flows would need reconciling rather than patching. Closed in PKG-A11Y-1 slice J, RC9b. The deferral above was right that the remedy was a choice and wrong that the choice needed participant evidence: the register itself named the reconciliation, that Listing Studio already solved the same problem, and reconciling with an existing solved surface is not a matter of taste. The signup flow now carries the same shape the Studio carries, a step name on the start side and `Step {step + 1} of 3` / `الخطوة {step + 1} من 3` on the end side, both at 0.75rem, above the bar rather than inside it. The three bars became `aria-hidden="true"`, because once the count is text beside them they are a second announcement of the same fact and, empty, they were never anything but noise in the accessibility tree. The step names are taken from what each step actually asks, `Your role` / `دورك`, `About your work` / `عن عملك` and `Your details` / `بياناتك`, because a count says how far and not what of. Western numerals in both languages. One design constraint is recorded here because it is not visible in the markup: the step name is NOT an `<h2>`. `src/styles/sat-platform.css:638` sets `h2 { font-size: clamp(1.3125rem, 6.6vw, 1.875rem) !important; }` inside `@media (max-width: 600px)`, an unscoped `!important` of the same class as finding 194, and an inline `style` cannot outrank it, so a heading here would render at 21 to 30 pixels on a phone directly above a 4 pixel bar. The instruction is to resolve the interaction properly rather than spend the design on the semantics, so the step name is a styled `<span>` and the naming role is carried by the focusable group described in finding 199. Guard tests in `src/lib/formGroups.test.ts` assert the count text in both languages, the `STEP_NAMES` array, the `aria-hidden` on the bar row and the absence of a heading, the last two paired deliberately so that hiding the bars cannot survive the removal of the text. Evidence classification: automated source scan and guard tests. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 146 | P2 | The dashboard rail positions its unread badge with physical `margin-left`, which pushes it to the wrong edge in Arabic | Open | `.dnav a .badge{margin-left:auto;...}` at `src/styles/sat-platform.css:326`, repeated as `.dnav a .badge{margin-left:6px;}` in the narrow-width block at `src/styles/sat-platform.css:386`. `.dnav a` is a flex row, so in the Arabic build, where `HtmlLangDir` sets `dir="rtl"`, the main axis start is the right edge and `margin-left:auto` puts the free space on the left of the badge, holding the badge at the inline start beside the label instead of pushing it to the inline end. The badge carries the unread enquiry, viewing and requirement counts passed in at `src/app/[locale]/dashboard/layout.tsx:90` to `92`, so the count an Arabic lister most needs to find sits in the position an English lister's label occupies. The same stylesheet already uses the logical form correctly eleven lines later, `.dside .me .chev{margin-inline-start:auto;...}` at `src/styles/sat-platform.css:336`, which is the pattern the fix would follow. Rule engaged: SC 1.3.2 meaningful sequence, as the visual order stops matching the reading order under RTL. Not fixed in this slice because these two declarations are part of a wider set of physical properties still present in the platform stylesheet, and correcting one in isolation changes the rail on every dashboard page while leaving the rest inconsistent; the sweep reaches surfaces this pass did not read. |
| 147 | P1 | `ChromeGate` classes `/signup` as an APP route, so the marketing header and footer are withheld from a route a signed-out visitor reaches | Closed in PKG-A11Y-1 | RC13. The tier was decided by two bare alternations in `src/components/ChromeGate.tsx:19-20` sitting under a comment that said APP means "routes that carry their own navigation rail". `/signup` carries no navigation of any kind, so the comment and the list contradicted each other, and no test could read the comment. Consequences established from source before the fix: no header meant no `LanguageSwitch`, which lives only at `Header.tsx:88` and `:149`, so `/en/signup` and `/ar/signup` had no route between them; no footer meant no `SatFooter` and no `TabBar`, and at 820px and below `.auth-brand{display:none}` (sat-platform.css) also removes the logo, leaving the page with exactly one link on it, the sign-in link; and `/login`, the other half of the same journey, is MARKETING with full chrome, so a visitor crossing between them gained and lost navigation halfway through. SC 2.4.5, SC 3.2.3. Fix: the tier tables move to `src/lib/chrome.ts` as records keyed by route with a written reason per entry, `signup` is absent from APP so it falls to the MARKETING default, and `ChromeGate` now calls `chromeTier()` instead of holding a second copy of the rule. `src/lib/chromeGate.test.ts` holds nine guards, including that `/signup` and `/login` share a tier and that the tier is marketing, in both locales. Evidence: automated source scan and guard tests, 1664 passing; typecheck clean; ar-lint clean; prose GATE 0; reflow probe PASS at 14 renders EN and AR; radio probe PASS. Not tested on a physical device, not tested with an actual screen reader and not independently audited. WCAG 2.2 AA conformance is not claimed from this. |
| 148 | P1 | The inventory table is given `display:block`, which drops its table semantics at the narrow widths a phone uses | Closed in PKG-A11Y-1 | The recorded description points at `src/styles/globals.css` "around lines 376 and 380" and at a 1024px breakpoint; neither could be confirmed at `ae7b198`. `globals.css:376` is `.advpanel .grab` and `globals.css:380` is blank, and no rule in any stylesheet drops table semantics at 1024px. The defect itself is real and was relocated: `table.dt{display:block;overflow-x:auto;-webkit-overflow-scrolling:touch;max-width:100%;}` at `src/styles/globals.css:392`, inside the mobile fit guard `@media(max-width:920px)` opened at `src/styles/globals.css:388`. So the true breakpoint is 920px and below, not 1024px. The inventory table is the `<table className="dt">` at `src/app/[locale]/dashboard/listings/page.tsx:134`, whose `<thead>` at `135` declares the listing, enquiry count, status and action columns. Setting `display:block` on the `<table>` element removes the table role from the accessibility tree while the descendant `thead`, `tr`, `th` and `td` keep their `display:table-*` values, so the header cells stop being associated with the data cells and a screen-reader user reading a lister's whole inventory at phone width gets an undifferentiated run of text with no column context. Rule engaged: SC 1.3.1 info and relationships. Not fixed in this slice because the repair is a structural rewrite larger than a bug fix: `table.dt` is a platform class used by more than the inventory surface, so either the mobile fit guard is replaced by a wrapper-based scroll pattern everywhere it applies, or the table is given an explicit `role="table"` chain on every descendant, and both reach surfaces this pass did not audit. **Closed in PKG-A11Y-1 slice O (RC11).** The first option, taken properly. `table.dt{display:block;...}` is deleted from the mobile fit guard in `src/styles/globals.css` and replaced by a comment recording what it did and why it went; nothing sets `display` on a `.dt` table at any width now, so the table role survives to 320px and the header cells stay associated with the data cells. The reason the deletion is safe is that overflow is no longer the table's job at all: `.scrollx` in `src/styles/sat-platform.css` declares `overflow-x:auto;max-width:100%;min-width:0` unconditionally, at the top level rather than inside a media query, because content with a `minWidth` overflows whenever its column is narrower than that, which a desktop split view does as readily as a phone. The old rule only scrolled below 920px, so between 920px and the table's own `minWidth` the excess was clipped by `html,body{overflow-x:clip}` and unreachable. The systemic scope is the point: this was not repaired on the inventory table, it was repaired on all 21 data tables in the platform at once, plus two non-table scrollers, and a guard test now fails if any file outside three named exemptions declares a bare `overflowX: "auto"` or `overflow-x-auto`. Evidence classification: automated source and stylesheet scan (8 new tests in `src/lib/formGroups.test.ts`), plus browser-emulated measurement in Chromium at 7 viewports from 320x256 to 1280x1024 in EN and AR, whose track widths are numerically identical to the RC7 baseline, which is the evidence that deleting `display:block` and adding a caption to every table moved no layout. Not tested on a physical device, not tested with an actual screen reader, not independently audited. WCAG 2.2 AA conformance is not claimed from this. |
| 149 | P1 | The inventory table's horizontal scroll wrapper cannot be reached or scrolled by keyboard | Closed in PKG-A11Y-1 | `src/app/[locale]/dashboard/listings/page.tsx:133` opens `<div style={{ overflowX: "auto" }}>` and `page.tsx:134` puts a `<table className="dt" style={{ minWidth: 640 }}>` inside it. The wrapper carries no `tabIndex={0}`, no `role="region"` and no `aria-label`, so it is not in the tab order and cannot take arrow-key scrolling. Because the table declares `minWidth: 640`, the wrapper genuinely overflows at every viewport narrower than roughly 640px plus the `.dbody` padding declared at `src/styles/sat-platform.css:354`, which is the common case on a phone. A keyboard-only or switch user can tab through the links inside the table and be scrolled by focus, but cannot pan the region to read the enquiry count and status columns without focusing something in them, and there is nothing focusable in the enquiry-count column at all. Rules engaged: SC 2.1.1 keyboard and SC 1.4.10 reflow. Not fixed in this slice because adding `tabIndex={0}` inserts a new stop into the tab order of the inventory page and requires a name for it, which changes the focus order the rest of this pass was reasoning about, and the same wrapper pattern appears on other data surfaces; that is a structural change larger than a bug fix. **Closed in PKG-A11Y-1 slice O (RC11).** Both halves of the recorded blocker were true, and both were true only when the region actually scrolls, which is what made the repair possible without disturbing the focus order this pass was reasoning about. `src/components/ScrollRegion.tsx` measures itself on mount, on its own resize and on its content's resize, and applies `tabIndex={0}`, `role="region"` and `aria-label` only while `scrollWidth - clientWidth > 1`, which is the same evidence the browser uses to decide whether to paint a scrollbar. On a desktop where the inventory table fits, the page's tab order is exactly what it was; on a phone where it does not, there is one new stop, and it is named. One device pixel of tolerance keeps a fractional layout width from making the stop appear at some zoom levels and not others. The server render is always the non-scrollable form, so the first client render matches it and hydration is quiet, and the failure direction is deliberate: a region that should be a tab stop and briefly is not is recoverable, a tab stop announced on a region that does not scroll is a claim about a measurement nobody took. This is the reasoning finding 200 settled for the sticky rail, applied again: let the layout decide, not the markup. The focus ring is inset (`outline-offset:-2px`) because most of these wrappers sit inside a `.card` with `overflow:hidden`, which would clip an outset one. The "same wrapper pattern appears on other data surfaces" half is answered by converting every one of them: 21 tables across 17 files, plus the `/compare` comparison grid (`minWidth: 260 + items.length * 200`) and the `/deal` stepper (`minWidth: 460`), neither of which is a table and both of which carried the identical defect. That is why the component is named for the box rather than the content. Three bare scrollers remain and are exempt by name with reasons, each verified by reading it: the `MarketingHome` hero asset rail (buttons, with explicit `aria-label`led previous and next controls), the two `MapExplorer` rails (buttons and links only), and the `ListingStudio` step rail (buttons inside a named `<nav>`). All three are pannable by keyboard through focus movement, which a table cell containing plain text is not. Evidence classification: automated source scan; the conditional-attribute logic is asserted at source level rather than measured in a browser, because no dev server is reachable from this environment. Not tested on a physical device, not tested with an actual screen reader, not independently audited. WCAG 2.2 AA conformance is not claimed from this. |
| 150 | P1 | Listing Studio helper text uses the `/45` charcoal step, roughly 2.93 to 1 on white | Closed in PKG-A11Y-1 | `const help = "text-[11px] text-charcoal/45 mt-1";` at `src/components/ListingStudio.tsx:69`. That single constant is applied to the helper line under most gated fields in the studio, including `ListingStudio.tsx:660` on the floor-plan step, `ListingStudio.tsx:587` on the document step and `ListingStudio.tsx:1022` on the asset step, and the same step is used directly at `ListingStudio.tsx:1004` for the sentence that explains what the asterisk on a required field means. `charcoal` is `#14181B` at `tailwind.config.ts:20`. The ratio is arithmetic on that declared token and not a measurement: `#14181B` at alpha 0.45 composited over `#FFFFFF` gives `#959798`, whose WCAG relative luminance against white yields 2.93 to 1, agreeing with the recorded 2.97 to within compositing rounding. The text is set at 11px, so it is nowhere near the large-text threshold. Rule engaged: SC 1.4.3 contrast, minimum. Not fixed in this slice because it is the same design token change as finding 140: the `/45` step is used on eleven files this pass read and an unknown number it did not, so repainting it is a token sweep belonging to the parked visual package rather than a bug fix. **Corrected in RC15.** Closed by `beef75a` as part of finding 140's root cause; this row was never updated. The constant now reads `const help = "text-[0.6875rem] text-charcoal/65 mt-1";` at `src/components/ListingStudio.tsx:70`, which is 5.53 to 1 on white, and the sibling `lbl` constant on line 69 is `/70` at 6.61 to 1. The size also moved from a hardcoded 11px to `0.6875rem`, so it now answers a browser text-size setting, which was finding 174. Evidence class: automated. The ratios are arithmetic on the declared token values in the source at `beef75a`, recomputed at `7aaab03`, not a measurement of rendered paint, and `src/lib/palette.test.ts` recomputes them on every run so a future edit that reintroduces the step fails the suite. Not tested on a physical device, not tested with an actual screen reader and not independently audited. |
| 151 | P2 | `table.dt` aligns its cells with physical `text-align:left` and `right` instead of logical values | Open | `table.dt th,table.dt td{text-align:left;padding:12px 16px;font-size:13px;border-top:1px solid var(--silver);}` at `src/styles/sat-platform.css:474`, and `table.dt td.num{font-family:var(--mono);text-align:right;font-variant-numeric:tabular-nums;}` at `src/styles/sat-platform.css:476`. Neither is overridden for RTL anywhere: the only `[dir="rtl"] table.dt` rule in the stylesheet is `src/styles/sat-platform.css:695`, which sets `font-family`, `letter-spacing` and `text-transform` on `thead th` and touches no alignment. `table.dt` is the inventory table at `src/app/[locale]/dashboard/listings/page.tsx:134`, so in the Arabic build every header and body cell is flushed to the left of its column while the text reads right to left, and the numeric column is flushed to the right, which is the reverse of what the logical properties would produce. Rule engaged: SC 1.3.2 meaningful sequence. Not fixed in this slice because these are two declarations in a wider set of physical properties still present in the platform stylesheet, alongside the rail badge in finding 146 and the geocode list in finding 152; changing `table.dt` alone alters every surface that renders that class, including several this pass did not read, so it belongs to the physical-to-logical sweep rather than to a bug fix. |
| 152 | P2 | The geocode result list is aligned with physical `text-left` instead of a logical property | Open | `src/components/LocationPicker.tsx:146` gives each result button `className="block w-full text-left px-3 py-2 hover:bg-ivory-2 text-[13px]"`. Tailwind compiles `text-left` to a literal `text-align:left`, so it does not follow direction. The results are place names returned by `/api/geocode` at `LocationPicker.tsx:123` and are rendered with an Arabic label and an Arabic sub-line in the Arabic build, `LocationPicker.tsx:147`, inside the location step of the Listing Studio. The neighbouring padding is already handled correctly by symmetric `px-3`, so the alignment is the one physical value left in the rule. Rule engaged: SC 1.3.2 meaningful sequence. Not fixed in this slice because it is one instance of the same physical-to-logical sweep as findings 146 and 151, and correcting a single utility class in one component while the platform stylesheet still carries the rest leaves the inconsistency in place across surfaces this pass did not read. |
| 153 | P1 | The geocode result list carries no combobox or listbox semantics, so suggestions are never announced as suggestions for the input above them | Closed in PKG-A11Y-1 | `src/components/LocationPicker.tsx:141` is a plain `<input>` with an `aria-label` and nothing else: no `role="combobox"`, no `aria-expanded`, no `aria-controls`, no `aria-autocomplete` and no `aria-activedescendant`. When results arrive, `LocationPicker.tsx:143` to `149` renders them as an absolutely positioned `<div>` of `<button>` elements with no `role="listbox"`, no `role="option"`, no id and no live region. The consequence is that the list appears and disappears silently, `LocationPicker.tsx:146` sets `setResults([])` on choice with nothing announced, and a screen-reader user typing into the search box is given no indication that anything was offered; the buttons are reachable only by tabbing past the input into what reads as an unrelated group of unnamed controls. Rules engaged: SC 4.1.2 name, role, value and SC 4.1.3 status messages. Not fixed in this slice because implementing the combobox pattern properly means adding managed ids, `aria-activedescendant`, arrow-key handling that respects direction, and a decision about whether the list should be a listbox or a live-announced set of buttons; that is a structural rewrite larger than a bug fix, and it sits on the same component as the map in finding 160. Split out to PKG-A11Y-1 slice H in RC8 slice G and recorded as open, not excused. Slice G converted every other caption-attached-to-nothing site in the four critical journeys, and this one is deliberately the survivor: it is the only bare `<label>` remaining anywhere under the journey scan, and it is exempted by name in `src/lib/formGroups.test.ts` with the reason, so the guard passes while still pointing at the open work rather than forgetting it. The caption at `src/components/EditListingForm.tsx:350` cannot become a `htmlFor` or a legend the way the others did, because there is no control for it to point at until the picker grows the combobox structure this finding describes. Slice H does that work. **Closed in PKG-A11Y-1 slice H.** `src/components/LocationPicker.tsx` now implements the APG combobox pattern in full. The input at `:214` carries `role="combobox"`, `aria-expanded`, `aria-controls`, `aria-autocomplete="list"` and `aria-activedescendant`, and the results are a `<ul role="listbox">` at `:241` of `<li role="option">` with `aria-selected`, replacing the `<div>` of `<button>` elements. The options are list items rather than buttons on purpose: in this pattern focus stays in the input and the active option is named rather than focused, so a focusable option would put the same choice in the tab order twice and take the caret out of the box the lister is typing in. The pointer path uses `onMouseDown` with `preventDefault()` so that it fires before the input's blur and ends in the same `choose(i)` call at `:173` the keyboard ends in, which is what keeps the two paths from drifting apart. SC 4.1.3 is answered by an always-present `role="status" aria-live="polite"` region at `:231`; it is in the tree from first render rather than added with its text, because a region created at the same moment as its content is often not announced. The original note asked for "arrow-key handling that respects direction" and the answer is narrower than it sounds. A vertically stacked listbox has no direction, so ArrowDown, ArrowUp, Home and End are correct unchanged in both languages; the direction-aware part is `onKey` at `:190` deliberately NOT intercepting ArrowLeft and ArrowRight, because inside a text box those move the caret, they already reverse themselves under `dir="rtl"`, and taking them would break ordinary Arabic editing to serve a list the lister may not be looking at. The guard asserts both halves. With the picker's search box a real control, both captions that named nothing could become legends: `src/components/EditListingForm.tsx:357` is now `<fieldset style={fset}><legend style={lbl}>{t.loc}</legend>`, and `src/components/ListingStudio.tsx:703` is a `<fieldset>` whose legend replaces a `<div>` carrying a label class. The caption could never have become a `htmlFor`, and that is the reason it survived slice G: the picker is not one control, it is a search box, a map, a latitude and a longitude, and a caption over a set of controls is a legend. One deliberate subtraction at the Studio site: `flagFor("coordinates")` returned `aria-invalid`, which is not a supported attribute on `role="group"`, so only `aria-describedby` is carried onto the fieldset. Nothing is lost, because `aria-invalid` was equally inert on the `<div>` it used to sit on; the invalid state lives on the controls and the group now points at the error message that named it. The exemption in `src/lib/formGroups.test.ts` is gone rather than re-pointed, and `BARE_LABEL_EXEMPT` is now empty, which is the evidence that this was the last bare caption under the journey scan. `src/components/LocationPicker.tsx` was added to that guard's `JOURNEYS` list, so the component is now scanned rather than merely excused. Five new tests cover the combobox attributes, the list being driven from the input rather than from the tab order, the arrow keys taken and the two deliberately not taken, the finding 196 suppression, and the legend at both call sites. Gates at closure: typecheck clean, 1593 tests passing, `ar-lint: clean`, prose GATE 0 in 0 files, and `scripts/reflow-probe.mjs` returning PASS over 14 viewport renders with track widths numerically identical to the RC7 baseline, which is the evidence that two more div-to-fieldset conversions moved no layout. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 154 | P1 | The dashboard rail's section labels use `#6B7480` on the dark rail, roughly 3.77 to 1 | Closed in PKG-A11Y-1 | `.dnav .sec{font-family:var(--mono);font-size:9.5px;letter-spacing:.16em;color:#6B7480;text-transform:uppercase;padding:18px 12px 6px;}` at `src/styles/sat-platform.css:328`. The background is the rail itself, `.dside{...background:var(--ink);...}` at `src/styles/sat-platform.css:317`, and `--ink:#14181B` is declared at `src/styles/sat-platform.css:6`. The ratio is arithmetic on those two declared values and is not a measurement: WCAG relative luminance of `#6B7480` against `#14181B` computes to 3.77 to 1. That differs slightly from the 3.88 recorded in the ELITE-4 write-up; the figure stated here is the one that follows from the tokens actually read in the source at `ae7b198`. The label is set at 9.5px with `letter-spacing:.16em`, so it is nowhere near the large-text threshold, and the class is in live use: `src/components/DashNav.tsx:27` renders it for the "account" section passed in at `src/app/[locale]/dashboard/layout.tsx:93`. The same hex is the `--slate-2` token at `src/styles/sat-platform.css:8`. Rule engaged: SC 1.4.3 contrast, minimum. Not fixed in this slice because it is a design token change, `#6B7480` is `--slate-2` and is used well beyond the rail, so repainting it reaches surfaces this pass did not read and belongs to the parked visual package. **Corrected in RC15.** Closed by `beef75a`; this row was never updated. The rule now reads `.dnav .sec{font-family:var(--mono);font-size:0.59375rem;letter-spacing:.16em;color:#8A93A0;...}` at `src/styles/sat-platform.css:368`, and `#8A93A0` on `--ink:#14181B` computes to 5.75 to 1, up from 3.77. The value chosen is the existing `--slate-2` token rather than a new one, so the rail's labels and the platform's secondary text step stay the same colour. Evidence class: automated. The ratios are arithmetic on the declared token values in the source at `beef75a`, recomputed at `7aaab03`, not a measurement of rendered paint, and `src/lib/palette.test.ts` recomputes them on every run so a future edit that reintroduces the step fails the suite. Not tested on a physical device, not tested with an actual screen reader and not independently audited. |
| 155 | P1 | Every dashboard page renders two `<h1>` elements, one from the shell and one from the page | Closed in PKG-A11Y-1 | `src/app/[locale]/dashboard/layout.tsx:110` renders `<h1>{acctName}</h1>` inside `.dtopbar`, and it wraps every route under `/dashboard`. Each of those routes then renders its own `<h1>`: `src/app/[locale]/dashboard/listings/page.tsx:119`, `src/app/[locale]/dashboard/enquiries/page.tsx:84`, `src/app/[locale]/dashboard/enquiries/[id]/page.tsx:88`, `src/app/[locale]/dashboard/requirements/page.tsx:176`, `src/app/[locale]/dashboard/profile/page.tsx:54`, `src/app/[locale]/dashboard/new/page.tsx:144` and `src/app/[locale]/dashboard/listings/[id]/page.tsx:253`. On the inventory surface in particular the first `<h1>` a heading-navigation user reaches is the account name, not "My listings", so the shell heading is repeated identically on every page and carries no information about where the user is. Rule engaged: SC 1.3.1 info and relationships, with SC 2.4.6 headings and labels as the practical consequence; no conformance is asserted, since a duplicated `<h1>` is not by itself a stated failure of a single criterion. Not fixed in this slice because it is a heading-hierarchy change that alters shared chrome: the duplicate lives in the dashboard layout, so demoting it rewrites the heading outline of all seven pages above at once, four of which this pass did not read. Closed in PKG-A11Y-1 RC6. The shell heading is gone: `src/app/[locale]/dashboard/layout.tsx:124` now renders `<div className="acct font-display">{acctName}</div>`, and the guard asserts the dashboard layout contains no heading element at all. The account name is still the first thing in the topbar and is still in the rail's account link; what it stopped being is the answer to which page this is. `.font-display` was reused rather than a new font rule written, on purpose: `src/styles/globals.css:88` gives `h1,h2,h3,.font-display` the serif display face and `globals.css:82` gives `[dir="rtl"] h1, [dir="rtl"] h2, [dir="rtl"] h3, [dir="rtl"] .font-display` the Arabic face, so the element inherits exactly the machinery the `<h1>` was matching, in both scripts, and no new declaration exists to get the direction wrong. The obvious alternative, `.dtopbar .acct{font-family:var(--font-serif),Georgia,serif}`, is specificity (0,2,0) and would have beaten the Arabic rule, putting the Arabic account name into a Latin serif face. `src/styles/sat-platform.css:379` was rekeyed from `.dtopbar h1` to `.dtopbar .acct` because after the demotion no `h1` exists there. Removing the shell heading then exposed two routes that had none of their own: the overview at `src/app/[locale]/dashboard/page.tsx` had no heading whatsoever, and the viewings page had an `<h2>` on its populated branch and nothing at all on its empty one. Both now state their own title, and viewings holds it in a single `head` block used by both branches, so the page cannot title one state and leave the other silent; its empty state also gains the `<h2>` it never had. ONE DELIBERATE RENDERING CHANGE, MEASURED: `sat-platform.css:611` declares a bare `h1{font-size:clamp(1.625rem,8vw,2.5rem)!important}` inside its `@media(max-width:600px)` block, which had been inflating a compact topbar label to as much as 2.5rem on a phone. Before and after, in the same browser context, with the font custom properties injected so the family token is readable: at 768 and 1280px the account name is identical in both locales, 19px, weight 700, the serif token in `en` and the Arabic token in `ar`, which is the evidence that the display face survived the demotion; at 320, 390 and 430px it moves from 26px, 31.2px and 34.4px to the 19px its own rule declares, in both locales, with the font token unchanged. Its box height falls from 57.2, 34.3 and 37.8 to 28.5 in English, the 320 figure being the old label wrapping to two lines inside a 14px padded bar, and from 28.6, 34.3 and 37.8 to 26.6 in Arabic; its width from 288, 355.1 and 391.5 to 216.3 in English and from 278.4, 333.9 and 368.2 to 203.4 in Arabic. The phone rendering now matches the desktop declaration. That same bare rule still inflates every dashboard BODY page title on phones, which is pre-existing, consistent across those routes and not RC6's to change: it is recorded as finding 194 and assigned to the parked visual-quality package rather than repaired inside an accessibility slice, under the standing rule against another broad cosmetic sweep and Codex's instruction not to reduce visual quality to satisfy accessibility. GUARD: `src/lib/dashboardHeadings.test.ts` asserts no heading in the shell and exactly one `<h1>` in the source of each of the nine dashboard routes. It counts source rather than render deliberately, so that a page with two render branches cannot pass by stating a title on one of them. NEGATIVE CONTROL: restoring the shell `<h1>` and inlining a second one on the viewings branch fails both tests; both files were restored from backup and the restore verified by grep. EVIDENCE CLASS: automated scan plus browser emulated measurement, EN and AR, five widths. NOT tested with an actual screen reader and NOT independently audited. |
| 156 | P1 | The per-step state in Listing Studio is announced to a screen reader but is not written anywhere a sighted user can read it away from the step rail | Closed in PKG-A11Y-1 | Verified precisely, because the recorded description overstates the gap in one direction and understates it in another. What IS shown to a sighted user away from the rail: the position, `Step {step.index} of {steps.length}` at `src/components/ListingStudio.tsx:937`; the overall count, `{progress.answered} of {progress.askable} facts supplied` at `ListingStudio.tsx:940`; a `role="progressbar"` bar with `aria-valuenow` at `ListingStudio.tsx:942` to `952`; the current step title and purpose at `ListingStudio.tsx:1000` and `1001`; and, for a non-review step with askable fields, a per-step line at `ListingStudio.tsx:1102` to `1104` that prints both the count and the state word from `stateLabel`. What is NOT shown: the state of any step other than the one open. Inside the rail at `ListingStudio.tsx:967` to `987`, the state word is emitted only into a `<span className="sr-only">` at `ListingStudio.tsx:985`, and the sole visible marker is the `!` glyph at `ListingStudio.tsx:984`, which is `aria-hidden`, is rendered only when `p.state === "blocked"`, and is distinguished by the `text-red` colour class. So a screen-reader user hears "blocked", "partial" or "complete" for all steps, while a sighted user gets a red exclamation mark for blocked steps and nothing at all for the other states, and gets no state at all for any step once scrolled past the rail. Rules engaged: SC 1.4.1 use of colour for the blocked marker, and SC 1.3.1 for the states that have no visible expression. Not fixed in this slice because the correct remedy depends on evidence not available here: whether the missing states should become visible words on the rail chips, an inline summary above the panel, or nothing at all is a legibility judgement about a rail that already scrolls horizontally at narrow widths, and no participant evidence about how listers actually track studio progress was obtainable. Closed in PKG-A11Y-1 slice J, RC9b. The deferral above asked for a legibility judgement on a rail that already scrolls, and the judgement is that the rail is the right place, because the state belongs to the step and the chips are where the steps are. Each chip now renders `{p.answered}/{p.askable}` visibly and `aria-hidden`, beside the title, guarded by `p.askable > 0` so the review step does not print `0/0`. The count is the non-colour carrier the finding asked for: `0/5` reads as not started, `5/5` as done, anything between as part done, with no reliance on `border-signal` against `border-line` or on `text-signal` against `text-charcoal/70`. It is not a new vocabulary; it is the phrasing this component already prints twice, once for the whole listing and once for the current step, which is the reuse the deferral was holding out for. The `!` blocked marker stays, because a blocked step can be 4 of 5 and is therefore not separable from a merely unfinished one by the count alone. The `sr-only` span was extended rather than replaced, so a screen reader still gets the state word from `stateLabel` and now also the count in words, `, {n} of {m} facts supplied` and `، {n} من {m} حقيقة مُدخلة` with the Arabic comma; the visible `4/5` is hidden from it because a fraction read aloud is not what the mark means. Western numerals in both languages, which this platform requires regardless. Cost: roughly 28 pixels per chip on a rail that is already `overflow-x-auto` with `min-w-max`, so the distinction is bought with horizontal scrolling and not with layout, and the reflow probe reports the same fourteen viewport renders unchanged. Evidence classification: automated source scan and guard tests, plus the browser-emulated reflow probe as non-regression evidence. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 157 | P1 | Per-row `<select>` controls in Listing Studio repeat one identical accessible name across every row | Closed in PKG-A11Y-1 | `src/components/ListingStudio.tsx:578` gives every document-kind select `aria-label={t("Document type", "نوع المستند")}`, inside the `docFiles.map((file, i) => ...)` at `ListingStudio.tsx:575`, so uploading three deeds produces three controls all named "Document type". `src/components/ListingStudio.tsx:651` does the same with `aria-label={t("Plan type", "نوع المخطط")}` inside `floorFiles.map((file, i) => ...)` at `ListingStudio.tsx:648`. In both cases the only thing distinguishing the rows is the file name in the adjacent `<span>` at `ListingStudio.tsx:576` and `650`, which is not associated with the select by `aria-labelledby` or anything else, so a screen-reader user listing the form controls hears the same name repeated and cannot tell which uploaded file each select governs. The same defect class on buttons was repaired in this slice, which is why the pattern for the fix already exists in the file. Rules engaged: SC 4.1.2 name, role, value and SC 2.4.6 headings and labels. Not fixed in this slice because the correct remedy depends on evidence not available here: the disambiguator could be the row index, the file name, or an `aria-labelledby` pointing at the adjacent span, and the file name is user-supplied and can be empty, arbitrarily long or identical between two uploads, so which of the three actually helps is a question the design-partner round answers rather than one this pass can settle by reading source. Closed in PKG-A11Y-1 (RC8 slice G). Both sites now name the row rather than the column: `ListingStudio.tsx:585` reads ``aria-label={`${t("Document type", "نوع المستند")} ${i + 1}: ${file.name || t("unnamed file", "ملف بلا اسم")}`}`` and `ListingStudio.tsx:659` the same shape for plan rows. The deferral asked which of three disambiguators helps and said only the design-partner round could answer it. That framing was wrong in a way worth recording: the three candidates it listed are not alternatives, because the objections it raised against each are objections the others answer. The row index alone is uninformative, the file name alone can be empty, arbitrarily long or shared between two uploads, and `aria-labelledby` pointing at the adjacent span inherits every one of the file name's failure cases while adding a fragile id. Index and name together carry both properties at once: the index guarantees uniqueness in the empty and duplicate cases the deferral named, and the name carries which file this is. The ordinal is written in Western numerals in both languages per the platform rule, and the empty case falls back to a translated "unnamed file" rather than to a bare colon. Nothing here needed participant evidence; it needed the three options to be read as complements. Guarded by `src/lib/formGroups.test.ts`, which asserts both template literals verbatim and separately forbids any two controls in one journey file from sharing an `aria-label` literal. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 158 | P1 | Hardcoded `1fr 1fr` grid tracks in the listing edit form never collapse at narrow widths or high zoom | Closed in PKG-A11Y-1 | `src/components/EditListingForm.tsx:287` opens `<div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12 }}>` around the net area and price fields, and `src/components/EditListingForm.tsx:346` repeats the identical inline style around the contact phone and contact email fields. Both are inline styles, so no media query in `globals.css` or `sat-platform.css` can reach them; the mobile fit guard at `src/styles/globals.css:388` to `399` sets `min-width:0` on cards and rows but declares nothing that overrides an inline `grid-template-columns`, and the only `!important` track override in the file, `src/styles/globals.css:383`, is scoped to `.adv-jobs-grid`, a class these divs do not carry. There is no `minmax`, no `auto-fit` and no `repeat`, so at 320px CSS width, which is what a 1280px display gives at 400 percent zoom, two number inputs and their labels are forced to share the width in two fixed halves. Rule engaged: SC 1.4.10 reflow. Not fixed in this slice because collapsing the tracks means moving them out of inline styles into a class that a media query can reach, and this file styles itself entirely through the inline `lbl` and `inp` objects declared at the top of it; introducing a stylesheet dependency here is a layout rewrite larger than a bug fix. Closed in PKG-A11Y-1 (RC7 slice F). All four pairs now declare `repeat(auto-fit, minmax(min(100%, 8rem), 1fr))`: `EditListingForm.tsx` at the net area and price row and at the contact phone and email row, and `ProfileForm.tsx` at the website and logo row and at the public email and phone row, which is the same defect on the second journey 2 form and was not in the original report. The requirement success panel at `post-requirement/RequirementForm.tsx` uses `7rem` rather than `8rem` because its grid box is 226px at the 400 percent reference against the dashboard's 246, and 8rem would have collapsed a layout that renders correctly on a 360px phone. No stylesheet dependency was introduced and the inline `lbl` and `inp` objects are untouched, so the rewrite the deferral note anticipated was not needed. Correction to this finding as filed: it implies content is forced out of the viewport. Measured, it is not. `1fr 1fr` does not overflow here, because the inputs are `width:100%` inside divs whose min-content is set by the label text, so the grid squeezes rather than pushes; at the 400 percent reference each field resolved to a 117px column and the labels did not even wrap. The failure is that the fields are unusably narrow, not that anything is lost. Reporting it as an overflow failure would have been a claim the instrument does not support. Four sites keep `1fr 1fr` deliberately, each recorded with its reason in the exemption tables of `src/lib/reflow.test.ts`: `advisor/page.tsx:182`, which globals.css:412 overrides to one column at max-width:820px; `bilingual/page.tsx:101` and `:138`, an internal development surface; and `deal/termsheet/page.tsx:65`, outside the four critical journeys and a print-shaped document whose right answer is a layout decision rather than this substitution. Guarded by `src/lib/reflow.test.ts`, registered in `npm test`, which forbids the value in the four journeys, asserts the replacement floors are still present, and fails when an exemption points at a line that no longer holds the value it excuses. Non-regression is measured, not asserted: `scripts/reflow-probe.mjs` renders each pair at seven viewports in both languages and the resolved column widths from 360 upward are identical to the `1fr 1fr` baseline at every width (137, 152, 172, 327 and 583 for the three dashboard pairs; 127, 142, 162 and 331 for the requirement stat pair). Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 159 | P1 | The contact-channel checkbox group is introduced by a bare `<label>` rather than a `<fieldset>` with a `<legend>` | Closed in PKG-A11Y-1 | `src/components/EditListingForm.tsx:357` renders `<label style={lbl}>{t.channels}</label>`, the string "How people reach you" at `EditListingForm.tsx:125`, above the four channel checkboxes rendered at `EditListingForm.tsx:359` to `362`. That `<label>` has no `htmlFor` and wraps no control, so it labels nothing at all and is effectively inert markup; each checkbox is separately wrapped in its own `<label>` at `EditListingForm.tsx:360`, so each has its own name but none of them is tied to the group. A screen-reader user hears "WhatsApp checkbox", "Call checkbox" and so on with no indication of what question they answer. The intended pattern already exists in the sibling component: `src/components/ListingStudio.tsx:717` uses `<fieldset>` with `<legend className={lbl}>` at `ListingStudio.tsx:718` for the identical group of four channels, so the two forms that write the same database column disagree with each other. Rules engaged: SC 1.3.1 info and relationships and SC 4.1.2 name, role, value. Not fixed in this slice because swapping the element changes the rendered box, `<fieldset>` and `<legend>` carry user-agent margins, borders and legend positioning that a bare `<label>` does not, and this file has no stylesheet hook to neutralise them, only the inline `lbl` object; that makes it a visual change belonging to the parked visual package rather than a bug fix. Closed in PKG-A11Y-1 (RC8 slice G). The channel group at `EditListingForm.tsx:387` is now `<fieldset style={fset}>` with `<legend style={lbl}>{t.channels}</legend>` at `:388`, which is the same structure `ListingStudio.tsx:717` already shipped for the identical four channels, so the two forms that write the same column now agree. Correction to the deferral reason as filed: it states that swapping the element changes the rendered box because fieldset and legend carry user-agent margins, borders and legend positioning. They do not carry them here. `src/styles/globals.css:1` loads `@tailwind base`, whose preflight already sets `fieldset{margin:0;padding:0}`, `legend{padding:0}` and `border-width:0` on every element, so the box was inert before this change and is inert after it; the `fset` object restates those three zeroes locally only because this file styles itself entirely through inline objects and a reader should not have to know about preflight to read it. What the deferral did not see is the one thing preflight does not reset, `min-inline-size:min-content`, a user-agent default unique to fieldset that stops the element shrinking below the intrinsic width of its contents; that is finding 184's property again, and converting groups to fieldsets without handling it would have traded an accessibility defect for a reflow one. It is now handled once for the whole platform by `fieldset{min-inline-size:0}` at `src/styles/globals.css:222`. The other half the deferral was right to worry about is real and was also unfixed: there was no `legend` rule anywhere in `src/styles/`, so every group already written correctly rendered its caption at body size beside sibling labels at 0.75rem. `.field legend` at `src/styles/sat-platform.css:314` now mirrors `.field label`, which deliberately also corrects the three pre-existing legends in `RequirementForm.tsx` at `:276`, `:283` and `:348` that had been rendering oversized since they were written. Measured: 14 viewport renders in `scripts/reflow-probe.mjs`, EN and AR, unchanged from the RC7 baseline at every width including `edit-contact` at 1@246 at the 400 percent reference. Guarded by `src/lib/formGroups.test.ts`, registered in `npm test`, which forbids a `<label>` that has no `htmlFor` and wraps no control anywhere in the four critical journeys and separately asserts this exact fieldset and legend pair. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 160 | P1 | The MapLibre container in the location step has no accessible name, and its built-in controls are constructed with no locale so they announce in English in the Arabic build | Closed in PKG-A11Y-1 | `src/components/LocationPicker.tsx:153` renders the map host as `<div ref={mapEl} style={{ height: 260, borderRadius: 8, overflow: "hidden", border: "1px solid #dfe3e8" }} />` with no `role`, no `aria-label`, no `title` and no text alternative, and `src/components/LocationPicker.tsx:86` mounts a MapLibre `Map` into it. The controls are added at `src/components/LocationPicker.tsx:87`, `new maplibregl.NavigationControl({ showCompass: false })`, and `src/components/LocationPicker.tsx:88`, `new maplibregl.AttributionControl({ compact: true })`; neither the `Map` options at `LocationPicker.tsx:86` nor either control is passed a `locale` object, so MapLibre's default English strings for zoom in, zoom out and the attribution toggle are what reaches the accessibility tree even though the component already knows the language, `ar` is read at `LocationPicker.tsx:87` purely to choose which corner the control sits in. The draggable marker at `LocationPicker.tsx:91` is likewise unnamed. Rules engaged: SC 1.1.1 non-text content for the unnamed container, SC 4.1.2 name, role, value for the controls, and SC 3.1.2 language of parts for the English strings inside an Arabic document. The deferral reasoning above is corrected on both of its halves. The first half said the strings MapLibre renders could not be observed because no browser was reachable; that was the wrong place to look. The installed bundle states them: maplibre-gl 4.7.1 carries a `defaultLocale` table, resolves every one of them through `_getUIString(key)`, and writes each into the DOM at a `setAttribute('aria-label', ...)` call that was read at its site. Reading the dependency is not browser verification, and it is not claimed as such, but it is evidence and it settles the question the deferral posed. The second half said naming the container would create a false affordance without a non-canvas path to the same information; that path already exists in this component and was verified in it. The combobox above the map resolves a place name, a pasted map link or a bare `lat, lng` pair, and the two number inputs below it set the same coordinates, all three calling the same `place()` a map click calls. So the host is named: `role="group"` with `aria-label`, plus `aria-describedby` pointing at the instruction paragraph that was already beneath it, now extended to say that the latitude and longitude boxes are the keyboard path. `role="group"` and not the `role="img"` that `LocationFacts.tsx` uses, because that map is a picture of a location already decided and this one is an input: describing an editable control as a picture is worse than not describing it. `src/lib/mapLocale.ts` builds MapLibre's `locale` object from a new `mapControls` dictionary section in `src/i18n/dictionaries/en.json` and `ar.json`, and all four construction sites now pass it: `src/components/ListingsMap.tsx`, `src/components/MapExplorer.tsx`, `src/components/LocationFacts.tsx` and `src/components/LocationPicker.tsx`. The merge above is why a partial object is safe: an omitted key keeps MapLibre's own default instead of throwing at `_getUIString`. The table covers `Map.Title`, `Marker.Title`, `NavigationControl.ZoomIn`, `NavigationControl.ZoomOut`, `NavigationControl.ResetBearing`, `AttributionControl.ToggleAttribution`, `AttributionControl.MapFeedback`, `LogoControl.Title` and `Popup.Close`, and deliberately omits `ScaleControl`, `FullscreenControl`, `GeolocateControl`, `TerrainControl` and the cooperative-gestures strings because no site in `src` constructs any of them. Guard tests in `src/lib/formGroups.test.ts` assert that each of the four sites builds exactly one map, that each map is passed `mapLocale(...)` from the shared module, that every `NavigationControl` still passes `showCompass: false` so the one translated string that is not currently drawn cannot start rendering unnoticed, and that none of the five untranslated controls is introduced. Evidence classification: automated source scan, guard tests and source-level verification of the installed maplibre-gl 4.7.1 bundle at `node_modules/maplibre-gl/dist/maplibre-gl-dev.js`, where the `defaultLocale` table, the `_getUIString` resolver, the `setAttribute('aria-label', ...)` call sites and the `this._locale = Object.assign(Object.assign({}, defaultLocale), options.locale)` merge were each read directly. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 161 | P1 | `DashNav` renders its link set as a plain `<div>`, so the primary dashboard navigation is not a landmark | Closed in PKG-A11Y-1 | `src/components/DashNav.tsx:24` returns `<div className="dnav">` as the container for the whole item list, and the links themselves are rendered at `DashNav.tsx:29` to `33`. There is no `<nav>` element and no `role="navigation"`, and the only ancestor is the unnamed `<aside className="dside">` at `src/app/[locale]/dashboard/layout.tsx:83`. This is the same code site as finding 143 reached from journey 2 rather than a second defect; it is recorded separately because the inventory surface is where the consequence is felt, a lister managing listings has eight destinations at `dashboard/layout.tsx:87` to `96` and no landmark to jump to them by. The links are otherwise correctly built: `DashNav.tsx:29` sets `aria-current="page"` on the active route, so the current-location signal exists but the region that contains it does not. Rule engaged: SC 1.3.1 info and relationships, with SC 2.4.1 bypass blocks as the practical consequence. Not fixed in this slice because the component is mounted by the shared dashboard shell and by nothing else, so changing its container element changes the landmark structure of every page under `/dashboard` at once; that is a structural change to shared chrome rather than a bug fix, and it must be decided together with finding 143 rather than separately. Closed in PKG-A11Y-1 RC6, together with finding 143, which is the same code site. `DashNav` keeps its `<div className="dnav">` and gains no landmark of its own; the named navigation region is supplied one level up by the dashboard shell, which now opens `<nav className="dside" aria-label={db.navRailLabel}>` at `src/app/[locale]/dashboard/layout.tsx:91`. That is the whole fix for this finding as reported from journey 2: the eight destinations at `layout.tsx:96` to `103` now sit inside a navigation landmark with an accessible name in both languages, and the `aria-current="page"` signal at `src/components/DashNav.tsx:43` now sits inside a region rather than outside every one. The reason the component's own container did not change is recorded in the component at `DashNav.tsx:16` to `28`, so that a later reader does not add the `<nav>` this finding literally asks for and produce two landmarks over one item list, the inner one unnamed. The `<ul>` and `<li>` question was decided the same way and for the layout reason recorded against finding 143. Full evidence, the before and after probe table and the guard test are recorded against finding 143. EVIDENCE CLASS: automated scan plus browser emulated measurement, EN and AR, five widths. NOT tested with an actual screen reader and NOT independently audited. |
| 162 | P1 | Lightbox control names are English literals that do not switch with locale | Closed in PKG-A11Y-1 | Verified in src/components/Gallery.tsx at lines 76, 79 and 84, where the close, previous and next buttons carry `aria-label="Close"`, `aria-label="Previous"` and `aria-label="Next"`. The description says the strings sit in a component-local object; they do not. They are bare inline English string literals written directly on the JSX attribute, and there is no locale object in this file at all: the component's whole props surface is `images`, `title` and `photosLabel` (line 4), so it never learns which locale it is rendering under. The strings are English in both locales. SC 3.1.2 language of parts and SC 4.1.2 name role value are engaged, because an Arabic reader is handed three English names for the only controls in the dialog. Fixed by the route the deferral described, with one correction to it. The deferral proposed threading three new label props down from the page. That would have repaired these three attributes and left the file in the state that produced them, because the cause is not three attributes: it is that this component never learned which locale it was rendering under, and `photosLabel` is the shape that hid it. A single pre-translated word arriving as a prop made the file look bilingual while every other string in it was English, so the next name added here would have been English too. `locale` replaces `photosLabel` in the props, the component reads a new `gallery` dictionary section through `getDictionary(locale)`, and the close, previous and next buttons take their names from it. `src/app/[locale]/listings/[id]/page.tsx` now passes `locale` instead of an inline `ar ? "صور" : "photos"` ternary. Guard tests in `src/lib/formGroups.test.ts` assert that no `aria-label="Close|Previous|Next"` literal returns, that the dictionary read is present, and that the `photosLabel` prop does not come back. `Gallery.tsx` and `LocationFacts.tsx` were added to the journey scan at the same time, because journey 3 ends at a listing detail page that mounts both and holds neither of their names: the same gap that hid findings 167 and 200 behind `ListingsMap.tsx` in slice K, and finding 153 behind `LocationPicker.tsx` in slice H. Evidence classification: automated source scan and guard tests over the component source. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 163 | P2 | Lightbox arrow keys are not inverted under a right to left reading order | Open | Verified in src/components/Gallery.tsx at lines 21 and 22: `ArrowLeft` calls `prev()` and `ArrowRight` calls `next()` unconditionally. Nothing in the file reads `dir`, a locale prop or `document.dir`, so the mapping is the same in Arabic as in English and Right Arrow advances forward in a right to left gallery where forward is visually leftward. SC 1.3.2 meaningful sequence and the general expectation that directional keys follow the reading order are engaged. Not fixed in this slice because the correct remedy depends on evidence not available here: the component receives no locale, so fixing it means either adding a locale prop from the calling page or reading the resolved document direction at runtime, and choosing between those two is a component API decision rather than a bug fix. |
| 164 | P1 | Map flight animation is a JavaScript duration that no reduced-motion query can reach | Closed in PKG-A11Y-1 with the diagnosis corrected | RC12. The class of defect this row names is real and has been fixed. The instance it names was not a defect, and saying so is part of the closure rather than a footnote to it. `node_modules/maplibre-gl/dist/maplibre-gl.js` (4.7.1) defines `flyTo(t,i){if(!t.essential&&o.prefersReducedMotion){...return this.jumpTo(a,i)}...}`, and `easeTo` sets `t.duration=0` under the same condition, where `o.prefersReducedMotion` reads `matchMedia("(prefers-reduced-motion: reduce)").matches` live on every call. Drag inertia is guarded by the same flag. So the two `duration: 650` calls in ListingsMap.tsx already collapse to an instantaneous jump for a reader who has asked for reduced motion, and 650 is only the number used by readers who have not. Rewriting them would have added a second, weaker copy of a check the library performs correctly. The earlier row was written from our source alone, which is why it reached the wrong answer: the evidence was in the dependency. What was genuinely out of reach is scroll. src/styles/globals.css line 295 sets `html{scroll-behavior:auto}` under `prefers-reduced-motion: reduce`, but that declaration only decides what `behavior: "auto"` means, and `"auto"` is the DEFAULT; a call that passes `behavior: "smooth"` states its own behaviour and never consults the property. Five call sites did exactly that and animated for every reader regardless of the preference: src/components/MessagesClient.tsx line 92 and src/components/AdvisorWidget.tsx line 34 (both chat transcripts, which scroll on every message and are therefore the most repeated animation in the product), src/app/[locale]/advisor/page.tsx line 88, src/components/MarketingHome.tsx line 102 (the asset rail's paging buttons) and src/components/MapExplorer.tsx line 145 (the map's rail-to-selection sync). All five now call `scrollBehavior()` from the new src/lib/motion.ts, which returns `"auto"` under the preference and `"smooth"` otherwise, reading the query at call time rather than caching it at import for the same reason MapLibre reads its getter live: a reader can change the operating-system setting while the page is open. It answers false on the server, false where `matchMedia` is absent and false if the query throws, because an unanswerable question about a preference is not a preference. src/lib/motion.test.ts holds three guards beyond the unit behaviour: no source file outside motion.ts may write a literal `behavior: "smooth"`, no camera call may pass `essential` (the one flag that would opt a `flyTo` back out of MapLibre's handling), and maplibre-gl must still be on 4.x with both guard strings present in the installed bundle, so the correction above has to be re-read before it can be trusted again if the dependency moves. Evidence: automated (1655 tests) and source-read against the installed dependency. Not verified with an actual screen reader or on a physical device with the operating-system preference set; that remains an independent-verification item. |
| 165 | P1 | District bubbles and building pins carry the same fill and the same shape | Closed in PKG-A11Y-1 | RC12. The repair is an inversion, not a recolour. src/theme/palette.ts gains two named roles, `exactFill: BRAND.paper` and `exactRing: BRAND.harbor`, and the `p-c` layer in src/components/ListingsMap.tsx now paints `circle-color: MAP.exactFill` with `circle-stroke-color: MAP.exactRing`, radius 6.5 to 7 and stroke width 1.5 to 2.5 because a ring needs enough ring to read as one. A solid Harbor disc carrying a numeral means an aggregate; a Paper disc with a Harbor ring means one building. That difference survives greyscale, every form of colour blindness and the whole 34-pixel-to-7-pixel size range, which the previous mark system did not, because the district bubble's radius is itself data and at the small end of the ramp a one-space district and a building were very nearly the same mark. The pin stays below the ramp's floor of 16, so the size ordering that was doing all the work before is preserved rather than replaced. The legend was rewritten to match. Both swatches are now 13px and differ by fill instead of by size, both carry `aria-hidden` because the text beside them says everything they say, and the two strings moved out of inline ternaries into the `t3` object where each now names its own form alongside its meaning: "Solid dot / district (approximate), click to filter" and "Ring / one exact building", with the Arabic "نقطة مصمتة" and "حلقة". Naming the sensory characteristic alongside the meaning is the way round SC 1.3.3 asks for; a legend whose two swatches differ only in size is telling the reader to compare sizes. The Arabic district line also said "منطقة", region, where every other Arabic string on this surface says "حي"; a legend that renames the thing it explains is its own defect, and that is corrected too. The stale comment calling the pins green dots is gone. src/components/MapExplorer.tsx was read and deliberately left alone: its two mark systems are clusters and single points, they already differ by label (a count numeral against a price) and by colour family (a stepped neutral ramp against the asset-type categorical scale), and it has no legend at all, so introducing a shape difference there would have added an unnamed sensory difference rather than removing one. src/lib/mapMarks.test.ts holds six guards: the two layers may not share a fill, the two palette roles may not resolve to one value, the pin must stay under the bubble ramp's floor, the legend swatches must differ by fill and not by size, both legend strings must name their form in both languages and the Arabic district line must say حي and not منطقة, and the CSS `--harbor` and `--paper` tokens must still equal the paint values so the legend keeps mirroring the canvas. Evidence: automated and source-read. The rendered map at 400 percent zoom and under an actual screen reader is an independent-verification item; a canvas mark has no accessible representation and the two `<ul>` alternatives beside the map, which already list districts and buildings separately by name, remain the non-visual path. |
| 166 | P1 | The pending status dot colour is roughly 3.64 to 1 against white | Closed in PKG-A11Y-1 | Verified in src/styles/sat-platform.css: `--amber:#B7791F` is declared at line 18 and `.statusdot.pend{color:var(--amber);}` at line 372. The dot glyph itself is `background:currentColor` at line 371, so the same value carries both the 12px text and the 7px mark. The figure is arithmetic on the declared token values, never a measurement: computing WCAG relative luminance for #B7791F gives about 0.2384, and against `--paper:#FFFFFF` (line 7) that is (1.0 + 0.05) / (0.2384 + 0.05), or about 3.64 to 1. That is below the 4.5 to 1 that SC 1.4.3 contrast asks of text at this size and below the 3 to 1 that SC 1.4.11 non-text contrast asks nothing further of, but the text is the failing part. Not fixed in this slice because `--amber` is a root token consumed by at least nine rules in this stylesheet alone, including `.freeze.capped`, `.dnav a .badge.warn` and `.kcard .ic.a`, and darkening it reaches surfaces this pass did not read; it belongs to the parked visual package. **Corrected in RC15.** Closed by `beef75a` by splitting the token rather than repainting it, which is why the `--amber` hex is unchanged and this row read as open. `--amber:#B7791F` is now the MARK value only: `.statusdot.pend::before{background:var(--amber);}` at `src/styles/sat-platform.css:425` draws the 7px dot, and 3.64 to 1 clears the 3 to 1 that SC 1.4.11 asks of a graphical object. The text beside it takes `--status-attention-text:var(--amber-d)` at line 61, declared `amber-d: "#8A5A12"` in `tailwind.config.ts`, which is 5.91 to 1 on paper. `.statusdot.pend{color:var(--status-attention-text);}` at line 424. The status is never carried by the dot alone: the label sits beside it, so colour is not the only means of conveying it. The third and fourth tests in `src/lib/palette.test.ts` hold the separation and require every text-carrying status token to pass 4.5 to 1. Evidence class: automated. The ratios are arithmetic on the declared token values in the source at `beef75a`, recomputed at `7aaab03`, not a measurement of rendered paint, and `src/lib/palette.test.ts` recomputes them on every run so a future edit that reintroduces the step fails the suite. Not tested on a physical device, not tested with an actual screen reader and not independently audited. |
| 167 | P1 | The view toggles expose no current or pressed state | Closed in PKG-A11Y-1 | Verified in two places. The properties and insights toggle in src/app/[locale]/listings/page.tsx at lines 454 and 455 is a pair of `Link` elements whose only state signal is `className={insightsView ? "chip on" : "chip"}`; neither carries `aria-current` nor `aria-pressed`. The list to map toggle in src/components/ListingsMap.tsx at line 337 is a button labelled from `t2.showMap` with no `aria-expanded` and no pressed state, and its counterpart close button at line 328 is likewise bare. SC 4.1.2 name role value and SC 1.4.1 use of colour are engaged, because the active view is carried by a background colour class alone. Not fixed in this slice because the two toggles want different remedies, `aria-current="page"` on the navigational chip pair and `aria-expanded` on the disclosure button, and settling which control is a navigation and which is a disclosure is a structural decision about the split view rather than an attribute patch. Closed in slice K, and the structural decision the deferral asked for is recorded here rather than left in the source. It is read off `src/styles/sat-platform.css:695-706`, which is the only place the split view's behaviour is actually defined. Above 1080px `.lst-split` is a two column grid, `.lst-map-panel` is a sticky region inside it, and `.lst-map-toggle` is `display:none`: the map is simply part of the page and there is no control to describe. Below 1080px the panel is hidden until `.open`, and `.open` sets `position:fixed;inset:0;z-index:90`, which is a layer drawn over the entire viewport. The component already treated that state as a dialog, with `role="dialog"`, `aria-modal`, a focus trap and Escape to close. So the two controls are not two instances of one thing. The properties and insights pair are two `Link` elements that change the URL and reload the route, which is navigation, and the attribute that says which one you are on is `aria-current="page"`. The map toggle opens a modal dialog, and the attribute for that is `aria-haspopup="dialog"`. This register's own proposed remedy, `aria-expanded` on the toggle, is wrong and is corrected here: `aria-expanded` describes content that expands in place while the rest of the page stays reachable, and ARIA authoring practice specifically excludes modal dialog triggers from it. Announcing a control as collapsed or expanded while the thing it opens covers the screen and traps focus would describe the opposite of what happens. `aria-expanded` was therefore not added, and a guard test asserts it is absent from the file so the deferral's suggestion is not reinstated by a later reader of this row. SC 1.4.1 is answered separately, because `aria-current` is not visible: the active chip takes `fontWeight: 700` inline. Inline rather than on `.chip.on`, because since slice I `.chip.on` is the selected face of every native radio on the platform, and reweighting all of them to fix one pair would be a cosmetic sweep with no evidence behind it. Two guard tests in `src/lib/formGroups.test.ts` cover the pair and the toggle; `src/components/ListingsMap.tsx` was added to that file's journey scan in the same commit, because the map half of the split view had never been scanned. Evidence classification: automated source scan and guard tests, plus the browser-emulated reflow probe confirming the split view is unchanged at every width. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 168 | P1 | The Insights table has no caption and no scope on its header cells | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/listings/page.tsx at line 471, where `<table className="dt" style={{ minWidth: 520 }}>` opens; the `<thead>` on line 472 is a single row of five bare `<th>` elements with no `scope` attribute, and no `<caption>` element appears between the table tag and the head. The visible title sits outside the table as a `<div>` at line 465. SC 1.3.1 info and relationships is engaged: the row and column association is available visually but not programmatically, and the table has no accessible name of its own. Not fixed in this slice because the `.dt` table pattern is shared by the rent index, the dashboard listing tables and the admin account tables, and the header cells are generated from dictionary strings, so adding a caption means adding dictionary keys that this pass was instructed not to touch. **Closed in PKG-A11Y-1 slice O (RC11).** The recorded deferral said "adding a caption means adding dictionary keys that this pass was instructed not to touch". That turned out to be false, and finding out why is most of the fix: every one of the 21 tables already renders a visible title, and that title is already a dictionary value in scope at the table's call site. So the caption reuses the string the reader can already see rather than inventing a second name for the same thing, which is also better practice than a bespoke caption would have been. Not one dictionary key was added for a caption. The technique is `<caption className="sronly">` as the table's first child: `.sronly` is `position:absolute`, which is legal on a caption, removes it from the table box flow and leaves it in the accessibility tree, so the table gains an accessible name and loses nothing visually. It was chosen over a `table.dt>caption{...}` stylesheet rule so that there is one mechanism, greppable and testable at every call site. `scope="col"` is inserted into every `<th>` inside every `<thead>` (90+ header cells), and two tables gained row headers where a first column was acting as one: `/pricing`'s limit-comparison rows and `/saved`'s comparison `Row` helper, both rendered identically to before by pre-narrowing `.matrix th` to `.matrix thead th` and by cancelling the user-agent `th` defaults with `font-normal text-start`. Two guard tests hold the invariant: every `<table` in the tree has a `<caption` as its first child, and every `<th` inside a `<thead` carries `scope=`. The Insights table itself is at `src/app/[locale]/listings/page.tsx`, captioned `dl.indexCut`. Evidence classification: automated source scan plus the browser-emulated reflow measurement above, which is unchanged from baseline. Not tested with an actual screen reader and not independently audited. |
| 169 | P2 | Two links are styled as a tab strip but are not tabs and cannot become tabs | Open | Verified in src/app/[locale]/listings/[id]/page.tsx at lines 341 to 344. The container is `<div className="tabs">` and the two children carry the `t` and `t on` classes that src/styles/sat-platform.css lines 225 to 227 paint as a bottom-bordered tab strip. Confirming what they actually are, as instructed: the first is an in-page anchor to `href="#ov"` and the second is a Next `Link` to a different route, `L("/invest")`. So this is not an unlabelled tab widget; it is two navigation links wearing tab styling, and adding `role="tablist"`, `role="tab"` and `aria-selected` would be the wrong remedy, because there is no tab panel to select and activating the second one leaves the page. SC 1.3.1 info and relationships and SC 4.1.2 name role value are engaged by the mismatch between the visual affordance and the real semantics. Not fixed in this slice because the correct remedy is either to restyle the pair as links or to build real tab panels, and both are structural rewrites larger than a bug fix. |
| 170 | P1 | The listing video is served with no captions track | Open, partially mitigated in PKG-A11Y-1 | RC12. SC 1.2.2 is not satisfied and this row stays open, because captions are a content and ingest commitment rather than a markup change. Rendering an empty `<track>` would assert a captions track that does not exist, which is worse than the defect it pretends to fix. What shipped instead is the truth, stated where it is useful, plus the accessible name the player never had. The `<video>` in src/app/[locale]/listings/[id]/page.tsx now carries `title` and `aria-label` from `ld.videoTour`, the same string the YouTube and Vimeo iframe branch beside it has always carried, so a screen reader announces the player as the video tour rather than as "video". Below it a new bilingual line, `ld.videoNoCaptions` ("No captions are available for this video." / "لا تتوفر ترجمة نصية لهذا الفيديو."), tells a reader who depends on captions before they press play rather than after. The note is deliberately bare. It does not claim the written listing description carries the same information, because nobody has checked that a given lister's video says nothing the text does not, and owner ruling 3 forbids inferring a claim from a route type or a generic wording. Remaining work, named so it can be scheduled rather than rediscovered: a caption field on the listing media record, an upload path that accepts a WebVTT file per language, validation that the file is a real cue list rather than an empty one, and an authored track per video in English and Arabic. Until all four exist there is nothing for a `<track>` element to point at. Evidence: automated and source-read. |
| 171 | P1 | The listing description paragraph declares neither lang nor dir | Closed in PKG-A11Y-1 | Verified at `src/app/[locale]/listings/[id]/page.tsx` line 568 before the fix: `{(ar ? l.description_ar : l.description_en) && <p className="muted" style={{ ... }}>{ar ? l.description_ar : l.description_en}</p>}`. The paragraph carried no `lang` and no `dir`, so whichever of the two stored columns was rendered inherited the document direction set by `src/components/HtmlLangDir.tsx`. This is the one paragraph on the page that SAT did not compose. Everything else there comes from the dictionary or from a controlled vocabulary and is in the reader's language by construction; this is prose a stranger typed into a textarea, and the column it arrived in is that stranger's declaration rather than a measurement. It is frequently wrong in both directions: a lister who writes the whole listing in English pastes the same paragraph into both fields, and a lister writing in Arabic drops an English building name or a unit specification into the middle of one. English prose inheriting `lang="ar"` is not read aloud as accented English, it is read with Arabic phonetics, which is noise; and the bidi algorithm resolves the same run against the page direction and can reorder it on screen. SC 3.1.2 language of parts and SC 1.3.2 meaningful sequence are engaged; severity P1, journey 3, search, listing detail and Evidence Passport. The earlier note recorded this as blocked on a data-model question, and that was the right caution and the wrong conclusion. The passport's instrument is a stored flag, `reasonLang` read by `readCorrectionReason` in `src/lib/evidence.ts`, and it is correct there because a correction reason is filed through one field with one recorded language. It is unavailable here, and adding a language column would only move the problem, because the new flag would be the field name again. What is available is the text. Script is not inferred, it is read: the code points are either Arabic or they are not, which is evidence of the same kind as anything else this platform publishes and the only kind available for prose a stranger typed. Fixed by `src/lib/textScript.ts`, which counts letters, excludes Arabic-Indic and extended Arabic-Indic digits because SAT writes Western numerals in both languages, requires eight letters before a majority means anything and a seventy percent share before a run is called, and returns null for everything else including genuinely mixed text. A caller that gets null says so: `dir="auto"` states that the direction is to be resolved from the content, which is true, where `dir="rtl"` on a mixed paragraph is a claim about text nobody checked. `dir` is never omitted now. The one assumption in the module, that Latin script means English on a platform that publishes in exactly two languages, is stated in its doc comment rather than hidden, because the alternative of leaving the paragraph untagged makes the same claim silently through inheritance. Thirteen tests in `src/lib/textScript.test.ts` cover the floor, the majority, both digit ranges, the Arabic presentation-forms blocks, accented Latin, the mixed case and every refusal; a guard test in `src/lib/formGroups.test.ts` asserts the render site. Evidence classification: automated source scan and unit tests. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 172 | P2 | Several Evidence Passport label and value pairs are lists rather than description lists | Open | Verified in src/components/EvidencePassport.tsx. The main provenance rows are already a description list, `<dl className="evi-dl">` at line 219 with real `<dt>` and `<dd>` children at lines 222 and 223, so the finding as recorded overstates the scope. What remains outside that structure is the permissions block at lines 306 to 311, where each of the three permission pairs renders as `<li><span>{k}</span><span className="evi-val">{v}</span></li>`, and the verification block at lines 245 to 256, which uses the same two generic spans inside a list item. The section headings above them at lines 265 and 304 are `<div className="evi-h">` rather than headings. SC 1.3.1 info and relationships is engaged, because the pairing between a label and its value is carried by flexbox placement rather than by markup. Not fixed in this slice because converting three sibling blocks to consistent description lists and promoting the section titles to real headings is a structural rewrite of the panel larger than a bug fix, and the panel has a render test at src/components/EvidencePassport.render.test.tsx that would need to move with it. |
| 173 | P2 | The result card heart is a decorative glyph that imitates a save control | Open | Verified and reported as found rather than as described. The glyph is rendered at src/components/satkit.tsx line 174, `{fav && <span className="fav"><Icon.heart size={15} /></span>}`, and src/app/[locale]/listings/page.tsx line 531 passes the bare `fav` flag on every result card. Checking the specific question asked: it is NOT inside any save control's accessible name. Every icon in this file goes through the `Ic` wrapper at line 58, which sets `aria-hidden="true"` and `focusable="false"`, so the heart contributes nothing to the name of the card link that encloses it. The defect that actually remains is the opposite one: there is no save control here at all. The span is inert, it is not a button, it has no press state, and the real toggle lives in a different component, src/components/SaveHeart.tsx, which these cards do not mount. SC 1.3.3 sensory characteristics and SC 4.1.2 name role value are engaged, since a reader is shown an affordance that does nothing. Not fixed in this slice because wiring the card heart to the real save state is a feature change, not an accessibility patch. |
| 174 | P1 | The whole type scale is declared in px and does not answer a browser text-size preference | Closed in PKG-A11Y-1 | Verified in src/styles/globals.css at lines 414 to 416, where every step of the scale is a px literal: `--fs-cap:9px` through `--fs-4xl:44px`, with `--fs-input:16px` among them. The right to left overrides at line 122 restate the same scale in px. Beyond the tokens, the journey surfaces set sizes as bare React numbers, which React serialises as px: src/app/[locale]/listings/[id]/page.tsx line 253 sets `fontSize: 30` on the page heading and line 568 sets `fontSize: 14.5` on the description, and src/app/[locale]/listings/page.tsx line 353 sets `fontSize: 32`. A px font-size does not scale with a browser default text size setting the way rem does. SC 1.4.4 resize text is engaged. Not fixed in this slice because converting the scale to rem is a design token change that reaches every surface in the product, including many this pass did not read, and it belongs to the parked visual package; a partial conversion would leave two scales disagreeing. Closed in PKG-A11Y-1 RC4 by converting wholesale, which is the only safe way to do it. The scale tokens, the right to left block, 772 bare numeric React fontSize values, 176 Tailwind text-[Npx] arbitrary values and 128 CSS font-size declarations are now rem, 1073 sites in 89 files, and a scan of src reports zero px font sizes remaining. The conversion is exact at the 16px default root size, so a reader who has changed nothing sees an identical page: 0.8125rem is 13px to the pixel. The one deliberate exception is --fs-input, which keeps a 16px floor as max(16px, 1rem) because it exists to stop iOS zooming the viewport when a text field takes focus below a computed 16px; the max() keeps that floor while still answering a larger preference. Evidence class: automated scan for the completeness of the conversion, plus browser-emulated measurement, scripts/responsive-probe.mjs over 13 shipped fragments in EN and AR at 320, 360, 390, 430, 768 and 1280, 156 measurements, no row past its box and no item wider than its content box. Guarded by src/lib/typeScale.test.ts, which fails on any new px font size anywhere in src. Not verified on a physical device and not verified with an actual browser text-size preference raised, which is the remaining independent-verification item for this finding. |
| 175 | P2 | Floor plan tiles open a new tab with no warning in the link name | Open | Verified in src/app/[locale]/listings/[id]/page.tsx at line 503: the plan tile is `<a key={i} href={fp.url} target="_blank" rel="noopener noreferrer" className="card" ...>` and its accessible name comes from the image alt at line 507 and the caption div at line 509, both of which are just the plan label. Nothing in the name says a new tab or window will open, and there is no icon or visually hidden note. The same pattern repeats at line 552 for the external video link. SC 3.2.5 change on request is engaged, and SC 2.4.4 link purpose in context bears on it. Not fixed in this slice because the remedy needs a dictionary string for the warning, in both English and Arabic, and this pass was instructed not to edit src/i18n/dictionaries; writing the sentence inline here would put a fourteenth English literal into a bilingual surface. |
| 176 | P2 | The breadcrumb arrow and slashes are inside the link name and are read aloud | Open | Verified in src/app/[locale]/listings/[id]/page.tsx at line 230. The entire breadcrumb is one link whose children are `{ar ? "→" : "←"} {dict.ld.crumbListingsUpper} / {String(dn).toUpperCase()} / {type.toUpperCase()}`. The arrow is a literal U+2192 or U+2190 character in the text content, not a background image and not marked `aria-hidden`, so it is part of the computed accessible name and is announced. The two forward slashes are in the name for the same reason. SC 4.1.2 name role value and SC 2.4.4 link purpose in context are engaged, since the name of the control includes punctuation that is purely visual. Not fixed in this slice because the honest remedy is to make this a real breadcrumb, an ordered list of separate links with CSS-generated separators, rather than one link that concatenates three levels; that is a structural rewrite larger than a bug fix, and the JSON-LD BreadcrumbList at lines 335 to 340 already describes the four-level structure the markup does not have. |
| 177 | P2 | Token drift, a map wash written as a literal rgba where a token declares the same value | Open | Verified as a single real instance. src/components/ListingsMap.tsx line 111 paints the hover highlight layer with `"circle-color": "rgba(58,110,165,0.14)"` written literally, while src/styles/sat-platform.css line 86 declares `--map-area-wash:rgba(58,110,165,0.14)`, the identical value, and the same file's palette import at line 8 exists precisely so that MapLibre paint reads named roles rather than inline colour; the surrounding lines 110 to 113 all use `MAP.pin` and `MAP.pinStroke` correctly, so this one literal is the outlier. The comment at lines 6 and 7 states the rule the line breaks. No WCAG success criterion is engaged directly; this is a maintenance defect that makes a future contrast correction miss one surface, which is how SC 1.4.11 non-text contrast regressions get introduced. Not fixed in this slice because the value has no entry in src/theme/palette.ts, so closing it means adding a named role to the palette module, and that module is the shared source for every map surface in the product. |
| 178 | P2 | Token drift, the sample tag on the requirements board is three raw hex literals | Open | Verified as a second real instance in a different file. src/app/[locale]/requirements/page.tsx line 71 renders the sample tag with `style={{ color: "#92400E", background: "#FFFBEB", borderColor: "#FCD34D", fontSize: 10.5 }}`. None of the three values appears anywhere in src/styles/sat-platform.css, and the declared attention role that this tag is plainly reaching for is `--status-attention:var(--amber)` with `--status-attention-wash:#FBF3E3` at line 38 of that file, which the requirement detail page uses correctly at src/app/[locale]/requirements/[id]/page.tsx lines 179 and 180. So the same semantic state is painted two different ways one click apart. Contrast is not asserted here, because #92400E on #FFFBEB is outside the declared token set and any figure would be arithmetic on values that no token owns. Not fixed in this slice because it is a design token change and the correct target role has to be chosen deliberately, attention or stale, and that choice reaches the tag component and every other surface that marks sample data. |
| 179 | P1 | Amber verdict text on its own wash computes to roughly 3.30 to 1 | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/requirements/[id]/page.tsx at lines 178 to 180, where a non-exact match takes `{ fg: "var(--status-attention)", bg: "var(--status-attention-wash)" }`, applied to the verdict tag at line 189 at `fontSize: 10.5`. The same pair is set on the lister side at src/app/[locale]/dashboard/requirements/page.tsx lines 221 to 223. The figure is arithmetic on the declared token values and is not a measurement: src/styles/sat-platform.css line 38 resolves `--status-attention` to `--amber`, which line 18 declares as #B7791F, and declares `--status-attention-wash:#FBF3E3` inline. WCAG relative luminance for #B7791F is about 0.2384 and for #FBF3E3 about 0.9016, giving (0.9016 + 0.05) / (0.2384 + 0.05), or about 3.30 to 1. SC 1.4.3 contrast is engaged, and at 10.5px this is not large text by any definition. Not fixed in this slice because the remedy is the same `--amber` token change recorded at 166, and it reaches surfaces this pass did not read; it belongs to the parked visual package. **Corrected in RC15.** Closed by `beef75a` through the same token split as 166; this row was never updated. The verdict tag now takes `{ fg: "var(--status-attention-text)", bg: "var(--status-attention-wash)" }` at `src/app/[locale]/requirements/[id]/page.tsx:243`, and `#8A5A12` on `#FBF3E3` computes to 5.36 to 1, up from 3.30. The stale counterpart `--status-stale-text:#A26100` is 4.95 to 1 on paper. The comment block at `src/styles/sat-platform.css:55` records the reasoning beside the tokens themselves. Evidence class: automated. The ratios are arithmetic on the declared token values in the source at `beef75a`, recomputed at `7aaab03`, not a measurement of rendered paint, and `src/lib/palette.test.ts` recomputes them on every run so a future edit that reintroduces the step fails the suite. Not tested on a physical device, not tested with an actual screen reader and not independently audited. |
| 180 | P1 | The listing attachment radio group cannot be cleared and has no accessible name | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/requirements/[id]/page.tsx at lines 215 to 222. Each match renders its own `<input type="radio" name="attach-listing" checked={on} onChange={() => setAttached(on ? null : m.listing_id)} />`. The clearing branch is written but is unreachable in practice, because a browser fires no change event when an already-checked radio is activated, so once a listing is attached the responder has no way back to attaching nothing. Separately, the inputs share a `name` but sit in per-match `<div>` containers with no `<fieldset>`, no `<legend>`, no `role="radiogroup"` and no `aria-labelledby`, so the group as a whole is unnamed; each individual radio does get a name from the wrapping label at line 215. SC 4.1.2 name role value and SC 3.3.2 labels or instructions are engaged. Not fixed in this slice because both halves want the same structural change, wrapping the whole match list in a named group and adding an explicit none option, and that is a rewrite of the panel rather than a bug fix. Closed in PKG-A11Y-1 (RC8 slice G). The match list at `src/app/[locale]/requirements/[id]/page.tsx:175` is now a `<fieldset>` whose `<legend>` at `:176` carries `{t.matchesH}`, the caption that was previously a bold `<div>`, so the group is named with no new dictionary key in either language. Each radio's own name now carries its listing title through `<span className="sronly">{mt}</span>`, so the options are told apart by what they are rather than by which card they are drawn inside. The way out of the group is an explicit attach-nothing option at `:249`, `checked={attached === null}` with `onChange={() => setAttached(null)}`, and it is the option that starts selected. Addition to the finding as filed: it records the clearing branch as written but unreachable, which is correct, and reads that as one defect with the missing group name. It is two. Attaching nothing is a choice a responder can legitimately want to make after making a different one, so a radio group with no such option is incomplete regardless of whether the toggle worked; `setAttached(on ? null : m.listing_id)` was not a broken implementation of a way out, it was a toggle written into a control type that has no toggle semantics, and the repair is a member of the group, not a fixed handler. The old expression is now forbidden by name in the guard. `minInlineSize: 0` is set inline on this fieldset in addition to the global rule because the group sits inside a narrow response panel. Guarded by `src/lib/formGroups.test.ts` with four assertions: the legend, the none option, the absence of the old toggle expression, and the per-radio title span. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 181 | P1 | The size inputs' visible label and their accessible names share no words | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/post-requirement/RequirementForm.tsx. Line 319 renders the visible label as `<label htmlFor="pr-size-min">{pr.sizeRange}</label>`, and the two inputs override it with `aria-label={pr.sizeMinLabel}` at line 324 and `aria-label={pr.sizeMaxLabel}` at line 330. Reading the values in src/i18n/dictionaries/en.json, line 878 gives `sizeRange` as "Size range (m²)" and lines 897 and 898 give "Smallest size (m²)" and "Largest size (m²)". The visible string "Size range" appears in neither accessible name, and because the `aria-label` wins over the associated `<label>`, the first input's programmatic name is "Smallest size (m²)" while the text a speech-input user reads beside it says "Size range". SC 2.5.3 label in name is engaged, and SC 4.1.2 name role value bears on it. Not fixed in this slice because the remedy needs new or reworded dictionary values that carry the visible words inside the accessible name, and this pass was instructed not to edit src/i18n/dictionaries. Closed in PKG-A11Y-1 (RC8 slice G). The size range at `src/app/[locale]/post-requirement/RequirementForm.tsx:337` is now a `<fieldset className="field">` whose `<legend>` at `:338` is `{pr.sizeRange}`, matching the three sibling groups this form already wrote that way at `:276`, `:283` and `:348`, and both accessible names now begin with the visible caption: `en.json` `postReq.sizeMinLabel` is "Size range (m²), smallest" and `sizeMaxLabel` is "Size range (m²), largest", with the Arabic pair using the Arabic comma. A speech user saying the words printed on the screen now addresses a control that answers to them, which is what SC 2.5.3 asks. The deferral reason, that this pass was instructed not to edit `src/i18n/dictionaries`, expired with that pass; the two reworded values and the strings for finding 180 are this slice's only dictionary changes and both files were rewritten textually and re-validated, never round-tripped through a parser. Scope correction: the finding does not say whether this is a pattern. An `src`-wide sweep for a visible `<label>` overridden by a differing `aria-label` on the control it names found this one site pair and no other, so it is a defect and not a class, and no sweep was performed. The second input, which previously had no visible label associated with it at all, is now inside the named group. `pr-size-min` remains the validation focus target at `:133` and is unchanged. Guarded by `src/lib/formGroups.test.ts`, which asserts the legend and, for both languages, that each name contains the caption verbatim. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 182 | P1 | Asset chips use aria-pressed for a single-valued choice | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/post-requirement/RequirementForm.tsx at line 272: `<button key={a} type="button" aria-pressed={asset === a} ... onClick={() => setAsset(a)}>`. The handler assigns rather than toggles, and the state it drives is the single string `asset` declared at line 71, so exactly one chip is ever pressed and pressing another silently unpresses the first. That is a radio group, and `aria-pressed` describes it as an array of independent toggles instead. The contrast is visible in the same file: the must-haves at line 345 use the identical chip pattern and there `toggle` really is multi-select, so `aria-pressed` is right there and wrong here. The enclosing `<fieldset>` and `<legend>` at lines 268 and 269 are correct and would carry over. SC 4.1.2 name role value is engaged. Not fixed in the slice that found it because converting to `role="radio"` inside a `role="radiogroup"` also requires roving tabindex and arrow-key handling to be written, which is a structural rewrite larger than a bug fix. **That deferral reason was wrong, and it is left standing above rather than quietly edited, because the mistake in it is the reason this defect class reached four sites.** The control this needed is not `role="radio"`. It is `<input type="radio">`, which is the platform's own implementation of the same role and which supplies roving tabindex, arrow keys, wrapping, direction-correct horizontal arrows, the impossibility of clearing a chosen option, and form participation, with no JavaScript written at all. The proof that this was available rather than merely arguable is that it was already in the same file three lines further down: the transaction-type control at `src/app/[locale]/post-requirement/RequirementForm.tsx:296` has been a native radio inside a `.seg` label with an `.sronly` input since the deal-type and timeline selectors were converted from `<span onClick>`, and the technique is documented in the comment on `.sronly` at `src/styles/globals.css:498`. The register looked at the ARIA pattern and priced the ARIA pattern's cost; it did not look one screen down. Fixed in slice I at `RequirementForm.tsx:286-293`: the eight `<button aria-pressed>` chips are now eight `<label class="chip">` elements each wrapping `<input type="radio" name="asset">`, the existing `<fieldset>` and `<legend>` carried over unchanged as predicted, and the inner `role="group"` was removed because the fieldset was already the group and the shared `name` is what binds the options. Nothing visual moved: the chips keep `.chip` and `.chip.on`, and the input is `.sronly`. One CSS rule was added with the conversion, `.chip:has(input:focus-visible)` at `src/styles/sat-platform.css:161`, modelled on `.seg label:has(input:focus-visible)` at `:223`, because an `.sronly` input is a one pixel clipped box and the browser would otherwise draw the focus ring where no one can see it; without that rule the conversion would have traded SC 4.1.2 for SC 2.4.7. The same defect class was then found at three further sites and is recorded as findings 197 and 198. Guarded by six tests in `src/lib/formGroups.test.ts`, which scan every shipping file under `src` rather than the four critical journeys alone, since the spread is the point: a rebuilt one-of-many chooser anywhere in the repository now fails the suite unless it is named in `HAND_BUILT_RADIO_EXEMPT` with a reason. Evidence classification: automated source scan, plus browser-emulated measurement in Chromium through `scripts/radio-probe.mjs`, which renders the shipped markup with the shipped stylesheet under a coarse pointer at 390 wide in English and Arabic and presses the keys: one tab stop per group for 24 options, ArrowDown walks and wraps, ArrowUp steps back, ArrowRight advances in `ltr` and retreats in `rtl`, two presses of Space on the chosen option leave it chosen, the label carries a 2px `rgb(58, 110, 165)` focus ring, every target clears 44 by 44, and the group name comes from the legend once. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 183 | P2 | The shared arrow icon is not mirrored under a right to left direction | Open | Verified in src/components/satkit.tsx at line 68, where `Icon.arrow` is a fixed path, `<path d="M5 12h14M13 6l6 6-6 6" />`, pointing rightward with no transform. It is used on the requirement journey at src/app/[locale]/post-requirement/RequirementForm.tsx line 428 on the submit button and line 216 on the view requirement link. Searching both stylesheets for a mirroring rule finds only two, `[dir="rtl"] .asset-arrow svg{ transform:scaleX(-1); }` at src/styles/globals.css line 118 and `[dir="rtl"] .dside .me .chev{transform:scaleX(-1);}` at src/styles/sat-platform.css line 337; neither selector reaches this icon. So an Arabic reader sees a forward-progress arrow pointing against the reading direction. SC 1.3.2 meaningful sequence and SC 1.3.3 sensory characteristics are engaged. Not fixed in this slice because the right remedy is a single rule that mirrors every directional glyph in the shared icon set, which reaches surfaces this pass did not read and belongs to the parked visual package. |
| 184 | P1 | A 320px minimum grid track overflows a 400 percent zoom viewport | Closed in PKG-A11Y-1 | Verified in src/app/[locale]/requirements/page.tsx at line 59: `gridTemplateColumns: "repeat(auto-fill,minmax(320px,1fr))"`. The `auto-fill` collapses to one column but the `minmax` floor does not shrink below 320px. The container at line 37 is `padding: "36px 24px 48px"`, so at the 1280 by 1024 reference resized to 400 percent, which is a 320px CSS viewport, the available width is 272px and the track still demands 320px, producing horizontal scrolling for the card content. The requirement detail page uses the safer `minmax(120px,1fr)` at line 125 of src/app/[locale]/requirements/[id]/page.tsx, so the pattern is inconsistent within the journey. SC 1.4.10 reflow is engaged. Not fixed in this slice because lowering the floor changes the card layout at every breakpoint, and picking the replacement value is a layout decision belonging to the parked visual package rather than a bug fix. Closed in PKG-A11Y-1 (RC7 slice F). `requirements/page.tsx:59` now reads `repeat(auto-fill,minmax(min(100%, 320px), 1fr))`, and the same substitution was applied to the other 30 fixed `minmax(<N>px, 1fr)` floors across 22 files, because the defect was a pattern and not one line. `minmax(min(100%, Npx), 1fr)` resolves identically to `minmax(Npx, 1fr)` at every width where the container is at least N wide and collapses only in the case that previously failed, so no working layout moves. Two families were deliberately left alone with the reason verified in source: `bilingual/page.tsx:101`'s `minmax(40px,auto)` label gutter, which is below every content box measured, and `.lst-split`'s 300px map track at `sat-platform.css:682`, which is removed entirely at max-width:1080px by :687 and :688 and therefore exists only at widths where 300px fits. Correction to this finding as filed: it states the defect produces horizontal scrolling. It does not, and that is worse. `html,body{max-width:100%;overflow-x:clip}` at `sat-platform.css:617` and `globals.css:220` means clipped overflow is not scrollable overflow: measured, the defect track put a 320px card inside a 272px box at the 400 percent reference, an overhang of 48px, and 8px of overhang at 360, while `document.documentElement.scrollWidth - clientWidth` reported zero in both cases. The card's right edge was cut off and unreachable rather than scrollable, which is why no automated check on this site had seen it and why `scripts/reflow-probe.mjs` measures per-item overhang instead. After the fix the card is 272px at 320 and 312 at 360 with zero overhang, and is unchanged from 390 upward (342, 382, two of 352 at 768, three of 349.3 at 1280) in both languages. Guarded by `src/lib/reflow.test.ts`, registered in `npm test`, which forbids an unescaped fixed floor anywhere under `src` and requires any survivor to be named with its reason. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 185 | P2 | Form inputs are set below 16px, which triggers focus zoom on iOS Safari | Open | Verified in three places. src/app/[locale]/post-requirement/RequirementForm.tsx line 433 declares the shared input style with `fontSize: 14`; src/app/[locale]/requirements/[id]/page.tsx line 313 declares its own with `fontSize: 13.5`; and the shared class they both sit on, `.input` at src/styles/sat-platform.css line 300, is `font-size:13.5px`. Because the two inline objects are spread onto the elements, they beat the stylesheet regardless. The codebase already knows the rule: src/styles/globals.css line 412 carries the comment that `--fs-input` stays 16px on purpose to prevent iOS focus zoom, and line 415 declares `--fs-input:16px`, but no rule on either requirement surface consumes it; the only consumers found in the tree are src/components/FilterBar.tsx and src/components/MarketingHome.tsx. The one mobile override that exists, src/styles/globals.css line 371, is scoped to `.advpanel input`. This is not a WCAG failure on its own, though SC 1.4.4 resize text and SC 1.4.10 reflow are engaged once the browser zooms the viewport without the user asking. Not fixed in this slice because the remedy is to route both inline styles through the declared token, and the two inline objects are shared by every control on their pages, so the change reaches layouts this pass did not measure. |
| 186 | P2 | Two h1 elements render together on the lister requirement surface | Open | Verified across a layout and its page. src/app/[locale]/dashboard/layout.tsx line 110 renders `<h1>{acctName}</h1>` inside the persistent top bar, and src/app/[locale]/dashboard/requirements/page.tsx line 176 renders `<h1 style={{ fontSize: 22, ... }}>{t.title}</h1>` as the page heading, so both are present in the same document whenever a lister opens the requirements queue that the Pitch links come from. Neither is inside a `<section>` with its own accessible name, so this is not the sectioning-element case. The three public requirement surfaces each carry exactly one: src/app/[locale]/requirements/page.tsx line 42, src/app/[locale]/requirements/[id]/page.tsx line 122, and src/app/[locale]/post-requirement/RequirementForm.tsx lines 198 and 238, which are mutually exclusive because line 198 sits inside the early return at line 190. SC 1.3.1 info and relationships and SC 2.4.6 headings and labels are engaged. Not fixed in this slice because demoting the top bar heading changes the heading outline of every page under the dashboard layout, which is a structural change across surfaces this pass did not read. |
| 187 | P1 | Client-fetched requirement board and detail content arrives outside any live region | Closed in PKG-A11Y-1 | Verified, and the note in the description is correct that the matches panel was fixed: src/app/[locale]/requirements/[id]/page.tsx line 165 now wraps the matches in `<div aria-live="polite" aria-busy={matchesLoading}>`. That was not the only instance. Two remain. src/app/[locale]/requirements/page.tsx fetches the whole board client-side at line 33 and swaps a loading line at lines 47 and 48 for a grid of cards at line 59, with no live region and no focus move, so a reader is told nothing when the board arrives. src/app/[locale]/requirements/[id]/page.tsx line 110 does the same for the entire requirement, returning a bare loading div and then replacing the whole document body at line 113. SC 4.1.3 status messages is engaged. Not fixed in this slice because announcing a whole-page swap through a live region is the wrong instrument; the honest remedy is a route-level loading boundary and a focus move to the new heading, which is a structural change to how these two routes are rendered. Closed in slice L, and half of that deferral was wrong. The route-level loading boundary does nothing here: the route itself resolves immediately and it is the browser's own fetch, started on mount, that arrives late, so there is no server wait for a boundary to cover. The focus move is worse than useless and was deliberately not implemented, which is the second time in this package the register's own proposed remedy has been corrected rather than applied. A screen reader user who lands on either URL does not wait in silence for the request: they begin reading the heading and the subtitle with the virtual cursor, which walks the document without moving DOM focus. `document.activeElement` is `document.body` for that reader and for a reader who has touched nothing, so no guard written around focus can distinguish them, and calling `focus()` when the data settles would drag an attentive reader back to the top of a page they were already part way through. Focus is moved when the user asked for the change. Nobody asked for this one; the page finished loading. The instrument that fits is a status message, which is exactly what SC 4.1.3 is for, and it moves nobody. Both routes now carry a `.sronly` `role="status" aria-live="polite"` region and set `aria-busy` on the container while the request is in flight. The structural care is in where the region sits rather than in its attributes: a live region announces changes inside an element the browser was already watching, so a region created in the same render as its text says nothing. `requirements/page.tsx` holds the region outside the loading branch entirely. `requirements/[id]/page.tsx` had three early returns with three unrelated root elements, which is the shape that breaks this, and was restructured so every branch opens with the same root and the same region as its first child, which is what makes React preserve it across the swap. The board's messages are the count that arrived, the empty-market sentence, or the failure sentence; the detail's are that the requirement loaded, that it does not exist, or that a response was registered. Two further repairs came with it. The board's `.catch` set `loading` to false and nothing else, so a request that failed fell into the empty branch and told the visitor that no occupier in the country is looking for space, which is the ELITE-4 J4-5 defect in a second place; it now states the failure, distinguishes it from an empty market in the sentence itself, and offers a retry. And a `!r.ok` response was being parsed as JSON and treated as success, so a 500 produced an empty board rather than a failure. Five guard tests in `src/lib/formGroups.test.ts` cover both regions, the region count on the detail page, the catch and the retry; one of them asserts the absence of `.focus()` in the board file, because a deliberate absence with no test against it reads as an oversight to the next reader and gets helpfully corrected. Evidence classification: automated source scan and guard tests, plus the browser-emulated reflow probe confirming both surfaces are unchanged at every width. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 188 | P2 | The attachment radios clear the 24px floor but only below 1025px | Open | Verified against the central rule as instructed. The radios at src/app/[locale]/requirements/[id]/page.tsx lines 216 to 221 ARE wrapped in a label that directly contains them, `<label className="row gap8" ...>` opening at line 215 with the input as an immediate child, so the new rule at src/styles/globals.css line 274, `label:has(> input[type="radio"]){min-height:44px;}`, does match them, and line 273 gives the input itself `min-width:24px;min-height:24px`. What remains is narrower than the finding as recorded. Both rules sit inside the `@media(max-width:1024px)` block opened at line 267, so at 1025px and above neither applies and the radio falls back to the user agent default of roughly 13px with no label floor; a touchscreen laptop or a tablet in landscape gets the unpadded control. The 44px rule also depends on `:has()`, which has no fallback here. SC 2.5.8 target size minimum, which asks 24px, is engaged for the wide-viewport case, and SC 2.5.5 target size enhanced would ask 44px throughout. Not fixed in this slice because the correct remedy is to key the floor on `(pointer:coarse)` rather than on viewport width, and that change reaches every checkbox and radio in the product. |
| 189 | P2 | Every row on the lister queue offers a link named only "Pitch" | Open | Verified in src/app/[locale]/dashboard/requirements/page.tsx at line 244: inside the `rows.map` opened at line 204, each row renders `<Link href={`/${lp}/requirements/${b.id}`} className="btn secondary sm rowact">{t.pitch}</Link>`, and `t.pitch` resolves to "Pitch" at line 81 of the same file and to `تقدّم` at line 65, mirrored in src/i18n/dictionaries/en.json and ar.json at line 663. Every row therefore contributes an identically named link to a different destination, so a links list gives the reader no way to tell them apart. The requirement title that would distinguish them is a plain `<span>` at line 229, not part of the link. SC 2.4.4 link purpose in context is partly answered by the row, but SC 2.4.9 link purpose link only is not, and SC 4.1.2 name role value bears on it. Not fixed in this slice because appending the requirement title to the name needs a dictionary pattern string in both languages, and this pass was instructed not to edit src/i18n/dictionaries. |
| 190 | P2 | Requirement board card links take their whole card as an accessible name | Open | Verified in src/app/[locale]/requirements/page.tsx at line 67, where the card is a single `<Link ... className="card pad lift">` whose children run to line 89 and include the asset and deal tag, the reference code, the title, the district and city line, the size and budget line, the timeline line, up to four must-have chips, the interest count and the call to action. All of it is text content with no `aria-hidden` and no `aria-label` on the link, so the computed accessible name is the concatenation of roughly a dozen fragments, read as one uninterrupted paragraph. SC 2.4.4 link purpose in context and SC 2.4.9 link purpose link only are engaged, and SC 4.1.2 name role value bears on it. Not fixed in this slice because the two honest remedies, either giving the link an explicit short name and hiding the decorative fragments, or shrinking the link to the title and making the card a non-interactive container, are both structural rewrites of the card larger than a bug fix, and the second changes the click target the whole board depends on. |
| 191 | P2 | Two different validation failures emit byte-identical error text | Open | Verified in src/app/[locale]/post-requirement/RequirementForm.tsx: line 116 sets `e.title = pr.errRequired` when the requirement title is blank, and line 120 sets `e.name = pr.errRequired` when the contact name is blank. Both read the same key, whose value at src/i18n/dictionaries/en.json line 900 is "This is needed before the requirement can be posted." Each message is correctly anchored to its own field through `fieldNote` at line 228 and `aria-describedby`, so the association is not lost, but the sentence itself names neither field and the two are indistinguishable in an error summary or a screen reader's forms list. The neighbouring rules do this properly: line 117 uses `pr.errLocation` and line 121 uses `pr.errEmail`, both of which name what is wrong. SC 3.3.1 error identification and SC 3.3.3 error suggestion are engaged. Not fixed in this slice because splitting the shared key into two field-specific sentences means adding dictionary values in both languages, and this pass was instructed not to edit src/i18n/dictionaries. |
| 192 | P1 | The consent label promises a withdrawal that has no route, control or channel | Closed in PKG-A11Y-1 | Verified by search, and the promise is real in both languages. src/i18n/dictionaries/en.json line 892 ends the consent sentence with "I can withdraw this at any time.", and ar.json line 892 ends with `ويمكنني سحب هذه الموافقة في أي وقت.` The label is rendered at src/app/[locale]/post-requirement/RequirementForm.tsx line 400 beside a checkbox that must be ticked before a requirement can be posted. Stating what the search found: the requirements API exposes only `GET` and `POST` on src/app/api/requirements/route.ts at lines 14 and 70, a single `GET` on src/app/api/requirements/[id]/route.ts line 23, a `POST` on the interest route and a `GET` on the matches route; there is no `DELETE`, no `PATCH`, no unpublish and no consent-revocation handler anywhere under src/app/api. A case-insensitive search for withdraw, revoke, unsubscribe and opt out across src/app, src/lib, src/components and supabase returns only unrelated matches: advertising permit expiry, search chip removal, listing verification retraction and admin account verification. No requirement surface offers a control, and the sentence names no email address or contact route. SC 3.3.2 labels or instructions is engaged, and the wider issue is that the label makes a commitment the product cannot honour. Not fixed in this slice because the remedy is either to build the withdrawal path or to reword a consent string, and both are decisions for the owner rather than an accessibility repair; the wording also sits in the dictionaries this pass was instructed not to edit. Closed in PKG-A11Y-1 by removing the untrue sentence rather than by building the control, and the reason for that choice is recorded here so it is not mistaken for the easy option. A requirement is posted anonymously: src/app/api/requirements/route.ts reads no session and the RPC stores contact_name, contact_email and contact_phone against no account, so there is no identity a later withdrawal could be authenticated against. The one self-service proof available, the public reference code paired with the contact email the poster typed, would need a security definer function because supabase/migrations/20260712213623_rls_tighten_pii_tables.sql locks the PII tables, and every Supabase tool in this environment is permission denied, so such a function can be neither created nor tested; shipping a control that calls a function which does not exist would be a worse defect than the sentence. Emailing a withdrawal link is held by the O12 ruling. The /contact page does carry a privacy and data requests section covering withdrawal of consent, but its address is still a COUNSEL placeholder in src/lib/legalContent.ts, so pointing the label at it would move the untruth one hop rather than remove it. The English label now ends "This preview has no self-service control for withdrawing that consent yet." and the Arabic ends ولا تتوفر في هذه النسخة التجريبية أداة ذاتية لسحب هذه الموافقة بعد. The missing control is not thereby dismissed: it is finding 193. |
| 193 | P1 | A requirement poster has no way to withdraw the consent that shares their contact details, and the right exists whether or not the product offers it | Open | Split out of finding 192 when that finding's untrue label was corrected, so that removing the sentence does not also remove the obligation behind it. PDPL gives a data subject the right to withdraw consent; src/app/api/requirements/route.ts obtains that consent through a required checkbox and stores contact_name, contact_email and contact_phone through the create_requirement RPC, and no route, control or working channel then lets the person reverse it. The user consequence is that contact details shared with owners and licensed brokers stay shared. Three dependencies, each of which must clear before this can close. First, identity: a requirement is posted with no account and no session, so a withdrawal must either be authenticated by a shared secret, the public reference code paired with the contact email that was recorded and is not published on the board, or the flow must move behind an account. Second, database: the PII tables are locked by supabase/migrations/20260712213623_rls_tighten_pii_tables.sql, so an anonymous write needs a security definer function, and the Supabase tooling in the build environment is permission denied, so the migration must be applied by the owner rather than by the build. Third, O5: /contact still carries COUNSEL placeholders instead of a privacy address, so even a manual withdrawal route has nowhere to land. The smallest complete remedy is a security definer function that, on an exact match of reference code and recorded contact email, clears the three contact columns and closes the brief, plus one public route and one small page in both languages, plus the consent label reverting to a statement of the control once it exists. Not attempted in PKG-A11Y-1 because the second dependency cannot be satisfied from this environment. The identity question is now written up as decision O18 in `docs/decision-register.md`, which sets out why neither `ref_code` nor the recorded email can serve as a secret, why the one-time emailed link is blocked twice over, the four mechanisms open to the owner, the risks that bind whichever is chosen, and the recovery path. Nothing is implemented until the owner rules on O18. |
| 194 | P2 | The global mobile `h1` rule is unscoped and `!important`, so every dashboard page title renders up to 2.5rem on a phone | Open | `src/styles/sat-platform.css:611` declares a bare `h1{font-size:clamp(1.625rem,8vw,2.5rem)!important;line-height:1.1!important;}` inside its `@media(max-width:600px)` block. It names no context and it is `!important`, so it reaches every first-level heading on the site, including the dashboard body titles, which are declared at 1.375rem and are page labels rather than marketing headlines. Measured rather than inferred, on the topbar account name that matched the same rule until RC6 demoted it: 26px at 320, 31.2px at 390 and 34.4px at 430, against the 19px its own declaration asks for, in both locales. The same treatment reaches the titles on listings, enquiries, the enquiry detail, requirements, profile, the new-listing flow, the listing detail, and now the overview and viewings, which are the nine routes under `/dashboard`. Found while measuring RC6 rather than reported from a journey. Not fixed there: it is a type-scale decision across every route that has a heading, the standing rule against another broad cosmetic sweep applies, and Codex's instruction for PKG-A11Y-1 is not to reduce visual quality to satisfy accessibility but to resolve the design properly. Assigned to the parked visual-quality package alongside findings 27, 47, 50 and 75. |
| 195 | P2 | The comparison page loading skeleton gives its label column 59 percent of the row at 400 percent zoom, leaving three 25px data columns | Open | Found while measuring RC7 rather than reported from a journey, and recorded instead of patched because the obvious repair measured as a no-op. `src/app/[locale]/compare/loading.tsx:6` declares `gridTemplateColumns: "160px repeat(3, 1fr)"`. The candidate rewrite `minmax(0, 160px) repeat(3, minmax(0, 1fr))` was measured in `scripts/reflow-probe.mjs` at seven viewports in both directions and resolves identically to the shipped value at every one of them: 160px, 25.33, 25.33, 25.34 at a 320 viewport and 160px, 328, 328, 328 at 1280. The first track takes its 160px maximum whenever the row fits, and it fits, because the skeleton divs have zero min-content. Shipping it would have been a change with no effect presented as a fix. The real observation is the one in the title: at the 400 percent reference the fixed label column takes 160 of a 272px row and the three comparison columns get 25.3px each, which is not a usable skeleton, and the same track is what the loaded table will inherit if it is copied. The remedy is a layout decision for the comparison surface, either a proportional label track or a stacked presentation below a breakpoint, not a substitution. SC 1.4.10 reflow is engaged. Not fixed in PKG-A11Y-1: the surface is outside the four critical journeys, and the standing rule against another broad cosmetic sweep applies to redesigning it here. Evidence classification: browser-emulated measurement only, English and Arabic, seven viewports; not tested on a physical device, not tested with an actual screen reader and not independently audited. |
| 196 | P2 | Choosing a location suggestion re-runs the search on the label it just wrote back, so the list reopens on the item already chosen | Closed in PKG-A11Y-1 | Found while implementing finding 153, not reported from a journey. `choose()` writes the picked label back into the search box, and that is a change to `q` like any other, so the debounced `[q]` effect fires 350ms later, refetches `/api/geocode` and reopens the dropdown on the place the lister has already accepted. Under the old `<div>` of `<button>` elements this was untidy and easy to miss. Under a combobox with a live region it is worse: the reopen sets `aria-expanded` back to true and re-announces a list of suggestions the lister did not ask for, immediately after telling them their choice was taken, which is a status message that contradicts the action that caused it. Severity P2 because nothing is lost or misstated and the map pin is already placed; it is noise, not an error. Fixed by holding the one query string that must not be searched in a `chosen` ref and returning early at `src/components/LocationPicker.tsx:121`, after the length guard and before the fetch. The ref is cleared on any keystroke in the input's `onChange`, so typing the same string again deliberately does search: the suppression is of one echo, not of a query. Guarded by "choosing a suggestion does not reopen the list on the thing just chosen" in `src/lib/formGroups.test.ts`, which asserts both the early return and the clearing `onChange`, because a fix that suppressed the echo without releasing it would be a worse defect than the one it replaced. Evidence classification: automated source scan plus browser-emulated measurement in Chromium at seven viewports from 320 by 256 to 1280 by 1024, in English and Arabic. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 197 | P1 | Signup's organisation-type and role choices declare `role="radiogroup"` but have no roving tabindex and no arrow keys, so every option is its own tab stop and the group cannot be operated the way its role advertises | Closed in PKG-A11Y-1 | Found by scanning for the finding 182 defect class across the repository rather than reported from a journey, and it is the reason slice I was scoped by root cause instead of by finding. `src/components/SignupFlow.tsx:45-47` built its `sel()` helper as a `<div role="radiogroup" aria-labelledby="lg-...">` of `<button type="button" role="radio" aria-checked={...}>` elements. The markup is not a mislabelling like 182: the role is the correct role, and it is claimed and then not honoured, which is the worse of the two. A `radiogroup` promises a screen-reader user and a keyboard user that the set is one stop and that the arrows move within it. Here each button kept its default tab stop, so the set was five stops for the organisation type, and no key handler existed at all, so the arrows did nothing. A keyboard user who has been told by the role that arrows will work presses them and the control does not respond. SC 4.1.2 name role value and SC 2.1.1 keyboard are both engaged; severity P1 because this sits in journey 1, authentication and organization onboarding, and is on the path every organization takes exactly once. Fixed by the same substitution as 182: `useId()` supplies a per-instance prefix, each option is a `<label class="chip">` wrapping `<input type="radio" name={`${uid}-${k}`}>`, and the `<div role="radiogroup">` became a `<fieldset>` with the existing label text as its `<legend>`, which retires the `aria-labelledby` and the generated `id` with it. The `useId()` prefix is not decoration: `name` is document scoped, and two `sel()` groups keyed the same way on one page would otherwise merge into a single radio group and unchoose each other. The multi-select asset chips at `:139` and `:154` in the same file were deliberately left alone; they are genuine independent toggles over an array and `aria-pressed` is correct there, which is the same distinction finding 182 drew against the must-haves in `RequirementForm`. Evidence classification: automated source scan, plus browser-emulated measurement in Chromium through `scripts/radio-probe.mjs` in English and Arabic under a coarse pointer; the group now reports one tab stop, arrows that walk and wrap, direction-correct horizontal arrows, a 2px focus ring on the label, and every target at or above 44 by 44. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 198 | P1 | The viewing-slot rail and the qualifying answers on a listing enquiry claim `role="radiogroup"`, have no keyboard support, clear the chosen option when it is chosen again, and announce their own name twice | Closed in PKG-A11Y-1 | Found by the same repository-wide scan as finding 197. `src/components/ListingEnquiry.tsx:280-282` and `:294-296` each built a `<div role="radiogroup">` of `<button role="radio" aria-checked>` chips, and each lost a different part of the contract, which is why they are recorded together rather than as instances of 197. First, the keyboard: no roving tabindex and no arrow handling, so five viewing slots were five tab stops and the arrows were inert, exactly as in 197. Second, and this one is specific to these two groups, the handlers were `setSlot(slot === sl.iso ? null : sl.iso)` and `setQual(p => ({...p, [q.k]: p[q.k] === o.v ? "" : o.v}))`: choosing the option already chosen cleared it. A radio group has no path back to nothing chosen once something is, so the control was doing something its declared role forbids, and a user who had been told these were radios would have no reason to expect a second press to undo the first. Third, each group carried an `aria-label` identical to the text of its own legend, so the name was announced twice. SC 4.1.2 and SC 2.1.1 are engaged; severity P1 because this is the end of journey 3, search to listing detail to enquiry, and booking a viewing is the conversion action of that journey. Fixed by the same substitution: `useId()` prefixed names, `<label class="chip">` wrapping `<input type="radio">`, `<fieldset>` and `<legend>` in place of the div and its `aria-label`, the viewing legend kept `.sronly` because the heading above it is the visible name. The deselect was removed rather than reproduced, and that is a deliberate behaviour change recorded here so it is not mistaken for an oversight: if an explicit no-preference state is ever wanted it belongs in the group as an option with a written label, not as an invisible second meaning attached to the option already chosen. Two guard tests in `src/lib/formGroups.test.ts` assert the two handler shapes are gone by their exact text, because a regression here would be silent. Evidence classification: automated source scan, plus browser-emulated measurement in Chromium through `scripts/radio-probe.mjs` in English and Arabic under a coarse pointer, which presses Space twice on the chosen option and asserts it is still chosen. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 199 | P1 | Advancing or returning a signup step unmounts the button that holds focus, so focus falls to document.body with nothing announced and the next Tab restarts at the top of the document | Closed in PKG-A11Y-1 | Found while fixing finding 145, not reported from a journey. `src/components/SignupFlow.tsx` renders exactly one of three step panels at a time, and every transition between them is driven by a `setStep(...)` hung on a `Continue` or `Back` button that is inside the panel being replaced. The element that holds focus is therefore destroyed by the same render that draws the next step. Focus falls to `document.body`, nothing is announced, and a keyboard user's next Tab starts again from the top of the document rather than from the step that just opened, so reaching the first field of step 2 costs the whole header every time. SC 2.4.3 focus order is engaged, and this is the identical defect class to ELITE-4 J1-5, already fixed in the same file for the success panel: the repair existed one screen later and had never been applied to the two transitions that come before it. Severity P1 because it sits in journey 1, authentication and organization onboarding, and every account passes through both transitions. Fixed by making the wrapper `<div>` returned by the non-done branch a `tabIndex={-1} role="group"` whose `aria-label` is the step line and the step name, `Step 2 of 3, About your work` / `الخطوة 2 من 3، عن عملك`, and focusing it from a `useEffect` on `step`. A `mounted` ref guards the first render: moving focus on mount would drag the viewport past the page heading for someone who arrived by ordinary navigation, which is a different SC 2.4.3 failure rather than a fix for this one. The container's focus indicator is not suppressed, and a guard test asserts no `outline: none` appears in the file, because the usual accompaniment to a programmatically focused container is a rule that hides the ring it draws. Four assertions in `src/lib/formGroups.test.ts` cover the focus call, the first-render guard, the named focusable wrapper and the absent outline suppression. Evidence classification: automated source scan and guard tests. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 200 | P1 | The listings map announces itself as a modal dialog whose backdrop is inert whenever it has been opened, including after the layout has widened and drawn it inline beside the results | Closed in PKG-A11Y-1 | Found while settling finding 167, not reported from a journey. `src/components/ListingsMap.tsx` keyed every dialog semantic on one boolean, `open`, which records that the user pressed the toggle and knows nothing about the viewport. But the toggle only exists below 1080px, and the overlay behaviour only exists below 1080px: above it `sat-platform.css:695-706` draws the same panel as a sticky column inside `.lst-split`, in normal document flow, beside the results it belongs to. So a map opened on a narrow window and then widened past the breakpoint, which is an ordinary rotation on a tablet or a window drag on a desktop, was drawn inline while still carrying `role="dialog"` and `aria-modal="true"`. The consequence is worse than a wrong label: `aria-modal="true"` is an instruction to assistive technology to treat everything outside the element as inert, so the results list, the filters and the view pair sitting visibly next to the map were removed from the accessibility tree while remaining fully operable by mouse and keyboard. A screen reader user would be told the page had one region when it had a page, and the only way out was a close button that the widened layout had set to `display:none`. The focus trap ran in the same state, so Tab cycled inside a panel that was not covering anything. SC 4.1.2 name role value and SC 2.4.3 focus order are engaged. Severity P1, journey 3, search and listing detail. Fixed by making the layout, not the button press, decide what the panel is. An `overlay` state tracks `window.matchMedia("(max-width:1080px)")` and is kept in sync by a `change` listener, `modal` is `open && overlay`, and crossing back above the breakpoint also sets `open` to false so the panel does not silently remain in a state the widened layout cannot show a way out of. `role`, `aria-modal`, the focus trap and its Escape handler are all gated on `modal` rather than `open`, so above the breakpoint the panel is a plain `role="region"` with the same accessible name. The trap's focus return additionally checks `toggle.offsetParent !== null` before calling `focus()`, because a `display:none` element accepts the call and drops focus to the body, which would have replaced this defect with finding 199's. `matchMedia` is read inside an effect and not during render, so the server and the first client render agree and there is no hydration mismatch; the panel is simply not a dialog until the client has measured, which is the safe direction to be wrong in. A guard test asserts the semantics are keyed on `modal` and that `modal` is defined from both terms. Evidence classification: automated source scan and guard tests, plus the browser-emulated reflow probe, which renders this surface from 320 through 1280 CSS pixels and confirms the breakpoint the fix reads is the breakpoint the stylesheet uses. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 201 | P1 | Registering interest in a requirement destroys the panel holding the focused submit button, so focus falls to document.body and nothing says the response was saved | Closed in PKG-A11Y-1 | Found while fixing finding 187, not reported from a journey. In `src/app/[locale]/requirements/[id]/page.tsx` the response form is a disclosure: a button labelled from `t.haveSpace` sets `show`, and the panel with the message box, the match list and the submit button is rendered only while `show` is true. On a successful registration the handler ran `setShow(false)`, and the element holding focus at that instant is the submit button inside the panel that call unmounts. Focus fell to `document.body`. This is finding 199 exactly, one journey over: the next Tab restarts at the top of the document, and for a keyboard user the cost of registering interest is the whole page header again. It is compounded here because the confirmation is also silent. The evidence that the response was saved is that the count in the heading above went up by one and an entry appeared in a list further down, both of which are visual, both of which are below the fold on a phone, and neither of which is announced. A responder using a screen reader submitted a message and was told nothing at all. SC 2.4.3 focus order and SC 4.1.3 status messages are engaged. Severity P1, journey 4, requirement creation and matching, and this is the conversion action of that journey. Fixed by giving the disclosure button a ref and returning focus to it, which is the ordinary behaviour when a disclosure closes and puts the user back at the control they operated, and by carrying the confirmation on the reload rather than asserting it before the reload proves it: `load()` takes an optional sentence and sets it into the same status region finding 187 installed, so the words appear only in the render that also shows the response in the list. The order matters and is deliberate: focus moves first, then the fetch resolves and the region updates, so a reader is at the disclosure button when the confirmation is spoken rather than being moved after it. Two guard tests in `src/lib/formGroups.test.ts` cover the focus return and the ref on the disclosure. Evidence classification: automated source scan and guard tests. Not tested on a physical device, not tested with an actual screen reader and not independently audited; WCAG 2.2 AA conformance is not claimed from this. |
| 202 | P2 | A requirement whose Arabic title was never written is shown to an Arabic reader in English, on both requirement surfaces | Open | Found while fixing finding 171 on journey 4, not reported from a journey. `src/app/[locale]/requirements/page.tsx` and `src/app/[locale]/requirements/[id]/page.tsx` both render a brief's title as `(ar && r.titleAr) || r.title`, and the district beside it as `(ar && r.districtAr) || r.district`. The fallback is a language crossing: where the occupier filed only an English title, an Arabic reader is handed English prose inside an Arabic page, and there is no marker that this has happened. The platform already knows this is wrong and already refuses it one journey over. `listingTitle()` in `src/lib/listingTitle.ts` falls from an own-language title to a composed `"{type} in {place}"` built from the dictionary, then to the asset type alone, then to the reference code, and its doc comment states the principle in as many words: the other language's title is deliberately not a rung, because showing an Arabic reader an English sentence is the same parity defect wearing a different shirt. The requirement path has no equivalent. Severity P2 rather than P1 because the text shown is the occupier's own and is not misstated, and because slice N has made the crossing honest where it occurs: both sites now declare the script of whatever the fallback produced, so the run is announced with the right phonetics and resolved in the right direction. What remains is the parity decision itself, which is a product question rather than a defect of markup: whether a requirement with no Arabic title should compose one from its own structured fields, the way `listingTitle` composes, or should be shown with an explicit marker that the text is in the other language. Not fixed in this slice because composing a requirement title needs a decision about which fields carry it and how a brief with neither a title nor a usable structure is named, and inventing that inside an accessibility package would be a product change smuggled in as a repair. |
| 203 | P1 | Sixteen client sites render a server-composed English sentence to readers of both languages, and four of them render a bare English literal as their own fallback | Fixed and awaiting deployment verification | Found while establishing the true scope of finding 22, not reported from a journey. Closed systemically across six slices, `bbdc22b`, `b731f7f`, `81844ed`, `085a4bc`, `0d62cb5` and `994f02e`, each confirmed READY at its own matching `meta.githubCommitSha`. **The measured end state, read from the source at `994f02e` and not carried forward from any slice narrative: 127 refusals across 16 route files, every one of them stating a stable code; 76 codes in the shared table; 14 client files naming those codes through `apiErrorMessage`, plus `AvailabilityReaffirm` naming its route's codes through the finding 22 table, which is 15 client files in total.** The remedy is the division finding 22 established and is deliberately not a second convention: the route states the reason as a stable `code`, `error` stays on the wire in English because a log and an API consumer read it, no client renders it, and `src/lib/apiErrors.ts` names the code in the language the page is already rendering, because the reader's language is a property of the page and not of the request body. Two tables exist and must not merge: `listingIntakeErrors.ts` serves the two listing intake routes with a two-argument function and a built-in fallback, `apiErrors.ts` serves the other fourteen with a three-argument function taking the caller's own contextual sentence, and a client uses the table its own route already speaks to. Three defects beyond translation were closed with it. Three routes in slice C and one in slice F returned PostgREST's own `error.message` to the browser, one of them on a public unauthenticated endpoint, which is an information disclosure and would still be a defect in a monolingual product; those four sentences now go to the log. Two routes composed an English enum or field name into the middle of a sentence, which cannot be translated at all, and emit two codes each instead. Four client sites tested the response payload for a reason rather than testing the status, so a refusal that stated no reason read as a success and one of them advanced the form to its confirmation screen. `src/components/ListingEnquiry.tsx` was the finding arriving from the other direction and is recorded separately because it reads as a false negative: it never put English on an Arabic page, it collapsed all twelve refusals into the single sentence it also shows for a dropped connection, so the language was right and the information was gone. One route is deliberately out of scope, `/api/requirements/[id]/matches`, whose eight refusals are never displayed; the reason is written into the guard file rather than left implicit. Eleven guards in `src/lib/apiErrors.test.ts` hold the scope, extended by the finding 22 client guard in `src/lib/formGroups.test.ts`: every refusal in scope states a code, every emitted code is nameable, every named code is emitted so nothing in the table is dead, no client renders the wire sentence, both halves of every entry are present and the Arabic half is Arabic, an unrecognised code falls to the caller's sentence, no sentence interpolates, no em dash reached the table or the routes it serves, no route guesses the reader's language from a header, no route puts the database's own sentence on the wire, and the publish gate's own reasons still survive to the client. **Not "Closed with live evidence", and this is a limit of the environment rather than of the work.** Every refusal in this finding is reached by a POST, and the only live channel available here is GET only and unauthenticated, so no refusal sentence can be exercised against the deployed build from this container. Closing this row requires one interactive session that submits an invalid enquiry, viewing request and requirement on the Arabic build and reads the rendered sentence. **A correction, recorded rather than quietly restated: the slice D, E and F commit messages state the table reached 56, 73 and 80 codes. Those were running additions, never measured. The measured figure at `994f02e` is 76. The commit messages are immutable and are left as written; this row is the corrected arithmetic.** |
| 204 | P1 | The release-state disclosure was nested inside the header node, so every APP-tier route withheld it, including `/advisor`, which is publicly reachable | Closed in PKG-A11Y-1 | Found while fixing finding 147, not reported from a journey. `src/app/[locale]/layout.tsx` built the preview notice and the marketing strip as the two branches of one ternary INSIDE the `header` node it passed to `ChromeGate`, so a switch whose stated purpose is navigation was also deciding a disclosure. The layout's own comment says the notice is "persistent and site wide rather than a per-page banner that some routes forget to mount"; the APP tier defeated exactly that. `globals.css:285` hides every `.topnotice` except `.preview-notice` below 640px, which is direct evidence the preview notice is treated as a disclosure that must survive everywhere. User consequence: `/advisor` is in `SITEMAP_ROUTES` in `src/lib/routePolicy.ts`, so it is publicly indexable once the release gates pass, and it was rendering figures with nothing on the page saying the deployment is sample data. `/docs` renders its own `SampleBanner`; `/dashboard`, `/admin` and `/messages` are session gated; `/advisor` is neither. Fix: `ChromeGate` takes a `notice` slot rendered unconditionally on all three tiers, and the layout hoists the ternary into `previewNotice` and `marketingNotice`. Exactly one is non-null for a given PREVIEW value and the notice slot precedes the header slot, so the rendered DOM order is byte-identical to before. Two guards hold the structure: that `{notice}` is not on a line mentioning `tier`, and that the header node contains no `preview-notice`. Evidence: automated source scan and guard tests. Not verified against a live APP-tier render, because `/advisor`, `/dashboard`, `/admin` and `/messages` are session gated and no session is obtainable in this container. |
| 205 | P1 | `/docs` renders no links at all, and the control that looks like a back button is a `<span>` | Closed in PKG-A11Y-1 | Found while checking finding 147's claim that it "does not generalise to any other route", which was recorded in `docs/a11y-p1-triage.md` and turned out to be true only of navigation. `src/app/[locale]/docs/page.tsx` contained zero `<Link>` elements. Its top bar drew a 32px disc holding a chevron, in the corner where a back control belongs, as a `<span>`: not focusable, not activatable and pointing nowhere. "Download all" and "Share" are `<span className="btn secondary sm">` for the same reason, and remain non-functional by design because the documents are sample data. User consequence: `/docs` is an APP route, so it has no header, no footer and no tab bar, and both of its rails are hidden below 980px and 680px (sat-platform.css:623-624). A keyboard or screen-reader user reaching it had no route off the page but the browser's back button, which SC 2.4.5 does not accept as a way to reach a page. Fix: the chevron is a real `<Link href={`/${params.locale}`}>` with `aria-label={d.backHome}`, a new bilingual key (EN "Back to home", AR "العودة إلى الرئيسية", matching `advisor.backHome`). The hit area is 44px to clear the SAT touch floor while the disc stays 32px, with a negative margin absorbing the difference so the row's layout is unchanged; the inner disc is `aria-hidden`. The guard walks every APP route, follows one level of `@/components` import, and requires either a `redirect()` or a `<Link>` whose href leaves the route. Evidence: automated source scan and guard tests. Not exercised in a browser, because `/docs` is behind `PRIVATE_PREFIXES` and the local production build cannot run in this container. |
